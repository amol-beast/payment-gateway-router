This file is a merged representation of a subset of the codebase, containing specifically included files and files not matching ignore patterns, combined into a single document by Repomix.

<file_summary>
This section contains a summary of this file.

<purpose>
This file contains a packed representation of a subset of the repository's contents that is considered the most important context.
It is designed to be easily consumable by AI systems for analysis, code review,
or other automated processes.
</purpose>

<file_format>
The content is organized as follows:
1. This summary section
2. Repository information
3. Directory structure
4. Repository files (if enabled)
5. Multiple file entries, each consisting of:
  - File path as an attribute
  - Full contents of the file
</file_format>

<usage_guidelines>
- This file should be treated as read-only. Any changes should be made to the
  original repository files, not this packed version.
- When processing this file, use the file path to distinguish
  between different files in the repository.
- Be aware that this file may contain sensitive information. Handle it with
  the same level of security as you would the original repository.
</usage_guidelines>

<notes>
- Some files may have been excluded based on .gitignore rules and Repomix's configuration
- Binary files are not included in this packed representation. Please refer to the Repository Structure section for a complete list of file paths, including binary files
- Only files matching these patterns are included: app/**/*, config/**/*, database/**/*, resources/**/*, routes/**/*
- Files matching these patterns are excluded: vendor/, node_modules/, storage/, bootstrap/cache/, public/build/, .env, *.log
- Files matching patterns in .gitignore are excluded
- Files matching default ignore patterns are excluded
- Files are sorted by Git change count (files with more changes are at the bottom)
</notes>

</file_summary>

<directory_structure>
app/Classes/Encryption.php
app/Classes/PaymentGateways/ICICI.php
app/Classes/PaymentGateways/PaymentGateway.php
app/Classes/PaymentGateways/PaymentGatewayFactory.php
app/Concerns/PasswordValidationRules.php
app/Concerns/ProfileValidationRules.php
app/Contracts/PaymentGatewayInterface.php
app/DTO/PaymentRequestDTO.php
app/DTO/PaymentResponseDTO.php
app/Enums/ClientApiLogResult.php
app/Enums/ConnectionType.php
app/Enums/PaymentMethod.php
app/Enums/PaymentType.php
app/Enums/SubscriptionPeriod.php
app/Enums/SubscriptionType.php
app/Enums/TransactionStatus.php
app/Enums/TransactionType.php
app/Events/ClientApiEvent.php
app/Events/PgTransactionEvent.php
app/Filament/Pages/Utils.php
app/Filament/Resources/ClientConnections/ClientConnectionsResource.php
app/Filament/Resources/ClientConnections/Concerns/ValidatesClientConnection.php
app/Filament/Resources/ClientConnections/Pages/CreateClientConnections.php
app/Filament/Resources/ClientConnections/Pages/EditClientConnections.php
app/Filament/Resources/ClientConnections/Pages/ListClientConnections.php
app/Filament/Resources/ClientConnections/Pages/ViewClientConnections.php
app/Filament/Resources/ClientConnections/Schemas/ClientConnectionsForm.php
app/Filament/Resources/ClientConnections/Schemas/ClientConnectionsInfolist.php
app/Filament/Resources/ClientConnections/Tables/ClientConnectionsTable.php
app/Filament/Resources/Clients/ClientsResource.php
app/Filament/Resources/Clients/Pages/CreateClients.php
app/Filament/Resources/Clients/Pages/EditClients.php
app/Filament/Resources/Clients/Pages/ListClients.php
app/Filament/Resources/Clients/Pages/ViewClients.php
app/Filament/Resources/Clients/Schemas/ClientsForm.php
app/Filament/Resources/Clients/Schemas/ClientsInfolist.php
app/Filament/Resources/Clients/Tables/ClientsTable.php
app/Filament/Resources/PGConnections/Concerns/ValidatesGatewayAttributes.php
app/Filament/Resources/PGConnections/Pages/CreatePGConnection.php
app/Filament/Resources/PGConnections/Pages/EditPGConnection.php
app/Filament/Resources/PGConnections/Pages/ListPGConnections.php
app/Filament/Resources/PGConnections/Pages/ViewPGConnection.php
app/Filament/Resources/PGConnections/PGConnectionResource.php
app/Filament/Resources/PGConnections/Schemas/PGConnectionAttributeSchema.php
app/Filament/Resources/PGConnections/Schemas/PGConnectionForm.php
app/Filament/Resources/PGConnections/Schemas/PGConnectionInfolist.php
app/Filament/Resources/PGConnections/Tables/PGConnectionsTable.php
app/Filament/Resources/Subscriptions/Pages/CreateSubscriptions.php
app/Filament/Resources/Subscriptions/Pages/EditSubscriptions.php
app/Filament/Resources/Subscriptions/Pages/ListSubscriptions.php
app/Filament/Resources/Subscriptions/Schemas/SubscriptionsForm.php
app/Filament/Resources/Subscriptions/SubscriptionsResource.php
app/Filament/Resources/Subscriptions/Tables/SubscriptionsTable.php
app/Filament/Resources/SubscriptionTransactions/Pages/CreateSubscriptionTransaction.php
app/Filament/Resources/SubscriptionTransactions/Pages/EditSubscriptionTransaction.php
app/Filament/Resources/SubscriptionTransactions/Pages/ListSubscriptionTransactions.php
app/Filament/Resources/SubscriptionTransactions/Pages/ViewSubscriptionTransaction.php
app/Filament/Resources/SubscriptionTransactions/Schemas/SubscriptionTransactionForm.php
app/Filament/Resources/SubscriptionTransactions/Schemas/SubscriptionTransactionInfolist.php
app/Filament/Resources/SubscriptionTransactions/SubscriptionTransactionResource.php
app/Filament/Resources/SubscriptionTransactions/Tables/SubscriptionTransactionsTable.php
app/Filament/Resources/Transactions/Pages/CreateTransactions.php
app/Filament/Resources/Transactions/Pages/EditTransactions.php
app/Filament/Resources/Transactions/Pages/ListTransactions.php
app/Filament/Resources/Transactions/Pages/ViewTransactions.php
app/Filament/Resources/Transactions/Schemas/TransactionsForm.php
app/Filament/Resources/Transactions/Schemas/TransactionsInfolist.php
app/Filament/Resources/Transactions/Tables/TransactionsTable.php
app/Filament/Resources/Transactions/TransactionsResource.php
app/Filament/Resources/Users/Pages/CreateUsers.php
app/Filament/Resources/Users/Pages/EditUsers.php
app/Filament/Resources/Users/Pages/ListUsers.php
app/Filament/Resources/Users/Schemas/UsersForm.php
app/Filament/Resources/Users/Tables/UsersTable.php
app/Filament/Resources/Users/UsersResource.php
app/Filament/Widgets/DashboardStatsWidget.php
app/Filament/Widgets/RecentTransactionsWidget.php
app/Http/Controllers/API/V1/PaymentController.php
app/Http/Controllers/Controller.php
app/Http/Controllers/UtilsController.php
app/Http/Middleware/HandleApiClientEncryptedRequest.php
app/Http/Middleware/HandleApiClientRequest.php
app/Http/Middleware/HandleApiRequest.php
app/Http/Middleware/VerifyHmacSignature.php
app/Http/Requests/Settings/ProfileDeleteRequest.php
app/Http/Requests/Settings/ProfileUpdateRequest.php
app/Http/Requests/V1/ValidatePaymentRequest.php
app/Listeners/LogClientApiEvent.php
app/Listeners/PgTransactionEventListener.php
app/Livewire/ClientApiLogsTable.php
app/Mail/TransactionStatusMail.php
app/Models/Client.php
app/Models/ClientApiLog.php
app/Models/ClientConnection.php
app/Models/ClientCustomer.php
app/Models/PGConnection.php
app/Models/Subscription.php
app/Models/SubscriptionTransaction.php
app/Models/SupportedPaymentGateway.php
app/Models/Transaction.php
app/Models/User.php
app/Policies/ClientConnectionsPolicy.php
app/Policies/ClientCustomerPolicy.php
app/Policies/ClientsPolicy.php
app/Policies/PGConnectionPolicy.php
app/Policies/SubscriptionPolicy.php
app/Policies/SubscriptionTransactionPolicy.php
app/Policies/TransactionPolicy.php
app/Policies/UserPolicy.php
app/Providers/AppServiceProvider.php
app/Providers/Filament/DashboardPanelProvider.php
app/Repositories/ClientConnectionRepository.php
app/Repositories/ClientRepository.php
app/Repositories/PgConnectionRepository.php
app/Services/TransactionService.php
config/app.php
config/auth.php
config/cache.php
config/data.php
config/database.php
config/filesystems.php
config/logging.php
config/mail.php
config/permission.php
config/queue.php
config/sanctum.php
config/services.php
config/session.php
database/.gitignore
database/factories/UserFactory.php
database/migrations/0001_01_01_000000_create_users_table.php
database/migrations/0001_01_01_000001_create_cache_table.php
database/migrations/0001_01_01_000002_create_jobs_table.php
database/migrations/2024_01_01_000000_create_passkeys_table.php
database/migrations/2025_08_14_170933_add_two_factor_columns_to_users_table.php
database/migrations/2026_07_11_165612_create_permission_tables.php
database/migrations/2026_07_14_140045_create_clients_table.php
database/migrations/2026_07_14_140046_create_client_customers_table.php
database/migrations/2026_07_14_140056_create_pg_connections_table.php
database/migrations/2026_07_14_140115_create_transactions_table.php
database/migrations/2026_07_14_140125_create_clients_connections_table.php
database/migrations/2026_07_15_013502_create_subscriptions_table.php
database/migrations/2026_07_15_013510_create_subscriptions_transactions_table.php
database/migrations/2026_07_15_114605_create_supported_payment_gateways_table.php
database/migrations/2026_07_17_153845_create_personal_access_tokens_table.php
database/migrations/2026_07_17_160000_create_client_user_table.php
database/migrations/2026_07_19_141015_add_transaction_type_to_client_connections_table.php
database/migrations/2026_07_19_150000_rename_transaction_type_on_clients_connections_table.php
database/migrations/2026_07_20_090000_create_client_api_logs_table.php
database/migrations/2026_07_21_100000_add_request_data_to_transactions_table.php
database/seeders/DatabaseSeeder.php
database/seeders/RolesAndPermissionsSeeder.php
database/seeders/SupportedPGSeeder.php
resources/views/emails/transaction-status.blade.php
resources/views/filament/modals/client-api-logs.blade.php
resources/views/filament/pages/utils.blade.php
resources/views/livewire/client-api-logs-table.blade.php
routes/api-v1.php
routes/api.php
routes/console.php
routes/web.php
</directory_structure>

<files>
This section contains the contents of the repository's files.

<file path="app/Classes/Encryption.php">
<?php

namespace App\Classes;

class Encryption
{

    static public function encrypt($data,$secret_key)
    {
        $cipher = 'aes-256-gcm';
        $ivLength = openssl_cipher_iv_length($cipher);
        $iv = random_bytes($ivLength);

        // Encrypt the data and capture the authentication tag
        $ciphertext = openssl_encrypt(
            json_encode($data),
            $cipher,
            $secret_key,
            OPENSSL_RAW_DATA,
            $iv,
            $tag
        );

        // Package IV, Tag, and Ciphertext together, then encode to Base64 for storage
        $encryptedData = base64_encode($iv . $tag . $ciphertext);
        return $encryptedData;
    }

    static public function decrypt($data,$secretKey)
    {
        $payload = base64_decode($data);
        $cipher = "aes-256-gcm";
        $ivLength = openssl_cipher_iv_length($cipher);

        // AES-256-GCM uses a standard 16-byte authentication tag
        $tagLength = 16;


        // Extract the component segments out of the unified binary string
        $iv = substr($payload, 0, $ivLength);
        $tag = substr($payload, $ivLength, $tagLength);
        $ciphertext = substr($payload, $ivLength + $tagLength);


        // Perform decryption and verify the authenticity tag
        $decryptedData = openssl_decrypt(
            $ciphertext,
            $cipher,
            $secretKey,
            OPENSSL_RAW_DATA,
            $iv,
            $tag
        );

        return $decryptedData;
    }
}
</file>

<file path="app/Classes/PaymentGateways/ICICI.php">
<?php

namespace App\Classes\PaymentGateways;

use App\Contracts\PaymentGatewayInterface;
use App\DTO\PaymentRequestDTO;
use App\DTO\PaymentResponseDTO;
use App\Enums\PaymentMethod;
use App\Enums\TransactionStatus;
use App\Models\Transaction;
use Carbon\Carbon;
use Devhammed\LaravelBrickMoney\Currency;
use Devhammed\LaravelBrickMoney\Money;
use Illuminate\Http\Request;
use Illuminate\Support\Facades\Http;

class ICICI implements PaymentGatewayInterface
{
    protected ?string $merchant_id;

    protected ?string $aggregator_id;

    protected ?string $encryption_key;

    protected ?string $sub_merchant_id;

    protected ?string $paymode;

    protected ?string $default_base_url;

    const PAYMENT_ENDPOINT = '/pg/api/v2/initiateSale';

    const STATUS_CHECK_ENDPOINT = '/pg/api/command';

    const REFUND_ENDPOINT = '/pg/api/command';

    const SETTLEMENT_DETAILS_ENDPOINT = '/pg/api/settlementDetails';

    public function __construct(array $pg_data)
    {
        $this->merchant_id = $pg_data['merchant_id'] ?? null;
        $this->aggregator_id = $pg_data['aggregator_id'] ?? null;
        $this->encryption_key = $pg_data['encryption_key'] ?? null;
        $this->default_base_url = $pg_data['default_base_url'] ?? null;
        $this->sub_merchant_id = $pg_data['sub_merchant_id'] ?? null;
        $this->paymode = $pg_data['paymode'] ?? null;
    }

    protected function getCurrencyCode($currency): string
    {
        if ($currency == 'INR') {
            return '356';
        }

        throw new \Exception('Invalid currency code.');
    }

    public function constructFromPaymentRequest(PaymentRequestDTO $paymentRequest, Transaction $transaction): array
    {
        $request_data = [
            'merchantId' => $this->merchant_id,
            'aggregatorID' => $this->aggregator_id,
            'merchantTxnNo' => $transaction->id.'-'.$paymentRequest->site_reference_id,
            'amount' => (string) $paymentRequest->amount->getAmount(),
            'currencyCode' => $this->getCurrencyCode($paymentRequest->currency),
            'payType' => '0',
            'customerEmailID' => $paymentRequest->customer['email'],
            'transactionType' => 'SALE',
            'txnDate' => Carbon::now()->format('YmdHis'),
            'returnURL' => route('handlePaymentResponse', ['pgClass' => 'ICICI']),
            'customerMobileNo' => $paymentRequest->customer['mobile'],
            'addlParam1' => (string) $transaction->id,
            'addlParam2' => $paymentRequest->site_reference_id,
        ];

        ksort($request_data);

        return $request_data;
    }

    protected function getConcatenatedRequestString($requestData): string
    {
        // assumes that request data is already sorted alphabetically by key
        $concatenatedRequestString = '';
        foreach ($requestData as $key => $value) {
            $concatenatedRequestString .= $value;
        }

        return $concatenatedRequestString;
    }

    protected function getHMacDigest($requestData): string
    {
        return hash_hmac('sha256', $this->getConcatenatedRequestString($requestData), $this->encryption_key);
    }

    protected function getPaymentEndpoint(): string
    {
        return $this->default_base_url.self::PAYMENT_ENDPOINT;
    }

    protected function getTransactionStatusEndpoint(): string
    {
        return $this->default_base_url.self::STATUS_CHECK_ENDPOINT;
    }

    public function getPaymentUrl(PaymentRequestDTO $paymentRequest, Transaction $transaction)
    {
        $requestObject = $this->constructFromPaymentRequest($paymentRequest, $transaction);
        $requestObject['secureHash'] = $this->getHMacDigest($requestObject);

        $response = Http::withHeaders([
            'Content-Type' => 'application/json',
        ])
            ->post($this->getPaymentEndpoint(), $requestObject);

        if ($response->failed()) {
            throw new \Exception('Payment Gateway Error: '.json_encode($response->body()));
        }
        $result = $response->json();

        if ($result['responseCode'] !== 'R1000') {
            throw new \Exception('Payment Gateway Error: '.json_encode($result));
        }

        return $result['redirectURI'].'?tranCtx='.$result['tranCtx'];
    }

    protected function mapResponseToPaymentResponseDTO($response): PaymentResponseDTO
    {
        // ICICI only supports INR currency, so hardcoding it here
        $amount = Money::of($response['amount'], 'INR');
        $pgFees = Money::of($response['oth_charge'], 'INR');

        $dbTransaction = Transaction::with(['client', 'pgConnection'])->find($response['addlParam1']);

        if (! $dbTransaction) {
            throw new \Exception('Transaction not found.');
        }

        $transaction = [
            'transactionDbId' => $response['addlParam1'],
            'siteReferenceId' => $response['addlParam2'],
            'status' => $response['responseCode'] === '0000' ? TransactionStatus::SUCCESS : TransactionStatus::FAILED,
            'transactionId' => $response['txnID'],
            'description' => $response['respDescription'],
            'amount' => $amount,
            'pgFees' => $pgFees,
            'totalAmount' => $amount->plus($pgFees),
            'transactionDateTime' => Carbon::createFromFormat('YmdHis', $response['paymentDateTime']),
            'currency' => Currency::of('INR'),
            'paymentMethod' => $this->mapPaymentModes($response['paymentMode']),
            'clientName' => $dbTransaction->client->name,
            'pgConnection' => $dbTransaction->pgConnection->name,
            'pgResponseRaw' => $response,
        ];

        return new PaymentResponseDTO(...$transaction);
    }

    protected function mapPaymentModes($paymentMode): PaymentMethod
    {
        return match ($paymentMode) {
            'CARD' => PaymentMethod::CARD,
            'NB' => PaymentMethod::NETBANKING,
            'WALLET' => PaymentMethod::WALLET,
            'UPI' => PaymentMethod::UPI,
            'debit_card' => PaymentMethod::UNKNOWN
        };
    }

    public function handlePaymentResponse($response): PaymentResponseDTO
    {
        return $this->mapResponseToPaymentResponseDTO($response);
    }

    public function constructTransactionStatusRequest(Transaction $transaction): array
    {
        $request_data = [
            'merchantId' => $this->merchant_id,
            'aggregatorID' => $this->aggregator_id,
            'merchantTxnNo' => $transaction->id.'-'.$transaction->site_reference_id,
            'originalTxnNo' => $transaction->id.'-'.$transaction->site_reference_id,
            'transactionType' => 'SALE',
        ];

        ksort($request_data);

        return $request_data;
    }

    public function getTransactionStatus(Transaction $transaction): PaymentResponseDTO
    {
        $requestObject = $this->constructTransactionStatusRequest($transaction);
        $requestObject['secureHash'] = $this->getHMacDigest($requestObject);

        $response = Http::withHeaders([
            'Content-Type' => 'application/json',
        ])
            ->post($this->getTransactionStatusEndpoint(), $requestObject);

        if ($response->failed()) {
            throw new \Exception('Payment Gateway Error: '.json_encode($response->body()));
        }
        $result = $response->json();

        if ($result['responseCode'] !== 'R1000') {
            throw new \Exception('Payment Gateway Error: '.json_encode($result));
        }

        return $this->mapResponseToPaymentResponseDTO($result);
    }
}
</file>

<file path="app/Concerns/ProfileValidationRules.php">
<?php

namespace App\Concerns;

use App\Models\User;
use Illuminate\Contracts\Validation\ValidationRule;
use Illuminate\Validation\Rule;

trait ProfileValidationRules
{
    /**
     * Get the validation rules used to validate user profiles.
     *
     * @return array<string, array<int, ValidationRule|array<mixed>|string>>
     */
    protected function profileRules(?int $userId = null): array
    {
        return [
            'name' => $this->nameRules(),
            'email' => $this->emailRules($userId),
        ];
    }

    /**
     * Get the validation rules used to validate user names.
     *
     * @return array<int, ValidationRule|array<mixed>|string>
     */
    protected function nameRules(): array
    {
        return ['required', 'string', 'max:255'];
    }

    /**
     * Get the validation rules used to validate user emails.
     *
     * @return array<int, ValidationRule|array<mixed>|string>
     */
    protected function emailRules(?int $userId = null): array
    {
        return [
            'required',
            'string',
            'email',
            'max:255',
            $userId === null
                ? Rule::unique(User::class)
                : Rule::unique(User::class)->ignore($userId),
        ];
    }
}
</file>

<file path="app/Contracts/PaymentGatewayInterface.php">
<?php

namespace App\Contracts;

interface PaymentGatewayInterface
{

}
</file>

<file path="app/Enums/ClientApiLogResult.php">
<?php

namespace App\Enums;

enum ClientApiLogResult: string
{
    case SUCCESS = "success";
    case ERROR = "error";
}
</file>

<file path="app/Enums/PaymentType.php">
<?php

namespace App\Enums;

enum PaymentType: string
{
    case ONE_TIME_PAYMENT = "one_time_payment";
    case SUBSCRIPTION = "subscription";
}
</file>

<file path="app/Enums/SubscriptionPeriod.php">
<?php

namespace App\Enums;

enum SubscriptionPeriod: string
{
    case MONTHLY = "monthly";
    case YEARLY = "yearly";
    case WEEKLY = "weekly";
    case DAILY = "daily";
    case QUARTERLY = "quarterly";
}
</file>

<file path="app/Enums/SubscriptionType.php">
<?php

namespace App\Enums;

enum SubscriptionType: string
{
    case SUBCRIPTION = "subscription";
    case RECURRING = "recurring";
}
</file>

<file path="app/Enums/TransactionStatus.php">
<?php

namespace App\Enums;

enum TransactionStatus: string
{
    case SUCCESS = 'success';
    case FAILED = 'failed';
    case PENDING = 'pending';
    case CANCELLED = 'cancelled';
    case REFUNDED = 'refunded';

    case PROCESSING = 'processing';

}
</file>

<file path="app/Enums/TransactionType.php">
<?php

namespace App\Enums;

enum TransactionType: string
{
    case SALE = "sale";
    case DONATION = "donation";

    /**
     * @return array<string, string>
     */
    public static function labels(): array
    {
        return [
            self::SALE->value => 'Sale',
            self::DONATION->value => 'Donation',
        ];
    }
}
</file>

<file path="app/Events/ClientApiEvent.php">
<?php

namespace App\Events;

use App\Enums\ClientApiLogResult;
use Illuminate\Foundation\Events\Dispatchable;

class ClientApiEvent
{
    use Dispatchable;

    /**
     * @param array<string, mixed> $requestData
     * @param array<string, mixed> $responseData
     */
    public function __construct(
        public readonly ?int $clientId,
        public readonly string $event,
        public readonly ClientApiLogResult $result,
        public readonly array $requestData = [],
        public readonly array $responseData = [],
        public readonly ?string $sourceIp = null,
    ) {
    }
}
</file>

<file path="app/Events/PgTransactionEvent.php">
<?php

namespace App\Events;

use App\DTO\PaymentResponseDTO;
use App\Enums\TransactionStatus;
use Illuminate\Contracts\Queue\ShouldQueueAfterCommit;
use Illuminate\Foundation\Events\Dispatchable;

class PgTransactionEvent implements ShouldQueueAfterCommit
{
    use Dispatchable;

    public function __construct(
        public readonly PaymentResponseDTO $paymentResponseDTO,
        public readonly TransactionStatus $transactionStatus,
    ) {}
}
</file>

<file path="app/Filament/Pages/Utils.php">
<?php

namespace App\Filament\Pages;

use BackedEnum;
use Filament\Actions\Action;
use Filament\Pages\Page;
use Filament\Support\Icons\Heroicon;
use UnitEnum;

class Utils extends Page
{
    protected string $view = 'filament.pages.utils';

    protected static string|BackedEnum|null $navigationIcon = Heroicon::OutlinedWrenchScrewdriver;

    protected static string|UnitEnum|null $navigationGroup = 'Settings';

    public static function canAccess(): bool
    {
        return auth()->user()?->hasAnyRole(['admin', 'superadmin']) ?? false;
    }

    protected function getHeaderActions(): array
    {
        return [
            Action::make('testPayment')
                ->label('Test Payment')
                ->icon(Heroicon::OutlinedCreditCard)
                ->color('primary')
                ->url(route('testPayment')),
        ];
    }
}
</file>

<file path="app/Filament/Resources/ClientConnections/ClientConnectionsResource.php">
<?php

namespace App\Filament\Resources\ClientConnections;

use App\Filament\Resources\ClientConnections\Pages\CreateClientConnections;
use App\Filament\Resources\ClientConnections\Pages\EditClientConnections;
use App\Filament\Resources\ClientConnections\Pages\ListClientConnections;
use App\Filament\Resources\ClientConnections\Pages\ViewClientConnections;
use App\Filament\Resources\ClientConnections\Schemas\ClientConnectionsForm;
use App\Filament\Resources\ClientConnections\Schemas\ClientConnectionsInfolist;
use App\Filament\Resources\ClientConnections\Tables\ClientConnectionsTable;
use App\Models\ClientConnection;
use BackedEnum;
use Filament\Resources\Resource;
use Filament\Schemas\Schema;
use Filament\Support\Icons\Heroicon;
use Filament\Tables\Table;
use Illuminate\Database\Eloquent\Builder;
use Illuminate\Database\Eloquent\SoftDeletingScope;
use UnitEnum;

class ClientConnectionsResource extends Resource
{
    protected static ?string $model = ClientConnection::class;

    protected static string|BackedEnum|null $navigationIcon = Heroicon::OutlinedLink;

    protected static string|UnitEnum|null $navigationGroup = 'Settings';

    public static function form(Schema $schema): Schema
    {
        return ClientConnectionsForm::configure($schema);
    }

    public static function infolist(Schema $schema): Schema
    {
        return ClientConnectionsInfolist::configure($schema);
    }

    public static function table(Table $table): Table
    {
        return ClientConnectionsTable::configure($table);
    }

    public static function getRelations(): array
    {
        return [
            //
        ];
    }

    public static function getPages(): array
    {
        return [
            'index' => ListClientConnections::route('/'),
            'create' => CreateClientConnections::route('/create'),
            'view' => ViewClientConnections::route('/{record}'),
            'edit' => EditClientConnections::route('/{record}/edit'),
        ];
    }

    public static function getRecordRouteBindingEloquentQuery(): Builder
    {
        return parent::getRecordRouteBindingEloquentQuery()
            ->withoutGlobalScopes([
                SoftDeletingScope::class,
            ]);
    }
}
</file>

<file path="app/Filament/Resources/ClientConnections/Pages/CreateClientConnections.php">
<?php

namespace App\Filament\Resources\ClientConnections\Pages;

use App\Filament\Resources\ClientConnections\ClientConnectionsResource;
use App\Filament\Resources\ClientConnections\Concerns\ValidatesClientConnection;
use Filament\Resources\Pages\CreateRecord;

class CreateClientConnections extends CreateRecord
{
    use ValidatesClientConnection;

    protected static string $resource = ClientConnectionsResource::class;

    /**
     * @param  array<string, mixed>  $data
     * @return array<string, mixed>
     */
    protected function mutateFormDataBeforeCreate(array $data): array
    {
        $this->validateClientConnection($data);

        return $data;
    }
}
</file>

<file path="app/Filament/Resources/ClientConnections/Pages/EditClientConnections.php">
<?php

namespace App\Filament\Resources\ClientConnections\Pages;

use App\Filament\Resources\ClientConnections\ClientConnectionsResource;
use App\Filament\Resources\ClientConnections\Concerns\ValidatesClientConnection;
use Filament\Actions\DeleteAction;
use Filament\Actions\ForceDeleteAction;
use Filament\Actions\RestoreAction;
use Filament\Actions\ViewAction;
use Filament\Resources\Pages\EditRecord;

class EditClientConnections extends EditRecord
{
    use ValidatesClientConnection;

    protected static string $resource = ClientConnectionsResource::class;

    protected function getHeaderActions(): array
    {
        return [
            ViewAction::make(),
            DeleteAction::make(),
            ForceDeleteAction::make(),
            RestoreAction::make(),
        ];
    }

    /**
     * @param  array<string, mixed>  $data
     * @return array<string, mixed>
     */
    protected function mutateFormDataBeforeSave(array $data): array
    {
        $this->validateClientConnection($data, $this->record->getKey());

        return $data;
    }
}
</file>

<file path="app/Filament/Resources/ClientConnections/Pages/ListClientConnections.php">
<?php

namespace App\Filament\Resources\ClientConnections\Pages;

use App\Filament\Resources\ClientConnections\ClientConnectionsResource;
use Filament\Actions\CreateAction;
use Filament\Resources\Pages\ListRecords;

class ListClientConnections extends ListRecords
{
    protected static string $resource = ClientConnectionsResource::class;

    protected function getHeaderActions(): array
    {
        return [
            CreateAction::make(),
        ];
    }
}
</file>

<file path="app/Filament/Resources/ClientConnections/Pages/ViewClientConnections.php">
<?php

namespace App\Filament\Resources\ClientConnections\Pages;

use App\Filament\Resources\ClientConnections\ClientConnectionsResource;
use Filament\Actions\EditAction;
use Filament\Resources\Pages\ViewRecord;

class ViewClientConnections extends ViewRecord
{
    protected static string $resource = ClientConnectionsResource::class;

    protected function getHeaderActions(): array
    {
        return [
            EditAction::make(),
        ];
    }
}
</file>

<file path="app/Filament/Resources/ClientConnections/Schemas/ClientConnectionsInfolist.php">
<?php

namespace App\Filament\Resources\ClientConnections\Schemas;

use Filament\Schemas\Schema;

class ClientConnectionsInfolist
{
    public static function configure(Schema $schema): Schema
    {
        return $schema
            ->components([
                //
            ]);
    }
}
</file>

<file path="app/Filament/Resources/Clients/ClientsResource.php">
<?php

namespace App\Filament\Resources\Clients;

use App\Filament\Resources\Clients\Pages\CreateClients;
use App\Filament\Resources\Clients\Pages\EditClients;
use App\Filament\Resources\Clients\Pages\ListClients;
use App\Filament\Resources\Clients\Pages\ViewClients;
use App\Filament\Resources\Clients\Schemas\ClientsForm;
use App\Filament\Resources\Clients\Schemas\ClientsInfolist;
use App\Filament\Resources\Clients\Tables\ClientsTable;
use App\Models\Client;
use BackedEnum;
use Filament\Resources\Resource;
use Filament\Schemas\Schema;
use Filament\Support\Icons\Heroicon;
use Filament\Tables\Table;
use Illuminate\Database\Eloquent\Builder;
use Illuminate\Database\Eloquent\SoftDeletingScope;
use UnitEnum;

class ClientsResource extends Resource
{
    protected static ?string $model = Client::class;

    protected static string|BackedEnum|null $navigationIcon = Heroicon::OutlinedBuildingStorefront;

    protected static string|UnitEnum|null $navigationGroup = 'Settings';

    protected static ?string $recordTitleAttribute = 'name';

    public static function form(Schema $schema): Schema
    {
        return ClientsForm::configure($schema);
    }

    public static function infolist(Schema $schema): Schema
    {
        return ClientsInfolist::configure($schema);
    }

    public static function table(Table $table): Table
    {
        return ClientsTable::configure($table);
    }

    public static function getRelations(): array
    {
        return [
            //
        ];
    }

    public static function getPages(): array
    {
        return [
            'index' => ListClients::route('/'),
            'create' => CreateClients::route('/create'),
            'view' => ViewClients::route('/{record}'),
            'edit' => EditClients::route('/{record}/edit'),
        ];
    }

    public static function getRecordRouteBindingEloquentQuery(): Builder
    {
        return parent::getRecordRouteBindingEloquentQuery()
            ->withoutGlobalScopes([
                SoftDeletingScope::class,
            ]);
    }
}
</file>

<file path="app/Filament/Resources/Clients/Pages/ListClients.php">
<?php

namespace App\Filament\Resources\Clients\Pages;

use App\Filament\Resources\Clients\ClientsResource;
use Filament\Actions\CreateAction;
use Filament\Resources\Pages\ListRecords;

class ListClients extends ListRecords
{
    protected static string $resource = ClientsResource::class;

    protected function getHeaderActions(): array
    {
        return [
            CreateAction::make(),
        ];
    }
}
</file>

<file path="app/Filament/Resources/Clients/Pages/ViewClients.php">
<?php

namespace App\Filament\Resources\Clients\Pages;

use App\Filament\Resources\Clients\ClientsResource;
use Filament\Actions\EditAction;
use Filament\Resources\Pages\ViewRecord;

class ViewClients extends ViewRecord
{
    protected static string $resource = ClientsResource::class;

    protected function getHeaderActions(): array
    {
        return [
            EditAction::make(),
        ];
    }
}
</file>

<file path="app/Filament/Resources/Clients/Schemas/ClientsInfolist.php">
<?php

namespace App\Filament\Resources\Clients\Schemas;

use App\Models\Client;
use Filament\Actions\Action;
use Filament\Infolists\Components\IconEntry;
use Filament\Infolists\Components\KeyValueEntry;
use Filament\Infolists\Components\TextEntry;
use Filament\Schemas\Components\Section;
use Filament\Schemas\Schema;
use Filament\Support\Icons\Heroicon;

class ClientsInfolist
{
    public static function configure(Schema $schema): Schema
    {
        return $schema
            ->components([
                Section::make('Client Details')
                    ->description('The basics identifying this client.')
                    ->icon(Heroicon::OutlinedBuildingStorefront)
                    ->columns(4)
                    ->components([
                        TextEntry::make('name')
                            ->columnSpan(3),

                        IconEntry::make('status')
                            ->label('Active')
                            ->boolean()
                            ->columnSpan(1),
                    ]),

                Section::make('User Permissions')
                    ->description('Users granted the "can_view_client" permission for this client.')
                    ->icon(Heroicon::OutlinedShieldCheck)
                    ->components([
                        TextEntry::make('users.name')
                            ->hiddenLabel()
                            ->badge()
                            ->placeholder('No users assigned.'),
                    ]),

                Section::make('URLs')
                    ->description('Where clients are sent during and after authentication.')
                    ->icon(Heroicon::OutlinedGlobeAlt)
                    ->columns(4)
                    ->components([
                        TextEntry::make('website')
                            ->url(fn (Client $record): string => $record->website)
                            ->openUrlInNewTab()
                            ->columnSpan(4),

                        TextEntry::make('redirect_uri')
                            ->label('Redirect URI')
                            ->columnSpan(3),

                        TextEntry::make('redirect_uri_separator')
                            ->label('Query Separator')
                            ->columnSpan(1),

                        TextEntry::make('webhook_uri')
                            ->label('Webhook URI')
                            ->placeholder('—')
                            ->columnSpan(4),
                    ]),

                Section::make('API Credentials')
                    ->description('Used by this client to authenticate its API requests.')
                    ->icon(Heroicon::OutlinedKey)
                    ->columns(2)
                    ->components([
                        TextEntry::make('client_id')
                            ->label('Client ID')
                            ->copyable(),

                        TextEntry::make('client_secret')
                            ->label('Client Secret')
                            ->formatStateUsing(fn (): string => str_repeat('•', 32))
                            ->copyable()
                            ->copyableState(fn (Client $record): string => $record->client_secret)
                            ->copyMessage('Client secret copied.')
                            ->hintAction(
                                Action::make('revealClientSecret')
                                    ->label('Reveal')
                                    ->icon(Heroicon::OutlinedEye)
                                    ->color('gray')
                                    ->modalHeading('Client Secret')
                                    ->modalDescription(fn (Client $record): string => $record->client_secret)
                                    ->modalSubmitAction(false)
                                    ->modalCancelActionLabel('Close'),
                            ),
                    ]),

                Section::make('Additional Data')
                    ->description('Optional, freeform metadata stored alongside this client.')
                    ->icon(Heroicon::OutlinedRectangleStack)
                    ->collapsible()
                    ->collapsed()
                    ->components([
                        KeyValueEntry::make('data')
                            ->hiddenLabel(),
                    ]),
            ]);
    }
}
</file>

<file path="app/Filament/Resources/PGConnections/Concerns/ValidatesGatewayAttributes.php">
<?php

namespace App\Filament\Resources\PGConnections\Concerns;

use App\Filament\Resources\PGConnections\Schemas\PGConnectionAttributeSchema;
use Filament\Notifications\Notification;
use Filament\Support\Exceptions\Halt;

trait ValidatesGatewayAttributes
{
    /**
     * Guard the record from being persisted with attributes that don't
     * satisfy the required schema for the selected payment gateway.
     *
     * @param  array<string, mixed>  $data
     */
    protected function validateGatewayAttributes(array $data): void
    {
        $errors = PGConnectionAttributeSchema::validate(
            $data['pg_class'] ?? null,
            $data['attributes'] ?? [],
        );

        if ($errors === []) {
            return;
        }

        Notification::make()
            ->title('Invalid gateway attributes')
            ->body(implode(' ', $errors))
            ->danger()
            ->send();

        throw new Halt();
    }
}
</file>

<file path="app/Filament/Resources/PGConnections/Pages/CreatePGConnection.php">
<?php

namespace App\Filament\Resources\PGConnections\Pages;

use App\Filament\Resources\PGConnections\Concerns\ValidatesGatewayAttributes;
use App\Filament\Resources\PGConnections\PGConnectionResource;
use Filament\Resources\Pages\CreateRecord;

class CreatePGConnection extends CreateRecord
{
    use ValidatesGatewayAttributes;

    protected static string $resource = PGConnectionResource::class;

    /**
     * @param  array<string, mixed>  $data
     * @return array<string, mixed>
     */
    protected function mutateFormDataBeforeCreate(array $data): array
    {
        $this->validateGatewayAttributes($data);

        return $data;
    }
}
</file>

<file path="app/Filament/Resources/PGConnections/Pages/EditPGConnection.php">
<?php

namespace App\Filament\Resources\PGConnections\Pages;

use App\Filament\Resources\PGConnections\Concerns\ValidatesGatewayAttributes;
use App\Filament\Resources\PGConnections\PGConnectionResource;
use Filament\Actions\DeleteAction;
use Filament\Actions\ViewAction;
use Filament\Resources\Pages\EditRecord;

class EditPGConnection extends EditRecord
{
    use ValidatesGatewayAttributes;

    protected static string $resource = PGConnectionResource::class;

    protected function getHeaderActions(): array
    {
        return [
            ViewAction::make(),
            DeleteAction::make(),
        ];
    }

    /**
     * @param  array<string, mixed>  $data
     * @return array<string, mixed>
     */
    protected function mutateFormDataBeforeSave(array $data): array
    {
        $this->validateGatewayAttributes($data);

        return $data;
    }
}
</file>

<file path="app/Filament/Resources/PGConnections/Pages/ListPGConnections.php">
<?php

namespace App\Filament\Resources\PGConnections\Pages;

use App\Filament\Resources\PGConnections\PGConnectionResource;
use Filament\Actions\CreateAction;
use Filament\Resources\Pages\ListRecords;

class ListPGConnections extends ListRecords
{
    protected static string $resource = PGConnectionResource::class;

    protected function getHeaderActions(): array
    {
        return [
            CreateAction::make(),
        ];
    }
}
</file>

<file path="app/Filament/Resources/PGConnections/Pages/ViewPGConnection.php">
<?php

namespace App\Filament\Resources\PGConnections\Pages;

use App\Filament\Resources\PGConnections\PGConnectionResource;
use Filament\Actions\EditAction;
use Filament\Resources\Pages\ViewRecord;

class ViewPGConnection extends ViewRecord
{
    protected static string $resource = PGConnectionResource::class;

    protected function getHeaderActions(): array
    {
        return [
            EditAction::make(),
        ];
    }
}
</file>

<file path="app/Filament/Resources/PGConnections/PGConnectionResource.php">
<?php

namespace App\Filament\Resources\PGConnections;

use App\Filament\Resources\PGConnections\Pages\CreatePGConnection;
use App\Filament\Resources\PGConnections\Pages\EditPGConnection;
use App\Filament\Resources\PGConnections\Pages\ListPGConnections;
use App\Filament\Resources\PGConnections\Pages\ViewPGConnection;
use App\Filament\Resources\PGConnections\Schemas\PGConnectionForm;
use App\Filament\Resources\PGConnections\Schemas\PGConnectionInfolist;
use App\Filament\Resources\PGConnections\Tables\PGConnectionsTable;
use App\Models\PGConnection;
use BackedEnum;
use Filament\Resources\Resource;
use Filament\Schemas\Schema;
use Filament\Support\Icons\Heroicon;
use Filament\Tables\Table;
use UnitEnum;

class PGConnectionResource extends Resource
{
    protected static ?string $model = PGConnection::class;


    protected static string|BackedEnum|null $navigationIcon = Heroicon::OutlinedCreditCard;

    protected static string|UnitEnum|null $navigationGroup = 'Settings';

    protected static ?string $navigationLabel = 'PG Connections';
    protected static ?string $recordTitleAttribute = 'name';

    public static function form(Schema $schema): Schema
    {
        return PGConnectionForm::configure($schema);
    }

    public static function infolist(Schema $schema): Schema
    {
        return PGConnectionInfolist::configure($schema);
    }

    public static function table(Table $table): Table
    {
        return PGConnectionsTable::configure($table);
    }

    public static function getRelations(): array
    {
        return [
            //
        ];
    }

    public static function getPages(): array
    {
        return [
            'index' => ListPGConnections::route('/'),
            'create' => CreatePGConnection::route('/create'),
            'view' => ViewPGConnection::route('/{record}'),
            'edit' => EditPGConnection::route('/{record}/edit'),
        ];
    }
}
</file>

<file path="app/Filament/Resources/PGConnections/Schemas/PGConnectionForm.php">
<?php

namespace App\Filament\Resources\PGConnections\Schemas;

use App\Enums\ConnectionType;
use App\Models\SupportedPaymentGateway;
use Filament\Forms\Components\Select;
use Filament\Forms\Components\TextInput;
use Filament\Forms\Components\Toggle;
use Filament\Schemas\Components\Section;
use Filament\Schemas\Components\Utilities\Get;
use Filament\Schemas\Schema;
use Filament\Support\Icons\Heroicon;

class PGConnectionForm
{
    public static function configure(Schema $schema): Schema
    {
        return $schema
            ->components([
                Section::make('Gateway Connection')
                    ->description('Basic details identifying this payment gateway connection.')
                    ->icon(Heroicon::OutlinedCreditCard)
                    ->columns(4)
                    ->components([
                        TextInput::make('name')
                            ->prefixIcon(Heroicon::OutlinedTag)
                            ->columnSpan(2)
                            ->required()
                            ->maxLength(255),

                        Select::make('pg_class')
                            ->label('Payment Gateway Class')
                            ->prefixIcon(Heroicon::OutlinedBuildingLibrary)
                            ->options(fn (): array => SupportedPaymentGateway::query()
                                ->where('status', true)
                                ->pluck('name', 'pg_class')
                                ->all())
                            ->columnSpan(2)
                            ->searchable()
                            ->live()
                            ->required(),

                        Select::make('type')
                            ->prefixIcon(Heroicon::OutlinedAdjustmentsHorizontal)
                            ->options(ConnectionType::labels())
                            ->default(ConnectionType::TEST->value)
                            ->columnSpan(3)
                            ->required(),

                        Toggle::make('status')
                            ->label('Active')
                            ->onIcon(Heroicon::OutlinedCheckCircle)
                            ->offIcon(Heroicon::OutlinedXCircle)
                            ->onColor('success')
                            ->offColor('danger')
                            ->columnSpan(1)
                            ->default(true),
                    ]),

                Section::make('Attributes')
                    ->description('Credentials and settings required by the selected payment gateway.')
                    ->icon(Heroicon::OutlinedKey)
                    ->columns(2)
                    ->compact()
                    ->components(fn (Get $get): array => PGConnectionAttributeSchema::fields($get('pg_class'))),
            ]);
    }
}
</file>

<file path="app/Filament/Resources/PGConnections/Schemas/PGConnectionInfolist.php">
<?php

namespace App\Filament\Resources\PGConnections\Schemas;

use Filament\Schemas\Schema;

class PGConnectionInfolist
{
    public static function configure(Schema $schema): Schema
    {
        return $schema
            ->components([
                //
            ]);
    }
}
</file>

<file path="app/Filament/Resources/PGConnections/Tables/PGConnectionsTable.php">
<?php

namespace App\Filament\Resources\PGConnections\Tables;

use Filament\Actions\BulkActionGroup;
use Filament\Actions\DeleteBulkAction;
use Filament\Actions\EditAction;
use Filament\Actions\ViewAction;
use Filament\Tables\Columns\IconColumn;
use Filament\Tables\Columns\TextColumn;
use Filament\Tables\Table;
use Illuminate\Database\Eloquent\Builder;

class PGConnectionsTable
{
    public static function configure(Table $table): Table
    {
        return $table
            ->modifyQueryUsing(fn (Builder $query) => $query->withCount([
                'clientConnections as active_clients_count' => fn (Builder $query) => $query->where('status', true),
            ]))
            ->columns([
                TextColumn::make('name')
                    ->searchable(),

                TextColumn::make('pg_class')
                    ->label('PG Class')
                    ->searchable(),

                TextColumn::make('type')
                    ->label('Environment')
                    ->badge(),

                TextColumn::make('active_clients_count')
                    ->label('Active Clients')
                    ->numeric()
                    ->sortable(),

                IconColumn::make('status')
                    ->label('Status')
                    ->boolean(),
            ])
            ->filters([
                //
            ])
            ->recordActions([
                ViewAction::make(),
                EditAction::make(),
            ])
            ->toolbarActions([
                BulkActionGroup::make([
                    DeleteBulkAction::make(),
                ]),
            ]);
    }
}
</file>

<file path="app/Filament/Resources/Subscriptions/Pages/CreateSubscriptions.php">
<?php

namespace App\Filament\Resources\Subscriptions\Pages;

use App\Filament\Resources\Subscriptions\SubscriptionsResource;
use Filament\Resources\Pages\CreateRecord;

class CreateSubscriptions extends CreateRecord
{
    protected static string $resource = SubscriptionsResource::class;
}
</file>

<file path="app/Filament/Resources/Subscriptions/Pages/EditSubscriptions.php">
<?php

namespace App\Filament\Resources\Subscriptions\Pages;

use App\Filament\Resources\Subscriptions\SubscriptionsResource;
use Filament\Actions\DeleteAction;
use Filament\Actions\ForceDeleteAction;
use Filament\Actions\RestoreAction;
use Filament\Resources\Pages\EditRecord;

class EditSubscriptions extends EditRecord
{
    protected static string $resource = SubscriptionsResource::class;

    protected function getHeaderActions(): array
    {
        return [
            DeleteAction::make(),
            ForceDeleteAction::make(),
            RestoreAction::make(),
        ];
    }
}
</file>

<file path="app/Filament/Resources/Subscriptions/Pages/ListSubscriptions.php">
<?php

namespace App\Filament\Resources\Subscriptions\Pages;

use App\Filament\Resources\Subscriptions\SubscriptionsResource;
use Filament\Actions\CreateAction;
use Filament\Resources\Pages\ListRecords;

class ListSubscriptions extends ListRecords
{
    protected static string $resource = SubscriptionsResource::class;

    protected function getHeaderActions(): array
    {
        return [
            CreateAction::make(),
        ];
    }
}
</file>

<file path="app/Filament/Resources/Subscriptions/Schemas/SubscriptionsForm.php">
<?php

namespace App\Filament\Resources\Subscriptions\Schemas;

use Filament\Schemas\Schema;

class SubscriptionsForm
{
    public static function configure(Schema $schema): Schema
    {
        return $schema
            ->components([
                //
            ]);
    }
}
</file>

<file path="app/Filament/Resources/Subscriptions/SubscriptionsResource.php">
<?php

namespace App\Filament\Resources\Subscriptions;

use App\Filament\Resources\Subscriptions\Pages\CreateSubscriptions;
use App\Filament\Resources\Subscriptions\Pages\EditSubscriptions;
use App\Filament\Resources\Subscriptions\Pages\ListSubscriptions;
use App\Filament\Resources\Subscriptions\Schemas\SubscriptionsForm;
use App\Filament\Resources\Subscriptions\Tables\SubscriptionsTable;
use App\Models\Subscription;
use BackedEnum;
use Filament\Resources\Resource;
use Filament\Schemas\Schema;
use Filament\Support\Icons\Heroicon;
use Filament\Tables\Table;
use Illuminate\Database\Eloquent\Builder;
use Illuminate\Database\Eloquent\SoftDeletingScope;

class SubscriptionsResource extends Resource
{
    protected static ?string $model = Subscription::class;

    protected static string|BackedEnum|null $navigationIcon = Heroicon::OutlinedCalendarDays;

    public static function form(Schema $schema): Schema
    {
        return SubscriptionsForm::configure($schema);
    }

    public static function table(Table $table): Table
    {
        return SubscriptionsTable::configure($table);
    }

    public static function getRelations(): array
    {
        return [
            //
        ];
    }

    public static function getPages(): array
    {
        return [
            'index' => ListSubscriptions::route('/'),
            'create' => CreateSubscriptions::route('/create'),
            'edit' => EditSubscriptions::route('/{record}/edit'),
        ];
    }

    public static function getRecordRouteBindingEloquentQuery(): Builder
    {
        return parent::getRecordRouteBindingEloquentQuery()
            ->withoutGlobalScopes([
                SoftDeletingScope::class,
            ]);
    }
}
</file>

<file path="app/Filament/Resources/Subscriptions/Tables/SubscriptionsTable.php">
<?php

namespace App\Filament\Resources\Subscriptions\Tables;

use Filament\Actions\BulkActionGroup;
use Filament\Actions\DeleteBulkAction;
use Filament\Actions\EditAction;
use Filament\Actions\ForceDeleteBulkAction;
use Filament\Actions\RestoreBulkAction;
use Filament\Tables\Filters\TrashedFilter;
use Filament\Tables\Table;

class SubscriptionsTable
{
    public static function configure(Table $table): Table
    {
        return $table
            ->columns([
                //
            ])
            ->filters([
                TrashedFilter::make(),
            ])
            ->recordActions([
                EditAction::make(),
            ])
            ->toolbarActions([
                BulkActionGroup::make([
                    DeleteBulkAction::make(),
                    ForceDeleteBulkAction::make(),
                    RestoreBulkAction::make(),
                ]),
            ]);
    }
}
</file>

<file path="app/Filament/Resources/SubscriptionTransactions/Pages/CreateSubscriptionTransaction.php">
<?php

namespace App\Filament\Resources\SubscriptionTransactions\Pages;

use App\Filament\Resources\SubscriptionTransactions\SubscriptionTransactionResource;
use Filament\Resources\Pages\CreateRecord;

class CreateSubscriptionTransaction extends CreateRecord
{
    protected static string $resource = SubscriptionTransactionResource::class;
}
</file>

<file path="app/Filament/Resources/SubscriptionTransactions/Pages/EditSubscriptionTransaction.php">
<?php

namespace App\Filament\Resources\SubscriptionTransactions\Pages;

use App\Filament\Resources\SubscriptionTransactions\SubscriptionTransactionResource;
use Filament\Actions\DeleteAction;
use Filament\Actions\ViewAction;
use Filament\Resources\Pages\EditRecord;

class EditSubscriptionTransaction extends EditRecord
{
    protected static string $resource = SubscriptionTransactionResource::class;

    protected function getHeaderActions(): array
    {
        return [
            ViewAction::make(),
            DeleteAction::make(),
        ];
    }
}
</file>

<file path="app/Filament/Resources/SubscriptionTransactions/Pages/ListSubscriptionTransactions.php">
<?php

namespace App\Filament\Resources\SubscriptionTransactions\Pages;

use App\Filament\Resources\SubscriptionTransactions\SubscriptionTransactionResource;
use Filament\Actions\CreateAction;
use Filament\Resources\Pages\ListRecords;

class ListSubscriptionTransactions extends ListRecords
{
    protected static string $resource = SubscriptionTransactionResource::class;

    protected function getHeaderActions(): array
    {
        return [
            CreateAction::make(),
        ];
    }
}
</file>

<file path="app/Filament/Resources/SubscriptionTransactions/Pages/ViewSubscriptionTransaction.php">
<?php

namespace App\Filament\Resources\SubscriptionTransactions\Pages;

use App\Filament\Resources\SubscriptionTransactions\SubscriptionTransactionResource;
use Filament\Actions\EditAction;
use Filament\Resources\Pages\ViewRecord;

class ViewSubscriptionTransaction extends ViewRecord
{
    protected static string $resource = SubscriptionTransactionResource::class;

    protected function getHeaderActions(): array
    {
        return [
            EditAction::make(),
        ];
    }
}
</file>

<file path="app/Filament/Resources/SubscriptionTransactions/Schemas/SubscriptionTransactionForm.php">
<?php

namespace App\Filament\Resources\SubscriptionTransactions\Schemas;

use Filament\Forms\Components\DateTimePicker;
use Filament\Forms\Components\Select;
use Filament\Forms\Components\TextInput;
use Filament\Forms\Components\Textarea;
use Filament\Schemas\Schema;

class SubscriptionTransactionForm
{
    public static function configure(Schema $schema): Schema
    {
        return $schema
            ->components([
                Select::make('subscription_id')
                    ->relationship('subscription', 'id')
                    ->required(),
                TextInput::make('transaction_id')
                    ->required(),
                TextInput::make('amount')
                    ->required()
                    ->numeric(),
                TextInput::make('currency')
                    ->required()
                    ->default('INR'),
                TextInput::make('status')
                    ->required(),
                TextInput::make('payment_method')
                    ->required(),
                TextInput::make('pg_fees')
                    ->required()
                    ->numeric()
                    ->default(0),
                TextInput::make('pg_tax')
                    ->required()
                    ->numeric()
                    ->default(0),
                Textarea::make('data')
                    ->default(null)
                    ->columnSpanFull(),
                DateTimePicker::make('transaction_date_time')
                    ->required(),
            ]);
    }
}
</file>

<file path="app/Filament/Resources/SubscriptionTransactions/Schemas/SubscriptionTransactionInfolist.php">
<?php

namespace App\Filament\Resources\SubscriptionTransactions\Schemas;

use Filament\Infolists\Components\TextEntry;
use Filament\Schemas\Schema;

class SubscriptionTransactionInfolist
{
    public static function configure(Schema $schema): Schema
    {
        return $schema
            ->components([
                TextEntry::make('subscription.id')
                    ->label('Subscription'),
                TextEntry::make('transaction_id'),
                TextEntry::make('amount')
                    ->numeric(),
                TextEntry::make('currency'),
                TextEntry::make('status'),
                TextEntry::make('payment_method'),
                TextEntry::make('pg_fees')
                    ->numeric(),
                TextEntry::make('pg_tax')
                    ->numeric(),
                TextEntry::make('data')
                    ->placeholder('-')
                    ->columnSpanFull(),
                TextEntry::make('transaction_date_time')
                    ->dateTime(),
                TextEntry::make('created_at')
                    ->dateTime()
                    ->placeholder('-'),
                TextEntry::make('updated_at')
                    ->dateTime()
                    ->placeholder('-'),
            ]);
    }
}
</file>

<file path="app/Filament/Resources/SubscriptionTransactions/SubscriptionTransactionResource.php">
<?php

namespace App\Filament\Resources\SubscriptionTransactions;

use App\Filament\Resources\SubscriptionTransactions\Pages\CreateSubscriptionTransaction;
use App\Filament\Resources\SubscriptionTransactions\Pages\EditSubscriptionTransaction;
use App\Filament\Resources\SubscriptionTransactions\Pages\ListSubscriptionTransactions;
use App\Filament\Resources\SubscriptionTransactions\Pages\ViewSubscriptionTransaction;
use App\Filament\Resources\SubscriptionTransactions\Schemas\SubscriptionTransactionForm;
use App\Filament\Resources\SubscriptionTransactions\Schemas\SubscriptionTransactionInfolist;
use App\Filament\Resources\SubscriptionTransactions\Tables\SubscriptionTransactionsTable;
use App\Models\SubscriptionTransaction;
use BackedEnum;
use Filament\Resources\Resource;
use Filament\Schemas\Schema;
use Filament\Support\Icons\Heroicon;
use Filament\Tables\Table;
use UnitEnum;

class SubscriptionTransactionResource extends Resource
{
    protected static ?string $model = SubscriptionTransaction::class;

    protected static string|BackedEnum|null $navigationIcon = Heroicon::OutlinedArrowPath;

    protected static string|UnitEnum|null $navigationGroup = 'Transactions';

    protected static ?string $navigationLabel = 'Recurring Transactions';

    public static function form(Schema $schema): Schema
    {
        return SubscriptionTransactionForm::configure($schema);
    }

    public static function infolist(Schema $schema): Schema
    {
        return SubscriptionTransactionInfolist::configure($schema);
    }

    public static function table(Table $table): Table
    {
        return SubscriptionTransactionsTable::configure($table);
    }

    public static function getRelations(): array
    {
        return [
            //
        ];
    }

    public static function getPages(): array
    {
        return [
            'index' => ListSubscriptionTransactions::route('/'),
            'create' => CreateSubscriptionTransaction::route('/create'),
            'view' => ViewSubscriptionTransaction::route('/{record}'),
            'edit' => EditSubscriptionTransaction::route('/{record}/edit'),
        ];
    }
}
</file>

<file path="app/Filament/Resources/SubscriptionTransactions/Tables/SubscriptionTransactionsTable.php">
<?php

namespace App\Filament\Resources\SubscriptionTransactions\Tables;

use Filament\Actions\BulkActionGroup;
use Filament\Actions\DeleteBulkAction;
use Filament\Actions\EditAction;
use Filament\Actions\ViewAction;
use Filament\Tables\Columns\TextColumn;
use Filament\Tables\Table;

class SubscriptionTransactionsTable
{
    public static function configure(Table $table): Table
    {
        return $table
            ->columns([
                TextColumn::make('subscription.id')
                    ->searchable(),
                TextColumn::make('transaction_id')
                    ->searchable(),
                TextColumn::make('amount')
                    ->numeric()
                    ->sortable(),
                TextColumn::make('currency')
                    ->searchable(),
                TextColumn::make('status')
                    ->searchable(),
                TextColumn::make('payment_method')
                    ->searchable(),
                TextColumn::make('pg_fees')
                    ->numeric()
                    ->sortable(),
                TextColumn::make('pg_tax')
                    ->numeric()
                    ->sortable(),
                TextColumn::make('transaction_date_time')
                    ->dateTime()
                    ->sortable(),
                TextColumn::make('created_at')
                    ->dateTime()
                    ->sortable()
                    ->toggleable(isToggledHiddenByDefault: true),
                TextColumn::make('updated_at')
                    ->dateTime()
                    ->sortable()
                    ->toggleable(isToggledHiddenByDefault: true),
            ])
            ->filters([
                //
            ])
            ->recordActions([
                ViewAction::make(),
                EditAction::make(),
            ])
            ->toolbarActions([
                BulkActionGroup::make([
                    DeleteBulkAction::make(),
                ]),
            ]);
    }
}
</file>

<file path="app/Filament/Resources/Transactions/Pages/CreateTransactions.php">
<?php

namespace App\Filament\Resources\Transactions\Pages;

use App\Filament\Resources\Transactions\TransactionsResource;
use Filament\Resources\Pages\CreateRecord;

class CreateTransactions extends CreateRecord
{
    protected static string $resource = TransactionsResource::class;
}
</file>

<file path="app/Filament/Resources/Transactions/Pages/EditTransactions.php">
<?php

namespace App\Filament\Resources\Transactions\Pages;

use App\Filament\Resources\Transactions\TransactionsResource;
use Filament\Actions\DeleteAction;
use Filament\Actions\ViewAction;
use Filament\Resources\Pages\EditRecord;

class EditTransactions extends EditRecord
{
    protected static string $resource = TransactionsResource::class;

    protected function getHeaderActions(): array
    {
        return [
            ViewAction::make(),
            DeleteAction::make(),
        ];
    }
}
</file>

<file path="app/Filament/Resources/Transactions/Pages/ListTransactions.php">
<?php

namespace App\Filament\Resources\Transactions\Pages;

use App\Filament\Resources\Transactions\TransactionsResource;
use Filament\Actions\CreateAction;
use Filament\Resources\Pages\ListRecords;

class ListTransactions extends ListRecords
{
    protected static string $resource = TransactionsResource::class;

    protected function getHeaderActions(): array
    {
        return [
            CreateAction::make(),
        ];
    }
}
</file>

<file path="app/Filament/Resources/Transactions/Schemas/TransactionsForm.php">
<?php

namespace App\Filament\Resources\Transactions\Schemas;

use Filament\Schemas\Schema;

class TransactionsForm
{
    public static function configure(Schema $schema): Schema
    {
        return $schema
            ->components([
                //
            ]);
    }
}
</file>

<file path="app/Filament/Resources/Transactions/TransactionsResource.php">
<?php

namespace App\Filament\Resources\Transactions;

use App\Filament\Resources\Transactions\Pages\CreateTransactions;
use App\Filament\Resources\Transactions\Pages\EditTransactions;
use App\Filament\Resources\Transactions\Pages\ListTransactions;
use App\Filament\Resources\Transactions\Pages\ViewTransactions;
use App\Filament\Resources\Transactions\Schemas\TransactionsForm;
use App\Filament\Resources\Transactions\Schemas\TransactionsInfolist;
use App\Filament\Resources\Transactions\Tables\TransactionsTable;
use App\Models\Transaction;
use BackedEnum;
use Filament\Resources\Resource;
use Filament\Schemas\Schema;
use Filament\Support\Icons\Heroicon;
use Filament\Tables\Table;
use UnitEnum;

class TransactionsResource extends Resource
{
    protected static ?string $model = Transaction::class;

    protected static string|BackedEnum|null $navigationIcon = Heroicon::OutlinedBanknotes;

    protected static string|UnitEnum|null $navigationGroup = 'Transactions';

    protected static ?string $navigationLabel = 'One time Transaction';

    public static function form(Schema $schema): Schema
    {
        return TransactionsForm::configure($schema);
    }

    public static function infolist(Schema $schema): Schema
    {
        return TransactionsInfolist::configure($schema);
    }

    public static function table(Table $table): Table
    {
        return TransactionsTable::configure($table);
    }

    public static function getRelations(): array
    {
        return [
            //
        ];
    }

    public static function getPages(): array
    {
        return [
            'index' => ListTransactions::route('/'),
            'create' => CreateTransactions::route('/create'),
            'view' => ViewTransactions::route('/{record}'),
            'edit' => EditTransactions::route('/{record}/edit'),
        ];
    }
}
</file>

<file path="app/Filament/Resources/Users/Pages/CreateUsers.php">
<?php

namespace App\Filament\Resources\Users\Pages;

use App\Filament\Resources\Users\UsersResource;
use Filament\Resources\Pages\CreateRecord;

class CreateUsers extends CreateRecord
{
    protected static string $resource = UsersResource::class;
}
</file>

<file path="app/Filament/Resources/Users/Pages/EditUsers.php">
<?php

namespace App\Filament\Resources\Users\Pages;

use App\Filament\Resources\Users\UsersResource;
use Filament\Actions\DeleteAction;
use Filament\Actions\ForceDeleteAction;
use Filament\Actions\RestoreAction;
use Filament\Resources\Pages\EditRecord;

class EditUsers extends EditRecord
{
    protected static string $resource = UsersResource::class;

    protected function getHeaderActions(): array
    {
        return [
            DeleteAction::make(),
            ForceDeleteAction::make(),
            RestoreAction::make(),
        ];
    }
}
</file>

<file path="app/Filament/Resources/Users/Pages/ListUsers.php">
<?php

namespace App\Filament\Resources\Users\Pages;

use App\Filament\Resources\Users\UsersResource;
use Filament\Actions\CreateAction;
use Filament\Resources\Pages\ListRecords;

class ListUsers extends ListRecords
{
    protected static string $resource = UsersResource::class;

    protected function getHeaderActions(): array
    {
        return [
            CreateAction::make(),
        ];
    }
}
</file>

<file path="app/Filament/Resources/Users/Schemas/UsersForm.php">
<?php

namespace App\Filament\Resources\Users\Schemas;

use Filament\Schemas\Schema;

class UsersForm
{
    public static function configure(Schema $schema): Schema
    {
        return $schema
            ->components([
                //
            ]);
    }
}
</file>

<file path="app/Filament/Resources/Users/UsersResource.php">
<?php

namespace App\Filament\Resources\Users;

use App\Filament\Resources\Users\Pages\CreateUsers;
use App\Filament\Resources\Users\Pages\EditUsers;
use App\Filament\Resources\Users\Pages\ListUsers;
use App\Filament\Resources\Users\Schemas\UsersForm;
use App\Filament\Resources\Users\Tables\UsersTable;
use App\Models\User;
use BackedEnum;
use Filament\Resources\Resource;
use Filament\Schemas\Schema;
use Filament\Support\Icons\Heroicon;
use Filament\Tables\Table;
use Illuminate\Database\Eloquent\Builder;
use Illuminate\Database\Eloquent\SoftDeletingScope;
use UnitEnum;

class UsersResource extends Resource
{
    protected static ?string $model = User::class;

    protected static string|BackedEnum|null $navigationIcon = Heroicon::OutlinedUsers;

    protected static string|UnitEnum|null $navigationGroup = 'Settings';

    protected static ?string $recordTitleAttribute = 'name';

    public static function form(Schema $schema): Schema
    {
        return UsersForm::configure($schema);
    }

    public static function table(Table $table): Table
    {
        return UsersTable::configure($table);
    }

    public static function getRelations(): array
    {
        return [
            //
        ];
    }

    public static function getPages(): array
    {
        return [
            'index' => ListUsers::route('/'),
            'create' => CreateUsers::route('/create'),
            'edit' => EditUsers::route('/{record}/edit'),
        ];
    }

    public static function getRecordRouteBindingEloquentQuery(): Builder
    {
        return parent::getRecordRouteBindingEloquentQuery()
            ->withoutGlobalScopes([
                SoftDeletingScope::class,
            ]);
    }
}
</file>

<file path="app/Http/Controllers/API/V1/PaymentController.php">
<?php

namespace App\Http\Controllers\API\V1;

use App\DTO\PaymentRequestDTO;
use App\Enums\ClientApiLogResult;
use App\Events\ClientApiEvent;
use App\Http\Controllers\Controller;
use App\Http\Requests\V1\ValidatePaymentRequest;
use App\Services\TransactionService;
use Illuminate\Http\Request;

class PaymentController extends Controller
{
    public function initiatePayment(ValidatePaymentRequest $request)
    {
        try {
            $paymentRequestDto = PaymentRequestDTO::from($request->validated());
            $url = app(TransactionService::class)->initiatePayment($paymentRequestDto);

            return redirect()->away($url);
        } catch (\Throwable $e) {
            $this->logFailure($request, $e);

            return response()->json(['error' => $e->getFile()." ".$e->getLine()." ". $e->getMessage()], 400);
        }

    }

    private function logFailure(ValidatePaymentRequest $request, \Throwable $e): void
    {
        ClientApiEvent::dispatch(
            $request->input('clientDbId'),
            $request->route()?->getName() ?? $request->path(),
            ClientApiLogResult::ERROR,
            $request->input('decryptedData', []),
            ['error' => $e->getMessage()],
            $request->ip(),
        );
    }

    public function handlePaymentResponse(Request $request, $pgClass)
    {
        try {
            $response = $request->all();
            return app(TransactionService::class)->handlePaymentResponse($response, $pgClass);
        } catch (\Throwable $e) {
            return response()->json(['error' => $e->getMessage()], 400);
        }
    }

    public function getTransactionStatus(Request $request)
    {
        try {
            $transactionDbId = $request->input('transactionDbId');
            $paymentResponse = app(TransactionService::class)->getTransactionStatus($transactionDbId);

            return response()->json($paymentResponse->toArray());
        } catch (\Throwable $e) {
            return response()->json(['error' => $e->getMessage()], 400);
        }
    }

    public function getTransactionDetails(Request $request, string $referenceId)
    {
        try {
            $paymentResponse = app(TransactionService::class)->getTransactionByReference(
                $request->input('clientDbId'),
                $referenceId
            );

            return response()->json($paymentResponse->toArray());
        } catch (\Throwable $e) {
            return response()->json(['error' => $e->getMessage()], 404);
        }
    }

    public function getTransactions(Request $request)
    {
        try {
            $transactions = app(TransactionService::class)->getTransactionsList(
                $request->input('clientDbId'),
                $request->query('start_date'),
                $request->query('end_date'),
                (int) $request->query('per_page', 20)
            );

            return response()->json([
                'data' => $transactions->getCollection()->map(fn ($dto) => $dto->toArray())->all(),
                'meta' => [
                    'current_page' => $transactions->currentPage(),
                    'last_page' => $transactions->lastPage(),
                    'per_page' => $transactions->perPage(),
                    'total' => $transactions->total(),
                ],
            ]);
        } catch (\Throwable $e) {
            return response()->json(['error' => $e->getMessage()], 400);
        }
    }
}
</file>

<file path="app/Http/Controllers/Controller.php">
<?php

namespace App\Http\Controllers;

abstract class Controller
{
    //
}
</file>

<file path="app/Http/Controllers/UtilsController.php">
<?php

namespace App\Http\Controllers;

use App\Classes\Encryption;
use App\Enums\PaymentType;
use App\Enums\TransactionType;
use App\Http\Controllers\Controller;
use App\Models\Client;
use Illuminate\Http\Request;
use Illuminate\Support\Facades\Http;
use Illuminate\Support\Str;

class UtilsController extends Controller
{
    public function testPayment(Request $request) {
        $client_id = $request->query('clientId', 'LQPXDMJR6Q9NXNTN');

        $client = Client::where("client_id", $client_id)->first();

        if (! $client) {
            abort(404, 'Client not found');
        }

        $amount = $request->query('amount', 1000);
        $data = [
            "clientId" => $client_id,
            "currency" => "INR",
            "amount" => $amount,
            "purpose" => "General Donation",
            "paymentType" => $request->query('paymentType', PaymentType::ONE_TIME_PAYMENT->value),
            "transactionType" => $request->query('transactionType', TransactionType::SALE->value),
            "reference_id" => Str::random(10),
            "customer" => [
                "name" => "Test Customer",
                "email" => "dd@dd.com",
                "mobile" => "9303903901"
            ],
        ];



        return redirect()
            ->away(route('initPayment',
                ["clientId" => $client_id,
                    "data"=> Encryption::encrypt($data,$client->client_secret)]));


    }
}
</file>

<file path="app/Http/Middleware/HandleApiClientEncryptedRequest.php">
<?php

namespace App\Http\Middleware;

use App\Classes\Encryption;
use App\Enums\ClientApiLogResult;
use App\Events\ClientApiEvent;
use App\Models\Client;
use Closure;
use Illuminate\Http\Request;
use Symfony\Component\HttpFoundation\Response;

class HandleApiClientEncryptedRequest
{
    public function handle(Request $request, Closure $next): Response
    {
        // 1. Retrieve the signature provided by the client
        $clientId = $request->input('clientId');
        if (! $clientId) {
            return response()->json(['error' => 'Client ID missing'], 401);
        }

        $encryptedData = $request->input('data');
        if (! $encryptedData) {
            return response()->json(['error' => 'Data missing'], 401);
        }

        // 2. Fetch the shared secret key from db
        $client = Client::where('client_id', $clientId)->where('status', 1)->first();
        if (! $client) {
            return response()->json(['error' => 'Invalid Client'], 401);
        }

        $secretKey = $client->client_secret;

        $decryptedData = Encryption::decrypt($encryptedData, $secretKey);

        if ($decryptedData === false) {
            $this->logEvent($request, $client, ClientApiLogResult::ERROR, ['error' => 'Invalid Data']);

            return response()->json(['error' => 'Invalid Data'], 401);
        }

        $decryptedData = json_decode($decryptedData,true);

        if(!$decryptedData) {
            $this->logEvent($request, $client, ClientApiLogResult::ERROR, ['error' => 'Malformed Data']);

            return response()->json(['error' => 'Malformed Data'], 401);
        }

        $request->merge(['decryptedData'=> $decryptedData,'clientDbId' => $client->id]);
        return $next($request);
    }

    /**
     * @param array<string, mixed> $responseData
     * @param array<string, mixed>|null $requestData
     */
    private function logEvent(Request $request, Client $client, ClientApiLogResult $result, array $responseData, ?array $requestData = null): void
    {
        ClientApiEvent::dispatch(
            $client->id,
            $request->route()?->getName() ?? $request->path(),
            $result,
            $requestData ?? [],
            $responseData,
            $request->ip(),
        );
    }
}
</file>

<file path="app/Http/Middleware/HandleApiClientRequest.php">
<?php

namespace App\Http\Middleware;

use App\Classes\Encryption;
use App\Enums\ClientApiLogResult;
use App\Events\ClientApiEvent;
use App\Models\Client;
use Closure;
use Illuminate\Http\Request;
use Symfony\Component\HttpFoundation\Response;

class HandleApiClientRequest
{
    public function handle(Request $request, Closure $next): Response
    {
        // 1. Retrieve the signature provided by the client

        // 2. Fetch the shared secret key from db
        $client = Client::where('client_id', $clientId)->where('status', 1)->first();
        if (! $client) {
            return response()->json(['error' => 'Invalid Client'], 401);
        }

        $secretKey = $client->client_secret;

        $request->merge(['clientDbId' => $client->id]);
        return $next($request);
    }

    /**
     * @param array<string, mixed> $responseData
     * @param array<string, mixed>|null $requestData
     */
    private function logEvent(Request $request, Client $client, ClientApiLogResult $result, array $responseData, ?array $requestData = null): void
    {
        ClientApiEvent::dispatch(
            $client->id,
            $request->route()?->getName() ?? $request->path(),
            $result,
            $requestData ?? [],
            $responseData,
            $request->ip(),
        );
    }
}
</file>

<file path="app/Http/Middleware/HandleApiRequest.php">
<?php

namespace App\Http\Middleware;

use App\Enums\ClientApiLogResult;
use App\Events\ClientApiEvent;
use App\Models\Client;
use Closure;
use Illuminate\Http\Request;
use Symfony\Component\HttpFoundation\Response;

class HandleApiRequest
{
    public function handle(Request $request, Closure $next): Response
    {
        $token = $request->header('X-TOKEN');

        if (! $token || ! str_contains($token, ':')) {
            return response()->json(['error' => 'X-TOKEN header missing or malformed'], 401);
        }

        [$clientId, $clientSecret] = explode(':', $token, 2);

        $client = Client::where('client_id', $clientId)->where('status', 1)->first();

        if (! $client || ! hash_equals($client->client_secret, $clientSecret)) {
            $this->logEvent($request, $client, ['error' => 'Invalid client credentials']);

            return response()->json(['error' => 'Invalid credentials'], 401);
        }

        $request->merge(['clientDbId' => $client->id]);
        $request->attributes->set('client', $client);

        return $next($request);
    }

    /**
     * @param array<string, mixed> $responseData
     */
    private function logEvent(Request $request, ?Client $client, array $responseData): void
    {
        ClientApiEvent::dispatch(
            $client?->id,
            $request->route()?->getName() ?? $request->path(),
            ClientApiLogResult::ERROR,
            [],
            $responseData,
            $request->ip(),
        );
    }
}
</file>

<file path="app/Http/Middleware/VerifyHmacSignature.php">
<?php

namespace App\Http\Middleware;

use App\Models\Client;
use Closure;
use Illuminate\Http\Request;
use Symfony\Component\HttpFoundation\Response;

class VerifyHmacSignature
{
    public function handle(Request $request, Closure $next): Response
    {
        // 1. Retrieve the signature provided by the client
        $clientId = $request->header('X-Client-Id');
        if (! $clientId) {
            return response()->json(['error' => 'Client ID missing'], 401);
        }

        $clientSignature = $request->header('X-Signature');

        if (! $clientSignature) {
            return response()->json(['error' => 'Signature missing'], 401);
        }

        // 2. Fetch the shared secret key from db
        $client = Client::where('client_id', $clientId)->where('status', 1)->first();
        if (! $client) {
            return response()->json(['error' => 'Invalid Client'], 401);
        }

        $secretKey = $client->client_secret;

        // 3. Recompute the signature using the raw request payload
        $payload = $request->getContent();
        $computedSignature = hash_hmac('sha256', $payload, $secretKey);

        // 4. Use hash_equals to prevent timing attacks
        if (! hash_equals($computedSignature, $clientSignature)) {
            return response()->json(['error' => 'Invalid signature'], 401);
        }

        return $next($request);
    }
}
</file>

<file path="app/Http/Requests/Settings/ProfileDeleteRequest.php">
<?php

namespace App\Http\Requests\Settings;

use App\Concerns\PasswordValidationRules;
use Illuminate\Contracts\Validation\ValidationRule;
use Illuminate\Foundation\Http\FormRequest;

class ProfileDeleteRequest extends FormRequest
{
    use PasswordValidationRules;

    /**
     * Get the validation rules that apply to the request.
     *
     * @return array<string, ValidationRule|array<mixed>|string>
     */
    public function rules(): array
    {
        return [
            'password' => $this->currentPasswordRules(),
        ];
    }
}
</file>

<file path="app/Http/Requests/Settings/ProfileUpdateRequest.php">
<?php

namespace App\Http\Requests\Settings;

use App\Concerns\ProfileValidationRules;
use Illuminate\Contracts\Validation\ValidationRule;
use Illuminate\Foundation\Http\FormRequest;

class ProfileUpdateRequest extends FormRequest
{
    use ProfileValidationRules;

    /**
     * Get the validation rules that apply to the request.
     *
     * @return array<string, ValidationRule|array<mixed>|string>
     */
    public function rules(): array
    {
        return $this->profileRules($this->user()->id);
    }
}
</file>

<file path="app/Http/Requests/V1/ValidatePaymentRequest.php">
<?php

namespace App\Http\Requests\V1;

use App\Enums\PaymentType;
use App\Enums\TransactionType;
use App\Models\Transaction;
use Devhammed\LaravelBrickMoney\Rules\CurrencyRule;
use Illuminate\Contracts\Validation\ValidationRule;
use Illuminate\Contracts\Validation\Validator;
use Illuminate\Foundation\Http\FormRequest;
use Illuminate\Http\Exceptions\HttpResponseException;
use Illuminate\Http\Response;
use Illuminate\Validation\Rule;
use Illuminate\Validation\Rules\Enum;

class ValidatePaymentRequest extends FormRequest
{
    protected function prepareForValidation()
    {
        $this->merge([
            'site_reference_id' => $this->decryptedData['reference_id'],
            'clientId' => $this->decryptedData['clientId'],
            'currency' => $this->decryptedData['currency'],
            'amount' => $this->decryptedData['amount'],
            'transactionType' => $this->decryptedData['transactionType'],
            'paymentType' => $this->decryptedData['paymentType'],
            'customer' => $this->decryptedData['customer'],
            'requestData' => $this->decryptedData,
        ]);
    }

    /**
     * Determine if the user is authorized to make this request.
     */
    public function authorize(): bool
    {
        return true;
    }

    /**
     * Get the validation rules that apply to the request.
     *
     * @return array<string, ValidationRule|array<mixed>|string>
     */
    public function rules(): array
    {
        return [
            'clientDbId' => (['required', 'integer']),
            'clientId' => (['required', 'string', 'exists:clients,client_id']),
            'currency' => (['required', new CurrencyRule]),
            'amount' => (['required', 'numeric']),
            'transactionType' => (['required', 'string', new Enum(TransactionType::class)]),
            'paymentType' => (['required', 'string', new Enum(PaymentType::class)]),
            'site_reference_id' => (['required', 'string',
                Rule::unique(Transaction::class)->where(function ($query) {
                    return $query->where('client_id', $this->decryptedData['clientId'])
                        ->where('site_reference_id', $this->decryptedData['reference_id']);
                }),
            ]),

            'customer' => (['required', 'array']),
            'customer.name' => (['required', 'string']),
            'customer.email' => (['required', 'email']),
            'customer.mobile' => (['required', 'numeric']),

            'requestData' => (['required', 'array']),
        ];
    }

    /**
     * Handle a failed validation attempt.
     */
    protected function failedValidation(Validator $validator): void
    {
        // 1. Extract error messages
        $errors = $validator->errors()->toArray();

        // 3. Stop execution and throw custom JSON response
        throw new HttpResponseException(
            response()->json([
                'success' => false,
                'message' => 'Validation error occurred.',
                'errors' => $errors,
            ], Response::HTTP_UNPROCESSABLE_ENTITY) // Status Code: 422
        );
    }
}
</file>

<file path="app/Listeners/LogClientApiEvent.php">
<?php

namespace App\Listeners;

use App\Events\ClientApiEvent;
use App\Models\ClientApiLog;

class LogClientApiEvent
{
    public function handle(ClientApiEvent $event): void
    {
        ClientApiLog::create([
            'client_id' => $event->clientId,
            'event' => $event->event,
            'request_data' => $event->requestData,
            'response_data' => $event->responseData,
            'source_ip' => $event->sourceIp,
            'result' => $event->result,
            'datetime' => now(),
        ]);
    }
}
</file>

<file path="app/Listeners/PgTransactionEventListener.php">
<?php

namespace App\Listeners;

use App\Events\PgTransactionEvent;
use App\Mail\TransactionStatusMail;
use App\Models\Transaction;
use Illuminate\Contracts\Queue\ShouldQueue;
use Illuminate\Support\Facades\Http;
use Illuminate\Support\Facades\Log;
use Illuminate\Support\Facades\Mail;

class PgTransactionEventListener implements ShouldQueue
{
    public function handle(PgTransactionEvent $event): void
    {
        $transaction = Transaction::with('client.user')->find($event->paymentResponseDTO->transactionDbId);

        if (! $transaction || ! $transaction->client) {
            return;
        }

        $client = $transaction->client;

        if ($client->webhook_uri) {
            $this->sendWebhook($client, $event);
        }

        if (config('services.pg_transaction_notifications.email_enabled') && $client->user?->email) {
            $this->sendEmail($client->user->email, $event);
        }
    }

    protected function sendWebhook($client, PgTransactionEvent $event): void
    {
        $payload = [
            'transaction_id' => $event->paymentResponseDTO->transactionId,
            'site_reference_id' => $event->paymentResponseDTO->siteReferenceId,
            'status' => $event->transactionStatus->value,
            'amount' => (string) $event->paymentResponseDTO->amount->getAmount(),
            'total_amount' => (string) $event->paymentResponseDTO->totalAmount->getAmount(),
            'currency' => (string) $event->paymentResponseDTO->currency,
            'payment_method' => $event->paymentResponseDTO->paymentMethod->value,
            'transaction_date_time' => $event->paymentResponseDTO->transactionDateTime->toIso8601String(),
        ];

        $signature = hash_hmac('sha256', json_encode($payload), $client->client_secret);

        try {
            Http::withHeaders([
                'X-Client-Id' => $client->client_id,
                'X-Signature' => $signature,
            ])->post($client->webhook_uri, $payload)->throw();
        } catch (\Throwable $e) {
            Log::warning('PG transaction webhook delivery failed', [
                'client_id' => $client->client_id,
                'webhook_uri' => $client->webhook_uri,
                'error' => $e->getMessage(),
            ]);
        }
    }

    protected function sendEmail(string $email, PgTransactionEvent $event): void
    {
        try {
            Mail::to($email)->send(new TransactionStatusMail($event->paymentResponseDTO, $event->transactionStatus));
        } catch (\Throwable $e) {
            Log::warning('PG transaction status email delivery failed', [
                'email' => $email,
                'error' => $e->getMessage(),
            ]);
        }
    }
}
</file>

<file path="app/Livewire/ClientApiLogsTable.php">
<?php

namespace App\Livewire;

use App\Enums\ClientApiLogResult;
use App\Models\Client;
use App\Models\ClientApiLog;
use Closure;
use Filament\Actions\Action;
use Filament\Actions\Concerns\InteractsWithActions;
use Filament\Actions\Contracts\HasActions;
use Filament\Schemas\Concerns\InteractsWithSchemas;
use Filament\Schemas\Contracts\HasSchemas;
use Filament\Tables\Columns\TextColumn;
use Filament\Tables\Concerns\InteractsWithTable;
use Filament\Tables\Contracts\HasTable;
use Filament\Tables\Table;
use Illuminate\Database\Eloquent\Builder;
use Illuminate\Support\HtmlString;
use Livewire\Component;

class ClientApiLogsTable extends Component implements HasActions, HasSchemas, HasTable
{
    use InteractsWithActions;
    use InteractsWithSchemas;
    use InteractsWithTable;

    public Client $client;

    public function table(Table $table): Table
    {
        return $table
            ->query(fn (): Builder => ClientApiLog::query()->where('client_id', $this->client->id))
            ->columns([
                TextColumn::make('event')
                    ->searchable()
                    ->sortable(),

                TextColumn::make('result')
                    ->badge()
                    ->color(fn (ClientApiLogResult $state): string => match ($state) {
                        ClientApiLogResult::SUCCESS => 'success',
                        ClientApiLogResult::ERROR => 'danger',
                    })
                    ->searchable()
                    ->sortable(),

                TextColumn::make('source_ip')
                    ->label('Source IP')
                    ->searchable()
                    ->sortable(),

                TextColumn::make('request_data')
                    ->label('Request Data')
                    ->state(fn (ClientApiLog $record): string => $record->request_data ? json_encode($record->request_data) : '')
                    ->limit(50)
                    ->wrap()
                    ->action($this->makeJsonModalAction('viewRequestData', 'Request Data', fn (ClientApiLog $record) => $record->request_data)),

                TextColumn::make('response_data')
                    ->label('Response Data')
                    ->state(fn (ClientApiLog $record): string => $record->response_data ? json_encode($record->response_data) : '')
                    ->limit(50)
                    ->wrap()
                    ->action($this->makeJsonModalAction('viewResponseData', 'Response Data', fn (ClientApiLog $record) => $record->response_data)),

                TextColumn::make('datetime')
                    ->label('Datetime')
                    ->dateTime()
                    ->searchable()
                    ->sortable(),
            ])
            ->defaultSort('datetime', 'desc');
    }

    /**
     * @param Closure(ClientApiLog): (array<string, mixed>|null) $getData
     */
    protected function makeJsonModalAction(string $name, string $heading, Closure $getData): Action
    {
        return Action::make($name)
            ->modalHeading($heading)
            ->modalContent(fn (ClientApiLog $record): HtmlString => new HtmlString(
                '<pre class="fi-ta-text w-full max-w-full overflow-x-auto whitespace-pre-wrap break-all text-xs">'
                . e($getData($record) ? json_encode($getData($record), JSON_PRETTY_PRINT) : 'No data')
                . '</pre>'
            ))
            ->modalSubmitAction(false)
            ->modalCancelActionLabel('Close');
    }

    public function render()
    {
        return view('livewire.client-api-logs-table');
    }
}
</file>

<file path="app/Mail/TransactionStatusMail.php">
<?php

namespace App\Mail;

use App\DTO\PaymentResponseDTO;
use App\Enums\TransactionStatus;
use Illuminate\Bus\Queueable;
use Illuminate\Mail\Mailable;
use Illuminate\Mail\Mailables\Content;
use Illuminate\Mail\Mailables\Envelope;
use Illuminate\Queue\SerializesModels;

class TransactionStatusMail extends Mailable
{
    use Queueable, SerializesModels;

    public function __construct(
        public readonly PaymentResponseDTO $paymentResponseDTO,
        public readonly TransactionStatus $transactionStatus,
    ) {
    }

    public function envelope(): Envelope
    {
        return new Envelope(
            subject: 'Transaction '.$this->transactionStatus->value.' - '.$this->paymentResponseDTO->siteReferenceId,
        );
    }

    public function content(): Content
    {
        return new Content(
            markdown: 'emails.transaction-status',
        );
    }
}
</file>

<file path="app/Models/ClientApiLog.php">
<?php

namespace App\Models;

use App\Enums\ClientApiLogResult;
use Illuminate\Database\Eloquent\Model;
use Illuminate\Database\Eloquent\Relations\BelongsTo;

class ClientApiLog extends Model
{
    protected $casts = [
        'request_data' => 'array',
        'response_data' => 'array',
        'result' => ClientApiLogResult::class,
        'datetime' => 'datetime',
    ];

    /**
     * @return BelongsTo<Client, $this>
     */
    public function client(): BelongsTo
    {
        return $this->belongsTo(Client::class);
    }
}
</file>

<file path="app/Models/SupportedPaymentGateway.php">
<?php

namespace App\Models;

use Illuminate\Database\Eloquent\Model;

class SupportedPaymentGateway extends Model
{
    protected $casts = [
        'attributes' => 'array',
        'status' => 'boolean',
    ];
}
</file>

<file path="app/Policies/ClientConnectionsPolicy.php">
<?php

namespace App\Policies;

use App\Models\ClientConnection;
use App\Models\User;
use Illuminate\Auth\Access\Response;

class ClientConnectionsPolicy
{
    /**
     * Determine whether the user can view any models.
     */
    public function viewAny(User $user): bool
    {
        return ($user->can("can_view_client_pg_connection"));
    }

    /**
     * Determine whether the user can view the model.
     */
    public function view(User $user, ClientConnection $clientConnection): bool
    {
        return ($user->can("can_view_client_pg_connection"));
    }

    /**
     * Determine whether the user can create models.
     */
    public function create(User $user): bool
    {
        return ($user->can("can_create_client_pg_connection"));
    }

    /**
     * Determine whether the user can update the model.
     */
    public function update(User $user, ClientConnection $clientConnection): bool
    {
        return ($user->can("can_edit_client_pg_connection"));
    }

    /**
     * Determine whether the user can delete the model.
     */
    public function delete(User $user, ClientConnection $clientConnection): bool
    {
        return ($user->can("can_delete_client_pg_connection"));
    }

    /**
     * Determine whether the user can restore the model.
     */
    public function restore(User $user, ClientConnection $clientConnection): bool
    {
        return false;
    }

    /**
     * Determine whether the user can permanently delete the model.
     */
    public function forceDelete(User $user, ClientConnection $clientConnection): bool
    {
        return false;
    }
}
</file>

<file path="app/Policies/ClientCustomerPolicy.php">
<?php

namespace App\Policies;

use App\Models\ClientCustomer;
use App\Models\User;
use Illuminate\Auth\Access\Response;

class ClientCustomerPolicy
{
    /**
     * Determine whether the user can view any models.
     */
    public function viewAny(User $user): bool
    {
        return false;
    }

    /**
     * Determine whether the user can view the model.
     */
    public function view(User $user, ClientCustomer $clientCustomer): bool
    {
        return false;
    }

    /**
     * Determine whether the user can create models.
     */
    public function create(User $user): bool
    {
        return false;
    }

    /**
     * Determine whether the user can update the model.
     */
    public function update(User $user, ClientCustomer $clientCustomer): bool
    {
        return false;
    }

    /**
     * Determine whether the user can delete the model.
     */
    public function delete(User $user, ClientCustomer $clientCustomer): bool
    {
        return false;
    }

    /**
     * Determine whether the user can restore the model.
     */
    public function restore(User $user, ClientCustomer $clientCustomer): bool
    {
        return false;
    }

    /**
     * Determine whether the user can permanently delete the model.
     */
    public function forceDelete(User $user, ClientCustomer $clientCustomer): bool
    {
        return false;
    }
}
</file>

<file path="app/Policies/ClientsPolicy.php">
<?php

namespace App\Policies;

use App\Models\Client;
use App\Models\User;
use Illuminate\Auth\Access\Response;

class ClientsPolicy
{
    /**
     * Determine whether the user can view any models.
     */
    public function viewAny(User $user): bool
    {
        return $user->can('can_view_client') || $user->can('can_view_all_clients');
    }

    /**
     * Determine whether the user can view the model.
     */
    public function view(User $user, Client $client): bool
    {
        if ($user->can('can_view_all_clients')) {
            return true;
        }

        return $user->can('can_view_client')
            && $client->users()->whereKey($user->id)->exists();
    }

    /**
     * Determine whether the user can create models.
     */
    public function create(User $user): bool
    {
        return $user->can('can_create_client');
    }

    /**
     * Determine whether the user can update the model.
     */
    public function update(User $user, Client $clients): bool
    {
        return $user->can('can_edit_client');
    }

    /**
     * Determine whether the user can delete the model.
     */
    public function delete(User $user, Client $clients): bool
    {
        return $user->can('can_edit_client');
    }

    /**
     * Determine whether the user can restore the model.
     */
    public function restore(User $user, Client $client): bool
    {
        return false;
    }

    /**
     * Determine whether the user can permanently delete the model.
     */
    public function forceDelete(User $user, Client $client): bool
    {
        return false;
    }
}
</file>

<file path="app/Policies/PGConnectionPolicy.php">
<?php

namespace App\Policies;

use App\Models\PGConnection;
use App\Models\User;
use Illuminate\Auth\Access\Response;

class PGConnectionPolicy
{
    /**
     * Determine whether the user can view any models.
     */
    public function viewAny(User $user): bool
    {
        return ($user->can("can_view_pg_connection"));
    }

    /**
     * Determine whether the user can view the model.
     */
    public function view(User $user, PGConnection $pGConnection): bool
    {
        return ($user->can("can_view_pg_connection"));
    }

    /**
     * Determine whether the user can create models.
     */
    public function create(User $user): bool
    {
        return ($user->can("can_create_pg_connection"));
    }

    /**
     * Determine whether the user can update the model.
     */
    public function update(User $user, PGConnection $pGConnection): bool
    {
        return ($user->can("can_edit_pg_connection"));
    }

    /**
     * Determine whether the user can delete the model.
     */
    public function delete(User $user, PGConnection $pGConnection): bool
    {
        return ($user->can("can_delete_pg_connection"));
    }

    /**
     * Determine whether the user can restore the model.
     */
    public function restore(User $user, PGConnection $pGConnection): bool
    {
        return false;
    }

    /**
     * Determine whether the user can permanently delete the model.
     */
    public function forceDelete(User $user, PGConnection $pGConnection): bool
    {
        return false;
    }
}
</file>

<file path="app/Policies/SubscriptionPolicy.php">
<?php

namespace App\Policies;

use App\Models\Subscription;
use App\Models\User;
use Illuminate\Auth\Access\Response;

class SubscriptionPolicy
{
    /**
     * Determine whether the user can view any models.
     */
    public function viewAny(User $user): bool
    {
        return true;
    }

    /**
     * Determine whether the user can view the model.
     */
    public function view(User $user, Subscription $subscription): bool
    {
        return false;
    }

    /**
     * Determine whether the user can create models.
     */
    public function create(User $user): bool
    {
        return false;
    }

    /**
     * Determine whether the user can update the model.
     */
    public function update(User $user, Subscription $subscription): bool
    {
        return false;
    }

    /**
     * Determine whether the user can delete the model.
     */
    public function delete(User $user, Subscription $subscription): bool
    {
        return false;
    }

    /**
     * Determine whether the user can restore the model.
     */
    public function restore(User $user, Subscription $subscription): bool
    {
        return false;
    }

    /**
     * Determine whether the user can permanently delete the model.
     */
    public function forceDelete(User $user, Subscription $subscription): bool
    {
        return false;
    }
}
</file>

<file path="app/Policies/SubscriptionTransactionPolicy.php">
<?php

namespace App\Policies;

use App\Models\SubscriptionTransaction;
use App\Models\User;
use Illuminate\Auth\Access\Response;

class SubscriptionTransactionPolicy
{
    /**
     * Determine whether the user can view any models.
     */
    public function viewAny(User $user): bool
    {
        return $user->can('can_view_all_transactions') || $user->can('can_view_client');
    }

    /**
     * Determine whether the user can view the model.
     */
    public function view(User $user, SubscriptionTransaction $subscriptionTransaction): bool
    {
        if ($user->can('can_view_all_transactions')) {
            return true;
        }

        return $user->can('can_view_client')
            && $subscriptionTransaction->client->users()->whereKey($user->id)->exists();
    }

    /**
     * Determine whether the user can create models.
     */
    public function create(User $user): bool
    {
        return false;
    }

    /**
     * Determine whether the user can update the model.
     */
    public function update(User $user, SubscriptionTransaction $subscriptionTransaction): bool
    {
        return false;
    }

    /**
     * Determine whether the user can delete the model.
     */
    public function delete(User $user, SubscriptionTransaction $subscriptionTransaction): bool
    {
        return false;
    }

    /**
     * Determine whether the user can restore the model.
     */
    public function restore(User $user, SubscriptionTransaction $subscriptionTransaction): bool
    {
        return false;
    }

    /**
     * Determine whether the user can permanently delete the model.
     */
    public function forceDelete(User $user, SubscriptionTransaction $subscriptionTransaction): bool
    {
        return false;
    }
}
</file>

<file path="app/Repositories/ClientConnectionRepository.php">
<?php

namespace App\Repositories;

use App\Models\ClientConnection;
use Illuminate\Support\Facades\Cache;

class ClientConnectionRepository
{
    public function getClientPGConnection($clientId, $isRecurring)
    {
        return Cache::remember("clientPGConnection.{$clientId}.{$isRecurring}", 3600, function () use ($clientId, $isRecurring) {
            return ClientConnection::join("clients","clients.id","=","clients_connections.client_id")
            ->with('pgConnection')->where('clients.client_id', '=', $clientId)
                ->where('clients_connections.is_recurring', '=', $isRecurring)
                ->first()?->toArray() ?? [];
        });
    }
}
</file>

<file path="app/Repositories/ClientRepository.php">
<?php

namespace App\Repositories;
use App\Models\Client;
use Illuminate\Support\Facades\Cache;
class ClientRepository
{
    public function getClient($clientId)
    {
        return Cache::remember("client.{$clientId}", 300, function () use ($clientId) {
            return Client::where("id", $clientId)->first();
        });
    }
}
</file>

<file path="app/Repositories/PgConnectionRepository.php">
<?php

namespace App\Repositories;

class PgConnectionRepository
{

}
</file>

<file path="app/Services/TransactionService.php">
<?php

namespace App\Services;

use App\Classes\Encryption;
use App\Classes\PaymentGateways\PaymentGatewayFactory;
use App\DTO\PaymentRequestDTO;
use App\DTO\PaymentResponseDTO;
use App\Enums\PaymentMethod;
use App\Enums\PaymentType;
use App\Enums\TransactionStatus;
use App\Events\PgTransactionEvent;
use App\Models\ClientCustomer;
use App\Models\Transaction;
use App\Repositories\ClientConnectionRepository;
use App\Repositories\ClientRepository;
use App\Repositories\PgConnectionRepository;
use Carbon\Carbon;
use Illuminate\Http\RedirectResponse;
use Illuminate\Pagination\LengthAwarePaginator;
use Illuminate\Support\Facades\DB;
use Illuminate\Support\Str;

class TransactionService
{
    public function __construct(
        protected ClientConnectionRepository $clientConnectionRepository,
        protected ClientRepository $clientRepository,
        protected PgConnectionRepository $pgConnectionRepository
    ) {}

    public function initiatePayment(PaymentRequestDTO $paymentRequest)
    {
        $connection = $this->clientConnectionRepository->getClientPGConnection($paymentRequest->clientId,
            $paymentRequest->paymentType === PaymentType::ONE_TIME_PAYMENT ? 0 : 1);

        if (! $connection) {
            throw new \Exception('PG Connection not found for this client.');
        }

        $paymentGateway = PaymentGatewayFactory::create($connection['pg_connection']);
        $paymentRequest->setPgConnectionId($connection['pg_connection']['id']);
        $transaction = $this->saveTransactionFromPaymentRequest($paymentRequest);

        return $paymentGateway->getPaymentUrl($paymentRequest, $transaction);
    }

    public function handlePaymentResponse($response, $pgClass): RedirectResponse
    {
        $paymentGateway = PaymentGatewayFactory::createEmpty($pgClass);
        $paymentResponseDTO = $paymentGateway->handlePaymentResponse($response);

        $transaction = $this->updateTransaction($paymentResponseDTO);
        $client = $this->clientRepository->getClient($transaction->client_id);

        $encryptedData = $this->getEncryptedData($paymentResponseDTO->toArray(), $client->client_secret);

        return redirect()->away($client->redirect_uri.$client->redirect_uri_separator.'data='.urlencode($encryptedData));
    }

    protected function getEncryptedData($data, $key): string
    {
        return Encryption::encrypt($data, $key);
    }

    protected function updateTransaction(PaymentResponseDTO $paymentResponseDTO): Transaction
    {
        $transaction = $this->saveTransactionFromPaymentResponse($paymentResponseDTO);

        //dispatch event only if transaction status changes -to implement
        PgTransactionEvent::dispatch($paymentResponseDTO, $paymentResponseDTO->status);

        return $transaction;
    }

    protected function saveTransactionFromPaymentRequest(PaymentRequestDTO $paymentRequest): Transaction
    {
        return DB::transaction(function () use ($paymentRequest) {
            $client_customer = ClientCustomer::create([
                'client_id' => $paymentRequest->clientDbId,
                'uuid' => Str::ulid(),
                'name' => $paymentRequest->customer['name'],
                'email' => $paymentRequest->customer['email'],
                'mobile' => $paymentRequest->customer['mobile'],
            ]);

            return $client_customer->transactions()->create([
                'client_id' => $paymentRequest->clientDbId,
                'site_reference_id' => $paymentRequest->site_reference_id,
                'pg_connection_id' => $paymentRequest->getPgConnectionId(),
                'amount' => $paymentRequest->amount,
                'currency' => $paymentRequest->currency,
                'transaction_amount' => $paymentRequest->amount,
                'status' => TransactionStatus::PENDING,
                'request_data' => $paymentRequest->requestData,
            ]);
        });

    }

    protected function saveTransactionFromPaymentResponse(PaymentResponseDTO $paymentResponse): Transaction
    {
        return DB::transaction(function () use ($paymentResponse) {
            $transaction = Transaction::find($paymentResponse->transactionDbId);

            if (! $transaction) {
                throw new \Exception('Transaction not found.');
            }

            $transaction->update([
                'transaction_id' => $paymentResponse->transactionId,
                'response_code' => $paymentResponse->status === TransactionStatus::SUCCESS ? '0' : '1',
                'status' => $paymentResponse->status,
                'payment_method' => $paymentResponse->paymentMethod,
                'currency' => $paymentResponse->currency,
                'transaction_amount' => $paymentResponse->amount,
                'pg_fees' => $paymentResponse->pgFees,
                'total_amount' => $paymentResponse->totalAmount,
                'transaction_date_time' => $paymentResponse->transactionDateTime,
                'response_data' => $paymentResponse->pgResponseRaw,
            ]);

            return $transaction;
        });
    }

    public function getTransactionStatus($transactionDbId):PaymentResponseDTO
    {
        $transaction = Transaction::with(['pgConnection'])->find($transactionDbId);
        if (!$transaction) {
            throw new \Exception('Transaction not found.');
        }

        $paymentGateway = PaymentGatewayFactory::create($transaction->toArray()['pg_connection']);
        $paymentResponseDTO = $paymentGateway->getTransactionStatus($transaction);
        $this->updateTransaction($paymentResponseDTO);
        return $paymentResponseDTO;
    }

    public function getTransactionByReference(int|string $clientDbId, string $referenceId): PaymentResponseDTO
    {
        $transaction = Transaction::with(['client', 'pgConnection'])
            ->where('client_id', $clientDbId)
            ->where('site_reference_id', $referenceId)
            ->first();

        if (! $transaction) {
            throw new \Exception('Transaction not found.');
        }

        return $this->mapTransactionToPaymentResponseDTO($transaction);
    }

    public function getTransactionsList(int|string $clientDbId, ?string $startDate, ?string $endDate, int $perPage = 20): LengthAwarePaginator
    {
        $query = Transaction::with(['client', 'pgConnection'])
            ->where('client_id', $clientDbId);

        if ($startDate) {
            $query->where('transaction_date_time', '>=', Carbon::createFromFormat('Ymd', $startDate)->startOfDay());
        }

        if ($endDate) {
            $query->where('transaction_date_time', '<=', Carbon::createFromFormat('Ymd', $endDate)->endOfDay());
        }

        return $query->orderByDesc('transaction_date_time')
            ->paginate(min(max($perPage, 1), 100))
            ->through(fn (Transaction $transaction) => $this->mapTransactionToPaymentResponseDTO($transaction));
    }

    protected function mapTransactionToPaymentResponseDTO(Transaction $transaction): PaymentResponseDTO
    {
        $paymentMethod = PaymentMethod::tryFrom((string) $transaction->getRawOriginal('payment_method')) ?? PaymentMethod::UNKNOWN;

        try {
            $transactionDateTime = $transaction->transaction_date_time ?? $transaction->created_at;
        } catch (\Throwable) {
            $transactionDateTime = $transaction->created_at;
        }

        $transactionDateTime = Carbon::instance($transactionDateTime);

        return new PaymentResponseDTO(
            transactionDbId: (string) $transaction->id,
            siteReferenceId: $transaction->site_reference_id,
            status: $transaction->status,
            transactionId: (string) $transaction->transaction_id,
            description: $transaction->response_data['respDescription'] ?? '',
            amount: $transaction->transaction_amount['transaction_amount'],
            pgFees: $transaction->pg_fees['pg_fees'],
            totalAmount: $transaction->total_amount['total_amount'],
            transactionDateTime: $transactionDateTime,
            currency: $transaction->currency,
            paymentMethod: $paymentMethod,
            clientName: $transaction->client->name,
            pgConnection: $transaction->pgConnection->name ?? '',
            pgResponseRaw: $transaction->response_data ?? [],
        );
    }
}
</file>

<file path="config/app.php">
<?php

return [

    /*
    |--------------------------------------------------------------------------
    | Application Name
    |--------------------------------------------------------------------------
    |
    | This value is the name of your application, which will be used when the
    | framework needs to place the application's name in a notification or
    | other UI elements where an application name needs to be displayed.
    |
    */

    'name' => env('APP_NAME', 'Laravel'),

    /*
    |--------------------------------------------------------------------------
    | Application Environment
    |--------------------------------------------------------------------------
    |
    | This value determines the "environment" your application is currently
    | running in. This may determine how you prefer to configure various
    | services the application utilizes. Set this in your ".env" file.
    |
    */

    'env' => env('APP_ENV', 'production'),

    /*
    |--------------------------------------------------------------------------
    | Application Debug Mode
    |--------------------------------------------------------------------------
    |
    | When your application is in debug mode, detailed error messages with
    | stack traces will be shown on every error that occurs within your
    | application. If disabled, a simple generic error page is shown.
    |
    */

    'debug' => (bool) env('APP_DEBUG', false),

    /*
    |--------------------------------------------------------------------------
    | Application URL
    |--------------------------------------------------------------------------
    |
    | This URL is used by the console to properly generate URLs when using
    | the Artisan command line tool. You should set this to the root of
    | the application so that it's available within Artisan commands.
    |
    */

    'url' => env('APP_URL', 'http://localhost'),

    /*
    |--------------------------------------------------------------------------
    | Application Timezone
    |--------------------------------------------------------------------------
    |
    | Here you may specify the default timezone for your application, which
    | will be used by the PHP date and date-time functions. The timezone
    | is set to "UTC" by default as it is suitable for most use cases.
    |
    */

    'timezone' => 'UTC',

    /*
    |--------------------------------------------------------------------------
    | Application Locale Configuration
    |--------------------------------------------------------------------------
    |
    | The application locale determines the default locale that will be used
    | by Laravel's translation / localization methods. This option can be
    | set to any locale for which you plan to have translation strings.
    |
    */

    'locale' => env('APP_LOCALE', 'en'),

    'fallback_locale' => env('APP_FALLBACK_LOCALE', 'en'),

    'faker_locale' => env('APP_FAKER_LOCALE', 'en_US'),

    /*
    |--------------------------------------------------------------------------
    | Encryption Key
    |--------------------------------------------------------------------------
    |
    | This key is utilized by Laravel's encryption services and should be set
    | to a random, 32 character string to ensure that all encrypted values
    | are secure. You should do this prior to deploying the application.
    |
    */

    'cipher' => 'AES-256-CBC',

    'key' => env('APP_KEY'),

    'previous_keys' => [
        ...array_filter(
            explode(',', (string) env('APP_PREVIOUS_KEYS', '')),
        ),
    ],

    /*
    |--------------------------------------------------------------------------
    | Maintenance Mode Driver
    |--------------------------------------------------------------------------
    |
    | These configuration options determine the driver used to determine and
    | manage Laravel's "maintenance mode" status. The "cache" driver will
    | allow maintenance mode to be controlled across multiple machines.
    |
    | Supported drivers: "file", "cache"
    |
    */

    'maintenance' => [
        'driver' => env('APP_MAINTENANCE_DRIVER', 'file'),
        'store' => env('APP_MAINTENANCE_STORE', 'database'),
    ],

];
</file>

<file path="config/auth.php">
<?php

use App\Models\User;

return [

    /*
    |--------------------------------------------------------------------------
    | Authentication Defaults
    |--------------------------------------------------------------------------
    |
    | This option defines the default authentication "guard" and password
    | reset "broker" for your application. You may change these values
    | as required, but they're a perfect start for most applications.
    |
    */

    'defaults' => [
        'guard' => env('AUTH_GUARD', 'web'),
        'passwords' => env('AUTH_PASSWORD_BROKER', 'users'),
    ],

    /*
    |--------------------------------------------------------------------------
    | Authentication Guards
    |--------------------------------------------------------------------------
    |
    | Next, you may define every authentication guard for your application.
    | Of course, a great default configuration has been defined for you
    | which utilizes session storage plus the Eloquent user provider.
    |
    | All authentication guards have a user provider, which defines how the
    | users are actually retrieved out of your database or other storage
    | system used by the application. Typically, Eloquent is utilized.
    |
    | Supported: "session"
    |
    */

    'guards' => [
        'web' => [
            'driver' => 'session',
            'provider' => 'users',
        ],
    ],

    /*
    |--------------------------------------------------------------------------
    | User Providers
    |--------------------------------------------------------------------------
    |
    | All authentication guards have a user provider, which defines how the
    | users are actually retrieved out of your database or other storage
    | system used by the application. Typically, Eloquent is utilized.
    |
    | If you have multiple user tables or models you may configure multiple
    | providers to represent the model / table. These providers may then
    | be assigned to any extra authentication guards you have defined.
    |
    | Supported: "database", "eloquent"
    |
    */

    'providers' => [
        'users' => [
            'driver' => 'eloquent',
            'model' => env('AUTH_MODEL', User::class),
        ],

        // 'users' => [
        //     'driver' => 'database',
        //     'table' => 'users',
        // ],
    ],

    /*
    |--------------------------------------------------------------------------
    | Resetting Passwords
    |--------------------------------------------------------------------------
    |
    | These configuration options specify the behavior of Laravel's password
    | reset functionality, including the table utilized for token storage
    | and the user provider that is invoked to actually retrieve users.
    |
    | The expiry time is the number of minutes that each reset token will be
    | considered valid. This security feature keeps tokens short-lived so
    | they have less time to be guessed. You may change this as needed.
    |
    | The throttle setting is the number of seconds a user must wait before
    | generating more password reset tokens. This prevents the user from
    | quickly generating a very large amount of password reset tokens.
    |
    */

    'passwords' => [
        'users' => [
            'provider' => 'users',
            'table' => env('AUTH_PASSWORD_RESET_TOKEN_TABLE', 'password_reset_tokens'),
            'expire' => 60,
            'throttle' => 60,
        ],
    ],

    /*
    |--------------------------------------------------------------------------
    | Password Confirmation Timeout
    |--------------------------------------------------------------------------
    |
    | Here you may define the number of seconds before a password confirmation
    | window expires and users are asked to re-enter their password via the
    | confirmation screen. By default, the timeout lasts for three hours.
    |
    */

    'password_timeout' => env('AUTH_PASSWORD_TIMEOUT', 10800),

];
</file>

<file path="config/cache.php">
<?php

use Illuminate\Support\Str;

return [

    /*
    |--------------------------------------------------------------------------
    | Default Cache Store
    |--------------------------------------------------------------------------
    |
    | This option controls the default cache store that will be used by the
    | framework. This connection is utilized if another isn't explicitly
    | specified when running a cache operation inside the application.
    |
    */

    'default' => env('CACHE_STORE', 'database'),

    /*
    |--------------------------------------------------------------------------
    | Cache Stores
    |--------------------------------------------------------------------------
    |
    | Here you may define all of the cache "stores" for your application as
    | well as their drivers. You may even define multiple stores for the
    | same cache driver to group types of items stored in your caches.
    |
    | Supported drivers: "array", "database", "file", "memcached",
    |                    "redis", "dynamodb", "storage", "octane",
    |                    "session", "failover", "null"
    |
    */

    'stores' => [

        'array' => [
            'driver' => 'array',
            'serialize' => false,
        ],

        'database' => [
            'driver' => 'database',
            'connection' => env('DB_CACHE_CONNECTION'),
            'table' => env('DB_CACHE_TABLE', 'cache'),
            'lock_connection' => env('DB_CACHE_LOCK_CONNECTION'),
            'lock_table' => env('DB_CACHE_LOCK_TABLE'),
        ],

        'file' => [
            'driver' => 'file',
            'path' => storage_path('framework/cache/data'),
            'lock_path' => storage_path('framework/cache/data'),
        ],

        'storage' => [
            'driver' => 'storage',
            'disk' => env('CACHE_STORAGE_DISK'),
            'path' => env('CACHE_STORAGE_PATH', 'framework/cache/data'),
        ],

        'memcached' => [
            'driver' => 'memcached',
            'persistent_id' => env('MEMCACHED_PERSISTENT_ID'),
            'sasl' => [
                env('MEMCACHED_USERNAME'),
                env('MEMCACHED_PASSWORD'),
            ],
            'options' => [
                // Memcached::OPT_CONNECT_TIMEOUT => 2000,
            ],
            'servers' => [
                [
                    'host' => env('MEMCACHED_HOST', '127.0.0.1'),
                    'port' => env('MEMCACHED_PORT', 11211),
                    'weight' => 100,
                ],
            ],
        ],

        'redis' => [
            'driver' => 'redis',
            'connection' => env('REDIS_CACHE_CONNECTION', 'cache'),
            'lock_connection' => env('REDIS_CACHE_LOCK_CONNECTION', 'default'),
        ],

        'dynamodb' => [
            'driver' => 'dynamodb',
            'key' => env('AWS_ACCESS_KEY_ID'),
            'secret' => env('AWS_SECRET_ACCESS_KEY'),
            'region' => env('AWS_DEFAULT_REGION', 'us-east-1'),
            'table' => env('DYNAMODB_CACHE_TABLE', 'cache'),
            'endpoint' => env('DYNAMODB_ENDPOINT'),
        ],

        'octane' => [
            'driver' => 'octane',
        ],

        'failover' => [
            'driver' => 'failover',
            'stores' => [
                'database',
                'array',
            ],
        ],

    ],

    /*
    |--------------------------------------------------------------------------
    | Cache Key Prefix
    |--------------------------------------------------------------------------
    |
    | When utilizing the APC, database, memcached, Redis, and DynamoDB cache
    | stores, there might be other applications using the same cache. For
    | that reason, you may prefix every cache key to avoid collisions.
    |
    */

    'prefix' => env('CACHE_PREFIX', Str::slug((string) env('APP_NAME', 'laravel')).'-cache-'),

    /*
    |--------------------------------------------------------------------------
    | Serializable Classes
    |--------------------------------------------------------------------------
    |
    | This value determines the classes that can be unserialized from cache
    | storage. By default, no PHP classes will be unserialized from your
    | cache to prevent gadget chain attacks if your APP_KEY is leaked.
    |
    */

    'serializable_classes' => false,

];
</file>

<file path="config/data.php">
<?php

return [
    /*
     * The package will use this format when working with dates. If this option
     * is an array, it will try to convert from the first format that works,
     * and will serialize dates using the first format from the array.
     */
    'date_format' => DATE_ATOM,

    /*
     * When transforming or casting dates, the following timezone will be used to
     * convert the date to the correct timezone. If set to null no timezone will
     * be passed.
     */
    'date_timezone' => null,

    /*
     * It is possible to enable certain features of the package, these would otherwise
     * be breaking changes, and thus they are disabled by default. In the next major
     * version of the package, these features will be enabled by default.
     */
    'features' => [
        'cast_and_transform_iterables' => false,

        /*
         * When trying to set a computed property value, the package will throw an exception.
         * You can disable this behaviour by setting this option to true, which will then just
         * ignore the value being passed into the computed property and recalculate it.
         */
        'ignore_exception_when_trying_to_set_computed_property_value' => false,
    ],

    /*
     * Global transformers will take complex types and transform them into simple
     * types.
     */
    'transformers' => [
        DateTimeInterface::class => \Spatie\LaravelData\Transformers\DateTimeInterfaceTransformer::class,
        \Illuminate\Contracts\Support\Arrayable::class => \Spatie\LaravelData\Transformers\ArrayableTransformer::class,
        BackedEnum::class => Spatie\LaravelData\Transformers\EnumTransformer::class,
    ],

    /*
     * Global casts will cast values into complex types when creating a data
     * object from simple types.
     */
    'casts' => [
        DateTimeInterface::class => Spatie\LaravelData\Casts\DateTimeInterfaceCast::class,
        BackedEnum::class => Spatie\LaravelData\Casts\EnumCast::class,
//        Enumerable::class => Spatie\LaravelData\Casts\EnumerableCast::class,
    ],

    /*
     * Rule inferrers can be configured here. They will automatically add
     * validation rules to properties of a data object based upon
     * the type of the property.
     */
    'rule_inferrers' => [
        Spatie\LaravelData\RuleInferrers\SometimesRuleInferrer::class,
        Spatie\LaravelData\RuleInferrers\NullableRuleInferrer::class,
        Spatie\LaravelData\RuleInferrers\RequiredRuleInferrer::class,
        Spatie\LaravelData\RuleInferrers\BuiltInTypesRuleInferrer::class,
        Spatie\LaravelData\RuleInferrers\AttributesRuleInferrer::class,
    ],

    /*
     * Normalizers return an array representation of the payload, or null if
     * it cannot normalize the payload. The normalizers below are used for
     * every data object, unless overridden in a specific data object class.
     */
    'normalizers' => [
        Spatie\LaravelData\Normalizers\ModelNormalizer::class,
        // Spatie\LaravelData\Normalizers\FormRequestNormalizer::class,
        Spatie\LaravelData\Normalizers\ArrayableNormalizer::class,
        Spatie\LaravelData\Normalizers\ObjectNormalizer::class,
        Spatie\LaravelData\Normalizers\ArrayNormalizer::class,
        Spatie\LaravelData\Normalizers\JsonNormalizer::class,
    ],

    /*
     * Data objects can be wrapped into a key like 'data' when used as a resource,
     * this key can be set globally here for all data objects. You can pass in
     * `null` if you want to disable wrapping.
     */
    'wrap' => null,

    /*
     * Adds a specific caster to the Symphony VarDumper component which hides
     * some properties from data objects and collections when being dumped
     * by `dump` or `dd`. Can be 'enabled', 'disabled' or 'development'
     * which will only enable the caster locally.
     */
    'var_dumper_caster_mode' => 'development',

    /*
     * It is possible to skip the PHP reflection analysis of data objects
     * when running in production. This will speed up the package. You
     * can configure where data objects are stored and which cache
     * store should be used.
     *
     * Structures are cached forever as they'll become stale when your
     * application is deployed with changes. You can set a duration
     * in seconds if you want the cache to clear after a certain
     * timeframe.
     */
    'structure_caching' => [
        'enabled' => true,
        'directories' => [app_path('Data')],
        'cache' => [
            'store' => env('CACHE_STORE', env('CACHE_DRIVER', 'file')),
            'prefix' => 'laravel-data',
            'duration' => null,
        ],
        'reflection_discovery' => [
            'enabled' => true,
            'base_path' => base_path(),
            'root_namespace' => null,
        ],
    ],

    /*
     * A data object can be validated when created using a factory or when calling the from
     * method. By default, only when a request is passed the data is being validated. This
     * behaviour can be changed to always validate or to completely disable validation.
     */
    'validation_strategy' => \Spatie\LaravelData\Support\Creation\ValidationStrategy::OnlyRequests->value,

    /*
     * A data object can map the names of its properties when transforming (output) or when
     * creating (input). By default, the package will not map any names. You can set a
     * global strategy here, or override it on a specific data object.
     */
    'name_mapping_strategy' => [
        'input' => null,
        'output' => null,
    ],

    /*
     * When using an invalid include, exclude, only or except partial, the package will
     * throw an exception. You can disable this behaviour by setting this option to true.
     */
    'ignore_invalid_partials' => false,

    /*
     * When transforming a nested chain of data objects, the package can end up in an infinite
     * loop when including a recursive relationship. The max transformation depth can be
     * set as a safety measure to prevent this from happening. When set to null, the
     * package will not enforce a maximum depth.
     */
    'max_transformation_depth' => null,

    /*
     * When the maximum transformation depth is reached, the package will throw an exception.
     * You can disable this behaviour by setting this option to true which will return an
     * empty array.
     */
    'throw_when_max_transformation_depth_reached' => true,

    /*
     * When using the `make:data` command, the package will use these settings to generate
     * the data classes. You can override these settings by passing options to the command.
     */
    'commands' => [

        /*
         * Provides default configuration for the `make:data` command. These settings can be overridden with options
         * passed directly to the `make:data` command for generating single Data classes, or if not set they will
         * automatically fall back to these defaults. See `php artisan make:data --help` for more information
         */
        'make' => [

            /*
             * The default namespace for generated Data classes. This exists under the application's root namespace,
             * so the default 'Data` will end up as '\App\Data', and generated Data classes will be placed in the
             * app/Data/ folder. Data classes can live anywhere, but this is where `make:data` will put them.
             */
            'namespace' => 'Data',

            /*
             * This suffix will be appended to all data classes generated by make:data, so that they are less likely
             * to conflict with other related classes, controllers or models with a similar name without resorting
             * to adding an alias for the Data object. Set to a blank string (not null) to disable.
             */
            'suffix' => 'Data',
        ],
    ],

    /*
     * When using Livewire, the package allows you to enable or disable the synths
     * these synths will automatically handle the data objects and their
     * properties when used in a Livewire component.
     */
    'livewire' => [
        'enable_synths' => false,
    ],
];
</file>

<file path="config/database.php">
<?php

use Illuminate\Support\Str;
use Pdo\Mysql;

return [

    /*
    |--------------------------------------------------------------------------
    | Default Database Connection Name
    |--------------------------------------------------------------------------
    |
    | Here you may specify which of the database connections below you wish
    | to use as your default connection for database operations. This is
    | the connection which will be utilized unless another connection
    | is explicitly specified when you execute a query / statement.
    |
    */

    'default' => env('DB_CONNECTION', 'sqlite'),

    /*
    |--------------------------------------------------------------------------
    | Database Connections
    |--------------------------------------------------------------------------
    |
    | Below are all of the database connections defined for your application.
    | An example configuration is provided for each database system which
    | is supported by Laravel. You're free to add / remove connections.
    |
    */

    'connections' => [

        'sqlite' => [
            'driver' => 'sqlite',
            'url' => env('DB_URL'),
            'database' => env('DB_DATABASE', database_path('database.sqlite')),
            'prefix' => '',
            'foreign_key_constraints' => env('DB_FOREIGN_KEYS', true),
            'busy_timeout' => null,
            'journal_mode' => null,
            'synchronous' => null,
            'transaction_mode' => 'DEFERRED',
        ],

        'mysql' => [
            'driver' => 'mysql',
            'url' => env('DB_URL'),
            'host' => env('DB_HOST', '127.0.0.1'),
            'port' => env('DB_PORT', '3306'),
            'database' => env('DB_DATABASE', 'laravel'),
            'username' => env('DB_USERNAME', 'root'),
            'password' => env('DB_PASSWORD', ''),
            'unix_socket' => env('DB_SOCKET', ''),
            'charset' => env('DB_CHARSET', 'utf8mb4'),
            'collation' => env('DB_COLLATION', 'utf8mb4_unicode_ci'),
            'prefix' => '',
            'prefix_indexes' => true,
            'strict' => true,
            'engine' => null,
            'options' => extension_loaded('pdo_mysql') ? array_filter([
                Mysql::ATTR_SSL_CA => env('MYSQL_ATTR_SSL_CA'),
            ]) : [],
        ],

        'mariadb' => [
            'driver' => 'mariadb',
            'url' => env('DB_URL'),
            'host' => env('DB_HOST', '127.0.0.1'),
            'port' => env('DB_PORT', '3306'),
            'database' => env('DB_DATABASE', 'laravel'),
            'username' => env('DB_USERNAME', 'root'),
            'password' => env('DB_PASSWORD', ''),
            'unix_socket' => env('DB_SOCKET', ''),
            'charset' => env('DB_CHARSET', 'utf8mb4'),
            'collation' => env('DB_COLLATION', 'utf8mb4_unicode_ci'),
            'prefix' => '',
            'prefix_indexes' => true,
            'strict' => true,
            'engine' => null,
            'options' => extension_loaded('pdo_mysql') ? array_filter([
                Mysql::ATTR_SSL_CA => env('MYSQL_ATTR_SSL_CA'),
            ]) : [],
        ],

        'pgsql' => [
            'driver' => 'pgsql',
            'url' => env('DB_URL'),
            'host' => env('DB_HOST', '127.0.0.1'),
            'port' => env('DB_PORT', '5432'),
            'database' => env('DB_DATABASE', 'laravel'),
            'username' => env('DB_USERNAME', 'root'),
            'password' => env('DB_PASSWORD', ''),
            'charset' => env('DB_CHARSET', 'utf8'),
            'prefix' => '',
            'prefix_indexes' => true,
            'search_path' => 'public',
            'sslmode' => env('DB_SSLMODE', 'prefer'),
        ],

        'sqlsrv' => [
            'driver' => 'sqlsrv',
            'url' => env('DB_URL'),
            'host' => env('DB_HOST', 'localhost'),
            'port' => env('DB_PORT', '1433'),
            'database' => env('DB_DATABASE', 'laravel'),
            'username' => env('DB_USERNAME', 'root'),
            'password' => env('DB_PASSWORD', ''),
            'charset' => env('DB_CHARSET', 'utf8'),
            'prefix' => '',
            'prefix_indexes' => true,
            // 'encrypt' => env('DB_ENCRYPT', 'yes'),
            // 'trust_server_certificate' => env('DB_TRUST_SERVER_CERTIFICATE', 'false'),
        ],

    ],

    /*
    |--------------------------------------------------------------------------
    | Migration Repository Table
    |--------------------------------------------------------------------------
    |
    | This table keeps track of all the migrations that have already run for
    | your application. Using this information, we can determine which of
    | the migrations on disk haven't actually been run on the database.
    |
    */

    'migrations' => [
        'table' => 'migrations',
        'update_date_on_publish' => true,
    ],

    /*
    |--------------------------------------------------------------------------
    | Redis Databases
    |--------------------------------------------------------------------------
    |
    | Redis is an open source, fast, and advanced key-value store that also
    | provides a richer body of commands than a typical key-value system
    | such as Memcached. You may define your connection settings here.
    |
    */

    'redis' => [

        'client' => env('REDIS_CLIENT', 'phpredis'),

        'options' => [
            'cluster' => env('REDIS_CLUSTER', 'redis'),
            'prefix' => env('REDIS_PREFIX', Str::slug((string) env('APP_NAME', 'laravel')).'-database-'),
            'persistent' => env('REDIS_PERSISTENT', false),
        ],

        'default' => [
            'url' => env('REDIS_URL'),
            'host' => env('REDIS_HOST', '127.0.0.1'),
            'username' => env('REDIS_USERNAME'),
            'password' => env('REDIS_PASSWORD'),
            'port' => env('REDIS_PORT', '6379'),
            'database' => env('REDIS_DB', '0'),
            'max_retries' => env('REDIS_MAX_RETRIES', 3),
            'backoff_algorithm' => env('REDIS_BACKOFF_ALGORITHM', 'decorrelated_jitter'),
            'backoff_base' => env('REDIS_BACKOFF_BASE', 100),
            'backoff_cap' => env('REDIS_BACKOFF_CAP', 1000),
        ],

        'cache' => [
            'url' => env('REDIS_URL'),
            'host' => env('REDIS_HOST', '127.0.0.1'),
            'username' => env('REDIS_USERNAME'),
            'password' => env('REDIS_PASSWORD'),
            'port' => env('REDIS_PORT', '6379'),
            'database' => env('REDIS_CACHE_DB', '1'),
            'max_retries' => env('REDIS_MAX_RETRIES', 3),
            'backoff_algorithm' => env('REDIS_BACKOFF_ALGORITHM', 'decorrelated_jitter'),
            'backoff_base' => env('REDIS_BACKOFF_BASE', 100),
            'backoff_cap' => env('REDIS_BACKOFF_CAP', 1000),
        ],

    ],

];
</file>

<file path="config/filesystems.php">
<?php

return [

    /*
    |--------------------------------------------------------------------------
    | Default Filesystem Disk
    |--------------------------------------------------------------------------
    |
    | Here you may specify the default filesystem disk that should be used
    | by the framework. The "local" disk, as well as a variety of cloud
    | based disks are available to your application for file storage.
    |
    */

    'default' => env('FILESYSTEM_DISK', 'local'),

    /*
    |--------------------------------------------------------------------------
    | Filesystem Disks
    |--------------------------------------------------------------------------
    |
    | Below you may configure as many filesystem disks as necessary, and you
    | may even configure multiple disks for the same driver. Examples for
    | most supported storage drivers are configured here for reference.
    |
    | Supported drivers: "local", "ftp", "sftp", "s3"
    |
    */

    'disks' => [

        'local' => [
            'driver' => 'local',
            'root' => storage_path('app/private'),
            'serve' => true,
            'throw' => false,
            'report' => false,
        ],

        'public' => [
            'driver' => 'local',
            'root' => storage_path('app/public'),
            'url' => rtrim(env('APP_URL', 'http://localhost'), '/').'/storage',
            'visibility' => 'public',
            'throw' => false,
            'report' => false,
        ],

        's3' => [
            'driver' => 's3',
            'key' => env('AWS_ACCESS_KEY_ID'),
            'secret' => env('AWS_SECRET_ACCESS_KEY'),
            'region' => env('AWS_DEFAULT_REGION'),
            'bucket' => env('AWS_BUCKET'),
            'url' => env('AWS_URL'),
            'endpoint' => env('AWS_ENDPOINT'),
            'use_path_style_endpoint' => env('AWS_USE_PATH_STYLE_ENDPOINT', false),
            'throw' => false,
            'report' => false,
        ],

    ],

    /*
    |--------------------------------------------------------------------------
    | Symbolic Links
    |--------------------------------------------------------------------------
    |
    | Here you may configure the symbolic links that will be created when the
    | `storage:link` Artisan command is executed. The array keys should be
    | the locations of the links and the values should be their targets.
    |
    */

    'links' => [
        public_path('storage') => storage_path('app/public'),
    ],

];
</file>

<file path="config/logging.php">
<?php

use Monolog\Handler\NullHandler;
use Monolog\Handler\StreamHandler;
use Monolog\Handler\SyslogUdpHandler;
use Monolog\Processor\PsrLogMessageProcessor;

return [

    /*
    |--------------------------------------------------------------------------
    | Default Log Channel
    |--------------------------------------------------------------------------
    |
    | This option defines the default log channel that is utilized to write
    | messages to your logs. The value provided here should match one of
    | the channels present in the list of "channels" configured below.
    |
    */

    'default' => env('LOG_CHANNEL', 'stack'),

    /*
    |--------------------------------------------------------------------------
    | Deprecations Log Channel
    |--------------------------------------------------------------------------
    |
    | This option controls the log channel that should be used to log warnings
    | regarding deprecated PHP and library features. This allows you to get
    | your application ready for upcoming major versions of dependencies.
    |
    */

    'deprecations' => [
        'channel' => env('LOG_DEPRECATIONS_CHANNEL', 'null'),
        'trace' => env('LOG_DEPRECATIONS_TRACE', false),
    ],

    /*
    |--------------------------------------------------------------------------
    | Log Channels
    |--------------------------------------------------------------------------
    |
    | Here you may configure the log channels for your application. Laravel
    | utilizes the Monolog PHP logging library, which includes a variety
    | of powerful log handlers and formatters that you're free to use.
    |
    | Available drivers: "single", "daily", "slack", "syslog",
    |                    "errorlog", "monolog", "custom", "stack"
    |
    */

    'channels' => [

        'stack' => [
            'driver' => 'stack',
            'channels' => explode(',', (string) env('LOG_STACK', 'single')),
            'ignore_exceptions' => false,
        ],

        'single' => [
            'driver' => 'single',
            'path' => storage_path('logs/laravel.log'),
            'level' => env('LOG_LEVEL', 'debug'),
            'replace_placeholders' => true,
        ],

        'daily' => [
            'driver' => 'daily',
            'path' => storage_path('logs/laravel.log'),
            'level' => env('LOG_LEVEL', 'debug'),
            'days' => env('LOG_DAILY_DAYS', 14),
            'replace_placeholders' => true,
        ],

        'slack' => [
            'driver' => 'slack',
            'url' => env('LOG_SLACK_WEBHOOK_URL'),
            'username' => env('LOG_SLACK_USERNAME', env('APP_NAME', 'Laravel')),
            'emoji' => env('LOG_SLACK_EMOJI', ':boom:'),
            'level' => env('LOG_LEVEL', 'critical'),
            'replace_placeholders' => true,
        ],

        'papertrail' => [
            'driver' => 'monolog',
            'level' => env('LOG_LEVEL', 'debug'),
            'handler' => env('LOG_PAPERTRAIL_HANDLER', SyslogUdpHandler::class),
            'handler_with' => [
                'host' => env('PAPERTRAIL_URL'),
                'port' => env('PAPERTRAIL_PORT'),
                'connectionString' => 'tls://'.env('PAPERTRAIL_URL').':'.env('PAPERTRAIL_PORT'),
            ],
            'processors' => [PsrLogMessageProcessor::class],
        ],

        'stderr' => [
            'driver' => 'monolog',
            'level' => env('LOG_LEVEL', 'debug'),
            'handler' => StreamHandler::class,
            'handler_with' => [
                'stream' => 'php://stderr',
            ],
            'formatter' => env('LOG_STDERR_FORMATTER'),
            'processors' => [PsrLogMessageProcessor::class],
        ],

        'syslog' => [
            'driver' => 'syslog',
            'level' => env('LOG_LEVEL', 'debug'),
            'facility' => env('LOG_SYSLOG_FACILITY', LOG_USER),
            'replace_placeholders' => true,
        ],

        'errorlog' => [
            'driver' => 'errorlog',
            'level' => env('LOG_LEVEL', 'debug'),
            'replace_placeholders' => true,
        ],

        'null' => [
            'driver' => 'monolog',
            'handler' => NullHandler::class,
        ],

        'emergency' => [
            'path' => storage_path('logs/laravel.log'),
        ],

    ],

];
</file>

<file path="config/mail.php">
<?php

return [

    /*
    |--------------------------------------------------------------------------
    | Default Mailer
    |--------------------------------------------------------------------------
    |
    | This option controls the default mailer that is used to send all email
    | messages unless another mailer is explicitly specified when sending
    | the message. All additional mailers can be configured within the
    | "mailers" array. Examples of each type of mailer are provided.
    |
    */

    'default' => env('MAIL_MAILER', 'log'),

    /*
    |--------------------------------------------------------------------------
    | Mailer Configurations
    |--------------------------------------------------------------------------
    |
    | Here you may configure all of the mailers used by your application plus
    | their respective settings. Several examples have been configured for
    | you and you are free to add your own as your application requires.
    |
    | Laravel supports a variety of mail "transport" drivers that can be used
    | when delivering an email. You may specify which one you're using for
    | your mailers below. You may also add additional mailers if needed.
    |
    | Supported: "smtp", "sendmail", "mailgun", "ses", "ses-v2",
    |            "postmark", "resend", "log", "array",
    |            "failover", "roundrobin"
    |
    */

    'mailers' => [

        'smtp' => [
            'transport' => 'smtp',
            'scheme' => env('MAIL_SCHEME'),
            'url' => env('MAIL_URL'),
            'host' => env('MAIL_HOST', '127.0.0.1'),
            'port' => env('MAIL_PORT', 2525),
            'username' => env('MAIL_USERNAME'),
            'password' => env('MAIL_PASSWORD'),
            'timeout' => null,
            'local_domain' => env('MAIL_EHLO_DOMAIN', parse_url((string) env('APP_URL', 'http://localhost'), PHP_URL_HOST)),
        ],

        'ses' => [
            'transport' => 'ses',
        ],

        'postmark' => [
            'transport' => 'postmark',
            // 'message_stream_id' => env('POSTMARK_MESSAGE_STREAM_ID'),
            // 'client' => [
            //     'timeout' => 5,
            // ],
        ],

        'resend' => [
            'transport' => 'resend',
        ],

        'sendmail' => [
            'transport' => 'sendmail',
            'path' => env('MAIL_SENDMAIL_PATH', '/usr/sbin/sendmail -bs -i'),
        ],

        'log' => [
            'transport' => 'log',
            'channel' => env('MAIL_LOG_CHANNEL'),
        ],

        'array' => [
            'transport' => 'array',
        ],

        'failover' => [
            'transport' => 'failover',
            'mailers' => [
                'smtp',
                'log',
            ],
            'retry_after' => 60,
        ],

        'roundrobin' => [
            'transport' => 'roundrobin',
            'mailers' => [
                'ses',
                'postmark',
            ],
            'retry_after' => 60,
        ],

    ],

    /*
    |--------------------------------------------------------------------------
    | Global "From" Address
    |--------------------------------------------------------------------------
    |
    | You may wish for all emails sent by your application to be sent from
    | the same address. Here you may specify a name and address that is
    | used globally for all emails that are sent by your application.
    |
    */

    'from' => [
        'address' => env('MAIL_FROM_ADDRESS', 'hello@example.com'),
        'name' => env('MAIL_FROM_NAME', env('APP_NAME', 'Laravel')),
    ],

];
</file>

<file path="config/permission.php">
<?php

use Spatie\Permission\DefaultTeamResolver;
use Spatie\Permission\Models\Permission;
use Spatie\Permission\Models\Role;

return [

    'models' => [

        /*
         * When using the "HasPermissions" trait from this package, we need to know which
         * Eloquent model should be used to retrieve your permissions. Of course, it
         * is often just the "Permission" model but you may use whatever you like.
         *
         * The model you want to use as a Permission model needs to implement the
         * `Spatie\Permission\Contracts\Permission` contract.
         */

        'permission' => Permission::class,

        /*
         * When using the "HasRoles" trait from this package, we need to know which
         * Eloquent model should be used to retrieve your roles. Of course, it
         * is often just the "Role" model but you may use whatever you like.
         *
         * The model you want to use as a Role model needs to implement the
         * `Spatie\Permission\Contracts\Role` contract.
         */

        'role' => Role::class,

        /*
         * When using the "Teams" feature from this package, we need to know which
         * Eloquent model should be used to retrieve your teams. Of course, it
         * is often just the "Team" model but you may use whatever you like.
         */
        'team' => null,

        /*
         * When using the "HasModels" trait and passing raw IDs to syncModels,
         * attachModels, or detachModels, this model class will be used to
         * resolve those IDs. If null, defaults to the guard's model.
         */
        'default_model' => null,
    ],

    'table_names' => [

        /*
         * When using the "HasRoles" trait from this package, we need to know which
         * table should be used to retrieve your roles. We have chosen a basic
         * default value but you may easily change it to any table you like.
         */

        'roles' => 'roles',

        /*
         * When using the "HasPermissions" trait from this package, we need to know which
         * table should be used to retrieve your permissions. We have chosen a basic
         * default value but you may easily change it to any table you like.
         */

        'permissions' => 'permissions',

        /*
         * When using the "HasPermissions" trait from this package, we need to know which
         * table should be used to retrieve your models permissions. We have chosen a
         * basic default value but you may easily change it to any table you like.
         */

        'model_has_permissions' => 'model_has_permissions',

        /*
         * When using the "HasRoles" trait from this package, we need to know which
         * table should be used to retrieve your models roles. We have chosen a
         * basic default value but you may easily change it to any table you like.
         */

        'model_has_roles' => 'model_has_roles',

        /*
         * When using the "HasRoles" trait from this package, we need to know which
         * table should be used to retrieve your roles permissions. We have chosen a
         * basic default value but you may easily change it to any table you like.
         */

        'role_has_permissions' => 'role_has_permissions',
    ],

    'column_names' => [
        /*
         * Change this if you want to name the related pivots other than defaults
         */
        'role_pivot_key' => null, // default 'role_id',
        'permission_pivot_key' => null, // default 'permission_id',

        /*
         * Change this if you want to name the related model primary key other than
         * `model_id`.
         *
         * For example, this would be nice if your primary keys are all UUIDs. In
         * that case, name this `model_uuid`.
         */

        'model_morph_key' => 'model_id',

        /*
         * Change this if you want to use the teams feature and your related model's
         * foreign key is other than `team_id`.
         */

        'team_foreign_key' => 'team_id',
    ],

    /*
     * When set to true, the method for checking permissions will be registered on the gate.
     * Set this to false if you want to implement custom logic for checking permissions.
     */

    'register_permission_check_method' => true,

    /*
     * When set to true, Laravel\Octane\Events\OperationTerminated event listener will be registered
     * this will refresh permissions on every TickTerminated, TaskTerminated and RequestTerminated
     * NOTE: This should not be needed in most cases, but an Octane/Vapor combination benefited from it.
     */
    'register_octane_reset_listener' => false,

    /*
     * Events will fire when a role or permission is assigned/unassigned:
     * \Spatie\Permission\Events\RoleAttachedEvent
     * \Spatie\Permission\Events\RoleDetachedEvent
     * \Spatie\Permission\Events\PermissionAttachedEvent
     * \Spatie\Permission\Events\PermissionDetachedEvent
     *
     * To enable, set to true, and then create listeners to watch these events.
     */
    'events_enabled' => false,

    /*
     * Teams Feature.
     * When set to true the package implements teams using the 'team_foreign_key'.
     * If you want the migrations to register the 'team_foreign_key', you must
     * set this to true before doing the migration.
     * If you already did the migration then you must make a new migration to also
     * add 'team_foreign_key' to 'roles', 'model_has_roles', and 'model_has_permissions'
     * (view the latest version of this package's migration file)
     */

    'teams' => false,

    /*
     * The class to use to resolve the permissions team id
     */
    'team_resolver' => DefaultTeamResolver::class,

    /*
     * Passport Client Credentials Grant
     * When set to true the package will use Passports Client to check permissions
     */

    'use_passport_client_credentials' => false,

    /*
     * When set to true, the required permission names are added to exception messages.
     * This could be considered an information leak in some contexts, so the default
     * setting is false here for optimum safety.
     */

    'display_permission_in_exception' => false,

    /*
     * When set to true, the required role names are added to exception messages.
     * This could be considered an information leak in some contexts, so the default
     * setting is false here for optimum safety.
     */

    'display_role_in_exception' => false,

    /*
     * By default wildcard permission lookups are disabled.
     * See documentation to understand supported syntax.
     */

    'enable_wildcard_permission' => false,

    /*
     * The class to use for interpreting wildcard permissions.
     * If you need to modify delimiters, override the class and specify its name here.
     */
    // 'wildcard_permission' => Spatie\Permission\WildcardPermission::class,

    /* Cache-specific settings */

    'cache' => [

        /*
         * By default all permissions are cached for 24 hours to speed up performance.
         * When permissions or roles are updated the cache is flushed automatically.
         */

        'expiration_time' => DateInterval::createFromDateString('24 hours'),

        /*
         * The cache key used to store all permissions.
         */

        'key' => 'spatie.permission.cache',

        /*
         * You may optionally indicate a specific cache driver to use for permission and
         * role caching using any of the `store` drivers listed in the cache.php config
         * file. Using 'default' here means to use the `default` set in cache.php.
         */

        'store' => 'default',
    ],
];
</file>

<file path="config/queue.php">
<?php

return [

    /*
    |--------------------------------------------------------------------------
    | Default Queue Connection Name
    |--------------------------------------------------------------------------
    |
    | Laravel's queue supports a variety of backends via a single, unified
    | API, giving you convenient access to each backend using identical
    | syntax for each. The default queue connection is defined below.
    |
    */

    'default' => env('QUEUE_CONNECTION', 'database'),

    /*
    |--------------------------------------------------------------------------
    | Queue Connections
    |--------------------------------------------------------------------------
    |
    | Here you may configure the connection options for every queue backend
    | used by your application. An example configuration is provided for
    | each backend supported by Laravel. You're also free to add more.
    |
    | Drivers: "sync", "database", "beanstalkd", "sqs", "redis",
    |          "deferred", "background", "failover", "null"
    |
    */

    'connections' => [

        'sync' => [
            'driver' => 'sync',
        ],

        'database' => [
            'driver' => 'database',
            'connection' => env('DB_QUEUE_CONNECTION'),
            'table' => env('DB_QUEUE_TABLE', 'jobs'),
            'queue' => env('DB_QUEUE', 'default'),
            'retry_after' => (int) env('DB_QUEUE_RETRY_AFTER', 90),
            'after_commit' => false,
        ],

        'beanstalkd' => [
            'driver' => 'beanstalkd',
            'host' => env('BEANSTALKD_QUEUE_HOST', 'localhost'),
            'queue' => env('BEANSTALKD_QUEUE', 'default'),
            'retry_after' => (int) env('BEANSTALKD_QUEUE_RETRY_AFTER', 90),
            'block_for' => 0,
            'after_commit' => false,
        ],

        'sqs' => [
            'driver' => 'sqs',
            'key' => env('AWS_ACCESS_KEY_ID'),
            'secret' => env('AWS_SECRET_ACCESS_KEY'),
            'prefix' => env('SQS_PREFIX', 'https://sqs.us-east-1.amazonaws.com/your-account-id'),
            'queue' => env('SQS_QUEUE', 'default'),
            'suffix' => env('SQS_SUFFIX'),
            'region' => env('AWS_DEFAULT_REGION', 'us-east-1'),
            'after_commit' => false,
        ],

        'redis' => [
            'driver' => 'redis',
            'connection' => env('REDIS_QUEUE_CONNECTION', 'default'),
            'queue' => env('REDIS_QUEUE', 'default'),
            'retry_after' => (int) env('REDIS_QUEUE_RETRY_AFTER', 90),
            'block_for' => null,
            'after_commit' => false,
        ],

        'deferred' => [
            'driver' => 'deferred',
        ],

        'background' => [
            'driver' => 'background',
        ],

        'failover' => [
            'driver' => 'failover',
            'connections' => [
                'database',
                'deferred',
            ],
        ],

    ],

    /*
    |--------------------------------------------------------------------------
    | Job Batching
    |--------------------------------------------------------------------------
    |
    | The following options configure the database and table that store job
    | batching information. These options can be updated to any database
    | connection and table which has been defined by your application.
    |
    */

    'batching' => [
        'database' => env('DB_CONNECTION', 'sqlite'),
        'table' => 'job_batches',
    ],

    /*
    |--------------------------------------------------------------------------
    | Failed Queue Jobs
    |--------------------------------------------------------------------------
    |
    | These options configure the behavior of failed queue job logging so you
    | can control how and where failed jobs are stored. Laravel ships with
    | support for storing failed jobs in a simple file or in a database.
    |
    | Supported drivers: "database-uuids", "dynamodb", "file", "null"
    |
    */

    'failed' => [
        'driver' => env('QUEUE_FAILED_DRIVER', 'database-uuids'),
        'database' => env('DB_CONNECTION', 'sqlite'),
        'table' => 'failed_jobs',
    ],

];
</file>

<file path="config/sanctum.php">
<?php

use Illuminate\Cookie\Middleware\EncryptCookies;
use Illuminate\Foundation\Http\Middleware\ValidateCsrfToken;
use Laravel\Sanctum\Http\Middleware\AuthenticateSession;
use Laravel\Sanctum\Sanctum;

return [

    /*
    |--------------------------------------------------------------------------
    | Stateful Domains
    |--------------------------------------------------------------------------
    |
    | Requests from the following domains / hosts will receive stateful API
    | authentication cookies. Typically, these should include your local
    | and production domains which access your API via a frontend SPA.
    |
    */

    'stateful' => explode(',', env('SANCTUM_STATEFUL_DOMAINS', sprintf(
        '%s%s',
        'localhost,localhost:3000,127.0.0.1,127.0.0.1:8000,::1',
        Sanctum::currentApplicationUrlWithPort(),
        // Sanctum::currentRequestHost(),
    ))),

    /*
    |--------------------------------------------------------------------------
    | Sanctum Guards
    |--------------------------------------------------------------------------
    |
    | This array contains the authentication guards that will be checked when
    | Sanctum is trying to authenticate a request. If none of these guards
    | are able to authenticate the request, Sanctum will use the bearer
    | token that's present on an incoming request for authentication.
    |
    */

    'guard' => ['web'],

    /*
    |--------------------------------------------------------------------------
    | Expiration Minutes
    |--------------------------------------------------------------------------
    |
    | This value controls the number of minutes until an issued token will be
    | considered expired. This will override any values set in the token's
    | "expires_at" attribute, but first-party sessions are not affected.
    |
    */

    'expiration' => null,

    /*
    |--------------------------------------------------------------------------
    | Token Prefix
    |--------------------------------------------------------------------------
    |
    | Sanctum can prefix new tokens in order to take advantage of numerous
    | security scanning initiatives maintained by open source platforms
    | that notify developers if they commit tokens into repositories.
    |
    | See: https://docs.github.com/en/code-security/secret-scanning/about-secret-scanning
    |
    */

    'token_prefix' => env('SANCTUM_TOKEN_PREFIX', ''),

    /*
    |--------------------------------------------------------------------------
    | Sanctum Middleware
    |--------------------------------------------------------------------------
    |
    | When authenticating your first-party SPA with Sanctum you may need to
    | customize some of the middleware Sanctum uses while processing the
    | request. You may change the middleware listed below as required.
    |
    */

    'middleware' => [
        'authenticate_session' => AuthenticateSession::class,
        'encrypt_cookies' => EncryptCookies::class,
        'validate_csrf_token' => ValidateCsrfToken::class,
    ],

];
</file>

<file path="config/session.php">
<?php

use Illuminate\Support\Str;

return [

    /*
    |--------------------------------------------------------------------------
    | Default Session Driver
    |--------------------------------------------------------------------------
    |
    | This option determines the default session driver that is utilized for
    | incoming requests. Laravel supports a variety of storage options to
    | persist session data. Database storage is a great default choice.
    |
    | Supported: "file", "cookie", "database", "memcached",
    |            "redis", "dynamodb", "array"
    |
    */

    'driver' => env('SESSION_DRIVER', 'database'),

    /*
    |--------------------------------------------------------------------------
    | Session Lifetime
    |--------------------------------------------------------------------------
    |
    | Here you may specify the number of minutes that you wish the session
    | to be allowed to remain idle before it expires. If you want them
    | to expire immediately when the browser is closed then you may
    | indicate that via the expire_on_close configuration option.
    |
    */

    'lifetime' => (int) env('SESSION_LIFETIME', 120),

    'expire_on_close' => env('SESSION_EXPIRE_ON_CLOSE', false),

    /*
    |--------------------------------------------------------------------------
    | Session Encryption
    |--------------------------------------------------------------------------
    |
    | This option allows you to easily specify that all of your session data
    | should be encrypted before it's stored. All encryption is performed
    | automatically by Laravel and you may use the session like normal.
    |
    */

    'encrypt' => env('SESSION_ENCRYPT', false),

    /*
    |--------------------------------------------------------------------------
    | Session File Location
    |--------------------------------------------------------------------------
    |
    | When utilizing the "file" session driver, the session files are placed
    | on disk. The default storage location is defined here; however, you
    | are free to provide another location where they should be stored.
    |
    */

    'files' => storage_path('framework/sessions'),

    /*
    |--------------------------------------------------------------------------
    | Session Database Connection
    |--------------------------------------------------------------------------
    |
    | When using the "database" or "redis" session drivers, you may specify a
    | connection that should be used to manage these sessions. This should
    | correspond to a connection in your database configuration options.
    |
    */

    'connection' => env('SESSION_CONNECTION'),

    /*
    |--------------------------------------------------------------------------
    | Session Database Table
    |--------------------------------------------------------------------------
    |
    | When using the "database" session driver, you may specify the table to
    | be used to store sessions. Of course, a sensible default is defined
    | for you; however, you're welcome to change this to another table.
    |
    */

    'table' => env('SESSION_TABLE', 'sessions'),

    /*
    |--------------------------------------------------------------------------
    | Session Cache Store
    |--------------------------------------------------------------------------
    |
    | When using one of the framework's cache driven session backends, you may
    | define the cache store which should be used to store the session data
    | between requests. This must match one of your defined cache stores.
    |
    | Affects: "dynamodb", "memcached", "redis"
    |
    */

    'store' => env('SESSION_STORE'),

    /*
    |--------------------------------------------------------------------------
    | Session Sweeping Lottery
    |--------------------------------------------------------------------------
    |
    | Some session drivers must manually sweep their storage location to get
    | rid of old sessions from storage. Here are the chances that it will
    | happen on a given request. By default, the odds are 2 out of 100.
    |
    */

    'lottery' => [2, 100],

    /*
    |--------------------------------------------------------------------------
    | Session Cookie Name
    |--------------------------------------------------------------------------
    |
    | Here you may change the name of the session cookie that is created by
    | the framework. Typically, you should not need to change this value
    | since doing so does not grant a meaningful security improvement.
    |
    */

    'cookie' => env(
        'SESSION_COOKIE',
        Str::slug((string) env('APP_NAME', 'laravel')).'-session',
    ),

    /*
    |--------------------------------------------------------------------------
    | Session Cookie Path
    |--------------------------------------------------------------------------
    |
    | The session cookie path determines the path for which the cookie will
    | be regarded as available. Typically, this will be the root path of
    | your application, but you're free to change this when necessary.
    |
    */

    'path' => env('SESSION_PATH', '/'),

    /*
    |--------------------------------------------------------------------------
    | Session Cookie Domain
    |--------------------------------------------------------------------------
    |
    | This value determines the domain and subdomains the session cookie is
    | available to. By default, the cookie will be available to the root
    | domain without subdomains. Typically, this shouldn't be changed.
    |
    */

    'domain' => env('SESSION_DOMAIN'),

    /*
    |--------------------------------------------------------------------------
    | HTTPS Only Cookies
    |--------------------------------------------------------------------------
    |
    | By setting this option to true, session cookies will only be sent back
    | to the server if the browser has a HTTPS connection. This will keep
    | the cookie from being sent to you when it can't be done securely.
    |
    */

    'secure' => env('SESSION_SECURE_COOKIE'),

    /*
    |--------------------------------------------------------------------------
    | HTTP Access Only
    |--------------------------------------------------------------------------
    |
    | Setting this value to true will prevent JavaScript from accessing the
    | value of the cookie and the cookie will only be accessible through
    | the HTTP protocol. It's unlikely you should disable this option.
    |
    */

    'http_only' => env('SESSION_HTTP_ONLY', true),

    /*
    |--------------------------------------------------------------------------
    | Same-Site Cookies
    |--------------------------------------------------------------------------
    |
    | This option determines how your cookies behave when cross-site requests
    | take place, and can be used to mitigate CSRF attacks. By default, we
    | will set this value to "lax" to permit secure cross-site requests.
    |
    | See: https://developer.mozilla.org/en-US/docs/Web/HTTP/Headers/Set-Cookie#samesitesamesite-value
    |
    | Supported: "lax", "strict", "none", null
    |
    */

    'same_site' => env('SESSION_SAME_SITE', 'lax'),

    /*
    |--------------------------------------------------------------------------
    | Partitioned Cookies
    |--------------------------------------------------------------------------
    |
    | Setting this value to true will tie the cookie to the top-level site for
    | a cross-site context. Partitioned cookies are accepted by the browser
    | when flagged "secure" and the Same-Site attribute is set to "none".
    |
    */

    'partitioned' => env('SESSION_PARTITIONED_COOKIE', false),

    /*
    |--------------------------------------------------------------------------
    | Session Serialization
    |--------------------------------------------------------------------------
    |
    | This value controls the serialization strategy for session data, which
    | is JSON by default. Setting this to "php" allows the storage of PHP
    | objects in the session but can make an application vulnerable to
    | "gadget chain" serialization attacks if the APP_KEY is leaked.
    |
    | Supported: "json", "php"
    |
    */

    'serialization' => 'json',

];
</file>

<file path="database/.gitignore">
*.sqlite*
</file>

<file path="database/factories/UserFactory.php">
<?php

namespace Database\Factories;

use App\Models\User;
use Illuminate\Database\Eloquent\Factories\Factory;
use Illuminate\Support\Facades\Hash;
use Illuminate\Support\Str;

/**
 * @extends Factory<User>
 */
class UserFactory extends Factory
{
    /**
     * The current password being used by the factory.
     */
    protected static ?string $password;

    /**
     * Define the model's default state.
     *
     * @return array<string, mixed>
     */
    public function definition(): array
    {
        return [
            'name' => fake()->name(),
            'email' => fake()->unique()->safeEmail(),
            'email_verified_at' => now(),
            'password' => static::$password ??= Hash::make('password'),
            'remember_token' => Str::random(10),
            'two_factor_secret' => null,
            'two_factor_recovery_codes' => null,
            'two_factor_confirmed_at' => null,
        ];
    }

    /**
     * Indicate that the model's email address should be unverified.
     */
    public function unverified(): static
    {
        return $this->state(fn (array $attributes) => [
            'email_verified_at' => null,
        ]);
    }

    /**
     * Indicate that the model has two-factor authentication configured.
     */
    public function withTwoFactor(): static
    {
        return $this->state(fn (array $attributes) => [
            'two_factor_secret' => encrypt('secret'),
            'two_factor_recovery_codes' => encrypt(json_encode(['recovery-code-1'])),
            'two_factor_confirmed_at' => now(),
        ]);
    }
}
</file>

<file path="database/migrations/0001_01_01_000001_create_cache_table.php">
<?php

use Illuminate\Database\Migrations\Migration;
use Illuminate\Database\Schema\Blueprint;
use Illuminate\Support\Facades\Schema;

return new class extends Migration
{
    /**
     * Run the migrations.
     */
    public function up(): void
    {
        Schema::create('cache', function (Blueprint $table) {
            $table->string('key')->primary();
            $table->mediumText('value');
            $table->bigInteger('expiration')->index();
        });

        Schema::create('cache_locks', function (Blueprint $table) {
            $table->string('key')->primary();
            $table->string('owner');
            $table->bigInteger('expiration')->index();
        });
    }

    /**
     * Reverse the migrations.
     */
    public function down(): void
    {
        Schema::dropIfExists('cache');
        Schema::dropIfExists('cache_locks');
    }
};
</file>

<file path="database/migrations/0001_01_01_000002_create_jobs_table.php">
<?php

use Illuminate\Database\Migrations\Migration;
use Illuminate\Database\Schema\Blueprint;
use Illuminate\Support\Facades\Schema;

return new class extends Migration
{
    /**
     * Run the migrations.
     */
    public function up(): void
    {
        Schema::create('jobs', function (Blueprint $table) {
            $table->id();
            $table->string('queue')->index();
            $table->longText('payload');
            $table->unsignedSmallInteger('attempts');
            $table->unsignedInteger('reserved_at')->nullable();
            $table->unsignedInteger('available_at');
            $table->unsignedInteger('created_at');
        });

        Schema::create('job_batches', function (Blueprint $table) {
            $table->string('id')->primary();
            $table->string('name');
            $table->integer('total_jobs');
            $table->integer('pending_jobs');
            $table->integer('failed_jobs');
            $table->longText('failed_job_ids');
            $table->mediumText('options')->nullable();
            $table->integer('cancelled_at')->nullable();
            $table->integer('created_at');
            $table->integer('finished_at')->nullable();
        });

        Schema::create('failed_jobs', function (Blueprint $table) {
            $table->id();
            $table->string('uuid')->unique();
            $table->string('connection');
            $table->string('queue');
            $table->longText('payload');
            $table->longText('exception');
            $table->timestamp('failed_at')->useCurrent();

            $table->index(['connection', 'queue', 'failed_at']);
        });
    }

    /**
     * Reverse the migrations.
     */
    public function down(): void
    {
        Schema::dropIfExists('jobs');
        Schema::dropIfExists('job_batches');
        Schema::dropIfExists('failed_jobs');
    }
};
</file>

<file path="database/migrations/2024_01_01_000000_create_passkeys_table.php">
<?php

use Illuminate\Database\Migrations\Migration;
use Illuminate\Database\Schema\Blueprint;
use Illuminate\Support\Facades\Schema;

return new class extends Migration
{
    /**
     * Run the migrations.
     */
    public function up(): void
    {
        Schema::create('passkeys', function (Blueprint $table) {
            $table->id();
            $table->foreignId('user_id')->constrained()->cascadeOnDelete();
            $table->string('name');
            $table->string('credential_id')->unique();
            $table->json('credential');
            $table->timestamp('last_used_at')->nullable();
            $table->timestamps();

            $table->index('user_id');
        });
    }

    /**
     * Reverse the migrations.
     */
    public function down(): void
    {
        Schema::dropIfExists('passkeys');
    }
};
</file>

<file path="database/migrations/2025_08_14_170933_add_two_factor_columns_to_users_table.php">
<?php

use Illuminate\Database\Migrations\Migration;
use Illuminate\Database\Schema\Blueprint;
use Illuminate\Support\Facades\Schema;

return new class extends Migration
{
    /**
     * Run the migrations.
     */
    public function up(): void
    {
        Schema::table('users', function (Blueprint $table) {
            $table->text('two_factor_secret')->after('password')->nullable();
            $table->text('two_factor_recovery_codes')->after('two_factor_secret')->nullable();
            $table->timestamp('two_factor_confirmed_at')->after('two_factor_recovery_codes')->nullable();
        });
    }

    /**
     * Reverse the migrations.
     */
    public function down(): void
    {
        Schema::table('users', function (Blueprint $table) {
            $table->dropColumn([
                'two_factor_secret',
                'two_factor_recovery_codes',
                'two_factor_confirmed_at',
            ]);
        });
    }
};
</file>

<file path="database/migrations/2026_07_11_165612_create_permission_tables.php">
<?php

use Illuminate\Database\Migrations\Migration;
use Illuminate\Database\Schema\Blueprint;
use Illuminate\Support\Facades\Schema;

return new class extends Migration
{
    /**
     * Run the migrations.
     */
    public function up(): void
    {
        $teams = config('permission.teams');
        $tableNames = config('permission.table_names');
        $columnNames = config('permission.column_names');
        $pivotRole = $columnNames['role_pivot_key'] ?? 'role_id';
        $pivotPermission = $columnNames['permission_pivot_key'] ?? 'permission_id';

        throw_if(empty($tableNames), 'Error: config/permission.php not loaded. Run [php artisan config:clear] and try again.');
        throw_if($teams && empty($columnNames['team_foreign_key'] ?? null), 'Error: team_foreign_key on config/permission.php not loaded. Run [php artisan config:clear] and try again.');

        /**
         * See `docs/prerequisites.md` for suggested lengths on 'name' and 'guard_name' if "1071 Specified key was too long" errors are encountered.
         */
        Schema::create($tableNames['permissions'], static function (Blueprint $table) {
            $table->id(); // permission id
            $table->string('name');
            $table->string('guard_name');
            $table->timestamps();

            $table->unique(['name', 'guard_name']);
        });

        /**
         * See `docs/prerequisites.md` for suggested lengths on 'name' and 'guard_name' if "1071 Specified key was too long" errors are encountered.
         */
        Schema::create($tableNames['roles'], static function (Blueprint $table) use ($teams, $columnNames) {
            $table->id(); // role id
            if ($teams || config('permission.testing')) { // permission.testing is a fix for sqlite testing
                $table->unsignedBigInteger($columnNames['team_foreign_key'])->nullable();
                $table->index($columnNames['team_foreign_key'], 'roles_team_foreign_key_index');
            }
            $table->string('name');
            $table->string('guard_name');
            $table->timestamps();
            if ($teams || config('permission.testing')) {
                $table->unique([$columnNames['team_foreign_key'], 'name', 'guard_name']);
            } else {
                $table->unique(['name', 'guard_name']);
            }
        });

        Schema::create($tableNames['model_has_permissions'], static function (Blueprint $table) use ($tableNames, $columnNames, $pivotPermission, $teams) {
            $table->unsignedBigInteger($pivotPermission);

            $table->string('model_type');
            $table->unsignedBigInteger($columnNames['model_morph_key']);
            $table->index([$columnNames['model_morph_key'], 'model_type'], 'model_has_permissions_model_id_model_type_index');

            $table->foreign($pivotPermission)
                ->references('id') // permission id
                ->on($tableNames['permissions'])
                ->cascadeOnDelete();
            if ($teams) {
                $table->unsignedBigInteger($columnNames['team_foreign_key']);
                $table->index($columnNames['team_foreign_key'], 'model_has_permissions_team_foreign_key_index');

                $table->primary([$columnNames['team_foreign_key'], $pivotPermission, $columnNames['model_morph_key'], 'model_type'],
                    'model_has_permissions_permission_model_type_primary');
            } else {
                $table->primary([$pivotPermission, $columnNames['model_morph_key'], 'model_type'],
                    'model_has_permissions_permission_model_type_primary');
            }
        });

        Schema::create($tableNames['model_has_roles'], static function (Blueprint $table) use ($tableNames, $columnNames, $pivotRole, $teams) {
            $table->unsignedBigInteger($pivotRole);

            $table->string('model_type');
            $table->unsignedBigInteger($columnNames['model_morph_key']);
            $table->index([$columnNames['model_morph_key'], 'model_type'], 'model_has_roles_model_id_model_type_index');

            $table->foreign($pivotRole)
                ->references('id') // role id
                ->on($tableNames['roles'])
                ->cascadeOnDelete();
            if ($teams) {
                $table->unsignedBigInteger($columnNames['team_foreign_key']);
                $table->index($columnNames['team_foreign_key'], 'model_has_roles_team_foreign_key_index');

                $table->primary([$columnNames['team_foreign_key'], $pivotRole, $columnNames['model_morph_key'], 'model_type'],
                    'model_has_roles_role_model_type_primary');
            } else {
                $table->primary([$pivotRole, $columnNames['model_morph_key'], 'model_type'],
                    'model_has_roles_role_model_type_primary');
            }
        });

        Schema::create($tableNames['role_has_permissions'], static function (Blueprint $table) use ($tableNames, $pivotRole, $pivotPermission) {
            $table->unsignedBigInteger($pivotPermission);
            $table->unsignedBigInteger($pivotRole);

            $table->foreign($pivotPermission)
                ->references('id') // permission id
                ->on($tableNames['permissions'])
                ->cascadeOnDelete();

            $table->foreign($pivotRole)
                ->references('id') // role id
                ->on($tableNames['roles'])
                ->cascadeOnDelete();

            $table->primary([$pivotPermission, $pivotRole], 'role_has_permissions_permission_id_role_id_primary');
        });

        app('cache')
            ->store(config('permission.cache.store') != 'default' ? config('permission.cache.store') : null)
            ->forget(config('permission.cache.key'));
    }

    /**
     * Reverse the migrations.
     */
    public function down(): void
    {
        $tableNames = config('permission.table_names');

        throw_if(empty($tableNames), 'Error: config/permission.php not found and defaults could not be merged. Please publish the package configuration before proceeding, or drop the tables manually.');

        Schema::dropIfExists($tableNames['role_has_permissions']);
        Schema::dropIfExists($tableNames['model_has_roles']);
        Schema::dropIfExists($tableNames['model_has_permissions']);
        Schema::dropIfExists($tableNames['roles']);
        Schema::dropIfExists($tableNames['permissions']);
    }
};
</file>

<file path="database/migrations/2026_07_14_140045_create_clients_table.php">
<?php

use Illuminate\Database\Migrations\Migration;
use Illuminate\Database\Schema\Blueprint;
use Illuminate\Support\Facades\Schema;

return new class extends Migration
{
    /**
     * Run the migrations.
     */
    public function up(): void
    {
        Schema::create('clients', function (Blueprint $table) {
            $table->id();
            $table->ulid('uuid')->unique();
            $table->string('name');
            $table->string('client_id')->unique();
            $table->string('client_secret')->unique();
            $table->string('website');
            $table->string('redirect_uri');
            $table->string('redirect_uri_separator', 1)->default('?');
            $table->string('webhook_uri')->nullable();
            $table->string('logo')->nullable();
            $table->boolean('status')->default(true);
            $table->json("data")->nullable();
            $table->foreignId('user_id')->constrained();
            $table->softDeletes();
            $table->timestamps();
        });
    }

    /**
     * Reverse the migrations.
     */
    public function down(): void
    {
        Schema::dropIfExists('clients');
    }
};
</file>

<file path="database/migrations/2026_07_14_140046_create_client_customers_table.php">
<?php

use Illuminate\Database\Migrations\Migration;
use Illuminate\Database\Schema\Blueprint;
use Illuminate\Support\Facades\Schema;

return new class extends Migration
{
    /**
     * Run the migrations.
     */
    public function up(): void
    {
        Schema::create('client_customers', function (Blueprint $table) {
            $table->id();
            $table->ulid('uuid')->unique();
            $table->foreignId('client_id')->constrained();
            $table->string('name')->nullable();
            $table->string('email');
            $table->string('mobile')->nullable();
            $table->softDeletes();
            $table->timestamps();
        });
    }

    /**
     * Reverse the migrations.
     */
    public function down(): void
    {
        Schema::dropIfExists('clients');
    }
};
</file>

<file path="database/migrations/2026_07_14_140056_create_pg_connections_table.php">
<?php

use Illuminate\Database\Migrations\Migration;
use Illuminate\Database\Schema\Blueprint;
use Illuminate\Support\Facades\Schema;

return new class extends Migration
{
    /**
     * Run the migrations.
     */
    public function up(): void
    {
        Schema::create('pg_connections', function (Blueprint $table) {
            $table->id();
            $table->string('name');
            $table->string('pg_class');
            $table->json("attributes");
            $table->boolean("status")->default(true);
            $table->string("type")->default("TEST")->comment("TEST, PRODUCTION");
            $table->timestamps();
        });
    }

    /**
     * Reverse the migrations.
     */
    public function down(): void
    {
        Schema::dropIfExists('pg_connections');
    }
};
</file>

<file path="database/migrations/2026_07_14_140125_create_clients_connections_table.php">
<?php

use Illuminate\Database\Migrations\Migration;
use Illuminate\Database\Schema\Blueprint;
use Illuminate\Support\Facades\Schema;

return new class extends Migration
{
    /**
     * Run the migrations.
     */
    public function up(): void
    {
        Schema::create('clients_connections', function (Blueprint $table) {
            $table->id();
            $table->foreignId('client_id')->constrained();
            $table->foreignId('pg_connection_id')->constrained();
            $table->boolean('pg_fees_recovery')->default(false);
            $table->boolean('is_recurring')->default(false);
            $table->string('type')->default('TEST')->comment("TEST, PRODUCTION");
            $table->boolean('status')->default(true)->comment("1: active, 0: inactive");
            $table->dateTime('deleted_at')->nullable()->comment("deleted at");
            $table->timestamps();
        });
    }

    /**
     * Reverse the migrations.
     */
    public function down(): void
    {
        Schema::dropIfExists('clients_connections');
    }
};
</file>

<file path="database/migrations/2026_07_15_013502_create_subscriptions_table.php">
<?php

use Illuminate\Database\Migrations\Migration;
use Illuminate\Database\Schema\Blueprint;
use Illuminate\Support\Facades\Schema;

return new class extends Migration
{
    /**
     * Run the migrations.
     */
    public function up(): void
    {
        Schema::create('subscriptions', function (Blueprint $table) {
            $table->id();
            $table->foreignId('client_id')->constrained();
            $table->foreignId('client_customer_id')->constrained();
            $table->foreignId('pg_connection_id')->constrained();
            $table->string('subscription_type')->comment('SUBSCRIPTION, RECURRING');
            $table->string('site_reference_id');
            $table->unique(['client_id', 'site_reference_id']);
            $table->string('subscription_id');
            $table->unique(['pg_connection_id', 'subscription_id']);
            $table->string('plan_id')->nullable();
            $table->dateTime('start_date_time');
            $table->dateTime('end_date_time');
            $table->string('period')->comment('DAILY, WEEKLY, MONTHLY, QUATERLY, YEARLY');
            $table->integer('interval');
            $table->integer('amount');
            $table->string('payment_method')->nullable();
            $table->string('currency')->default('INR');
            $table->string('status')->comment('CREATED, AUTHENTICATED, ACTIVE, PENDING, HALTED, CANCELLED, PAUSED, EXPIRED, COMPLETED');
            $table->dateTime('deleted_at')->nullable();
            $table->timestamps();
        });
    }

    /**
     * Reverse the migrations.
     */
    public function down(): void
    {
        Schema::dropIfExists('subscriptions');
    }
};
</file>

<file path="database/migrations/2026_07_15_013510_create_subscriptions_transactions_table.php">
<?php

use Illuminate\Database\Migrations\Migration;
use Illuminate\Database\Schema\Blueprint;
use Illuminate\Support\Facades\Schema;

return new class extends Migration
{
    /**
     * Run the migrations.
     */
    public function up(): void
    {
        Schema::create('subscriptions_transactions', function (Blueprint $table) {
            $table->id();
            $table->foreignId('subscription_id')->constrained();
            $table->string('transaction_id');
            $table->unique(['subscription_id', 'transaction_id']);
            $table->integer('amount');
            $table->string('currency')->default('INR');
            $table->string('status')->comment('PENDING, SUCCESS, FAILED');
            $table->string('payment_method');
            $table->integer('pg_fees')->default(0);
            $table->integer('pg_tax')->default(0);
            $table->json('data')->nullable();
            $table->dateTime('transaction_date_time')->comment('transaction date time');
            $table->timestamps();
        });
    }

    /**
     * Reverse the migrations.
     */
    public function down(): void
    {
        Schema::dropIfExists('subscriptions_transactions');
    }
};
</file>

<file path="database/migrations/2026_07_15_114605_create_supported_payment_gateways_table.php">
<?php

use Illuminate\Database\Migrations\Migration;
use Illuminate\Database\Schema\Blueprint;
use Illuminate\Support\Facades\Schema;

return new class extends Migration
{
    /**
     * Run the migrations.
     */
    public function up(): void
    {
        Schema::create('supported_payment_gateways', function (Blueprint $table) {
            $table->id();
            $table->string('name');
            $table->string('pg_class');
            $table->json("attributes");
            $table->boolean('status')->default(true)->comment("1: active, 0: inactive");
            $table->timestamps();
        });
    }

    /**
     * Reverse the migrations.
     */
    public function down(): void
    {
        Schema::dropIfExists('supported_payment_gateways');
    }
};
</file>

<file path="database/migrations/2026_07_17_153845_create_personal_access_tokens_table.php">
<?php

use Illuminate\Database\Migrations\Migration;
use Illuminate\Database\Schema\Blueprint;
use Illuminate\Support\Facades\Schema;

return new class extends Migration
{
    /**
     * Run the migrations.
     */
    public function up(): void
    {
        Schema::create('personal_access_tokens', function (Blueprint $table) {
            $table->id();
            $table->morphs('tokenable');
            $table->text('name');
            $table->string('token', 64)->unique();
            $table->text('abilities')->nullable();
            $table->timestamp('last_used_at')->nullable();
            $table->timestamp('expires_at')->nullable()->index();
            $table->timestamps();
        });
    }

    /**
     * Reverse the migrations.
     */
    public function down(): void
    {
        Schema::dropIfExists('personal_access_tokens');
    }
};
</file>

<file path="database/migrations/2026_07_17_160000_create_client_user_table.php">
<?php

use Illuminate\Database\Migrations\Migration;
use Illuminate\Database\Schema\Blueprint;
use Illuminate\Support\Facades\Schema;

return new class extends Migration
{
    /**
     * Run the migrations.
     */
    public function up(): void
    {
        Schema::create('client_user', function (Blueprint $table) {
            $table->id();
            $table->foreignId('client_id')->constrained()->cascadeOnDelete();
            $table->foreignId('user_id')->constrained()->cascadeOnDelete();
            $table->timestamps();

            $table->unique(['client_id', 'user_id']);
        });
    }

    /**
     * Reverse the migrations.
     */
    public function down(): void
    {
        Schema::dropIfExists('client_user');
    }
};
</file>

<file path="database/migrations/2026_07_19_141015_add_transaction_type_to_client_connections_table.php">
<?php

use Illuminate\Database\Migrations\Migration;
use Illuminate\Database\Schema\Blueprint;
use Illuminate\Support\Facades\Schema;

return new class extends Migration
{
    /**
     * Run the migrations.
     */
    public function up(): void
    {
        Schema::table('clients_connections', function (Blueprint $table) {
            $table->string('transactionType')->default('sale')->after('type')->comment('sale, donation');
        });
    }

    /**
     * Reverse the migrations.
     */
    public function down(): void
    {
        Schema::table('clients_connections', function (Blueprint $table) {
            $table->dropColumn('transactionType');
        });
    }
};
</file>

<file path="database/migrations/2026_07_19_150000_rename_transaction_type_on_clients_connections_table.php">
<?php

use Illuminate\Database\Migrations\Migration;
use Illuminate\Database\Schema\Blueprint;
use Illuminate\Support\Facades\Schema;

return new class extends Migration
{
    /**
     * Run the migrations.
     */
    public function up(): void
    {
        Schema::table('clients_connections', function (Blueprint $table) {
            $table->renameColumn('transactionType', 'transaction_type');
        });
    }

    /**
     * Reverse the migrations.
     */
    public function down(): void
    {
        Schema::table('clients_connections', function (Blueprint $table) {
            $table->renameColumn('transaction_type', 'transactionType');
        });
    }
};
</file>

<file path="database/migrations/2026_07_20_090000_create_client_api_logs_table.php">
<?php

use Illuminate\Database\Migrations\Migration;
use Illuminate\Database\Schema\Blueprint;
use Illuminate\Support\Facades\Schema;

return new class extends Migration
{
    /**
     * Run the migrations.
     */
    public function up(): void
    {
        Schema::create('client_api_logs', function (Blueprint $table) {
            $table->id();
            $table->foreignId('client_id')->nullable()->constrained()->nullOnDelete();
            $table->string('event');
            $table->json('request_data')->nullable();
            $table->json('response_data')->nullable();
            $table->string('source_ip')->nullable();
            $table->string('result');
            $table->timestamp('datetime')->useCurrent();
            $table->timestamps();
        });
    }

    /**
     * Reverse the migrations.
     */
    public function down(): void
    {
        Schema::dropIfExists('client_api_logs');
    }
};
</file>

<file path="database/migrations/2026_07_21_100000_add_request_data_to_transactions_table.php">
<?php

use Illuminate\Database\Migrations\Migration;
use Illuminate\Database\Schema\Blueprint;
use Illuminate\Support\Facades\Schema;

return new class extends Migration
{
    /**
     * Run the migrations.
     */
    public function up(): void
    {
        Schema::table('transactions', function (Blueprint $table) {
            $table->json('request_data')->nullable()->after('transaction_date_time');
        });
    }

    /**
     * Reverse the migrations.
     */
    public function down(): void
    {
        Schema::table('transactions', function (Blueprint $table) {
            $table->dropColumn('request_data');
        });
    }
};
</file>

<file path="database/seeders/RolesAndPermissionsSeeder.php">
<?php

namespace Database\Seeders;

use Illuminate\Database\Seeder;
use Spatie\Permission\Models\Permission;
use Spatie\Permission\Models\Role;
use Spatie\Permission\PermissionRegistrar;

class RolesAndPermissionsSeeder extends Seeder
{
    /**
     * Run the database seeds.
     */
    public function run(): void
    {
        app()[PermissionRegistrar::class]->forgetCachedPermissions();

        $superAdminPermissions = [
            'can_create_admins',
            'can_edit_admins',
            'can_delete_admins',
        ];

        $adminPermissions = [
            'can_create_user',
            'can_edit_user',
            'can_delete_user',
            'can_view_user',
            'can_view_all_user',

            'can_create_client',
            'can_edit_client',
            'can_delete_client',
            'can_view_client',
            'can_view_all_clients',

            'can_create_pg_connection',
            'can_edit_pg_connection',
            'can_delete_pg_connection',
            'can_view_pg_connection',
            'can_view_all_pg_connection',

            'can_create_client_pg_connection',
            'can_edit_client_pg_connection',
            'can_delete_client_pg_connection',
            'can_view_client_pg_connection',
            'can_view_all_client_pg_connection',

            'can_create_transaction',
            'can_view_transaction',
            'can_view_all_transactions',
        ];

        $userPermissions = [
            'can_view_client',
            'can_view_transaction',
        ];

        $roles = [
            'superadmin' => $superAdminPermissions,
            'admin' => $adminPermissions,
            'user' => $userPermissions,
        ];

        $superAdminPermissions = array_merge($superAdminPermissions, $adminPermissions, $userPermissions);

        foreach ($roles as $role => $permissions) {

            $role = Role::firstOrCreate([
                'name' => $role,
                'guard_name' => 'web',
            ]);

            foreach ($superAdminPermissions as $permission) {
                $permission_db = Permission::firstOrCreate(['name' => $permission]);
                $role->givePermissionTo($permission_db);
            }

        }

    }
}
</file>

<file path="resources/views/emails/transaction-status.blade.php">
<x-mail::message>
# Transaction {{ ucfirst($transactionStatus->value) }}

| | |
|---|---|
| Reference ID | {{ $paymentResponseDTO->siteReferenceId }} |
| Transaction ID | {{ $paymentResponseDTO->transactionId }} |
| Status | {{ ucfirst($transactionStatus->value) }} |
| Amount | {{ $paymentResponseDTO->amount->format() }} |
| Date | {{ $paymentResponseDTO->transactionDateTime->toDateTimeString() }} |

Thanks,<br>
{{ config('app.name') }}
</x-mail::message>
</file>

<file path="resources/views/filament/modals/client-api-logs.blade.php">
@livewire(\App\Livewire\ClientApiLogsTable::class, ['client' => $client])
</file>

<file path="resources/views/filament/pages/utils.blade.php">
<x-filament-panels::page>
    <p class="fi-section-content-ctn text-sm text-gray-500 dark:text-gray-400">
        Administrative utilities and one-off actions for testing and debugging.
    </p>
</x-filament-panels::page>
</file>

<file path="resources/views/livewire/client-api-logs-table.blade.php">
<div>
    {{ $this->table }}
</div>
</file>

<file path="routes/api.php">
<?php

use Illuminate\Http\Request;
use Illuminate\Support\Facades\Route;

/*
Route::get('/user', function (Request $request) {
    return $request->user();
})->middleware('auth:sanctum');*/
Route::prefix('v1')->group(base_path('routes/api-v1.php'));
</file>

<file path="routes/console.php">
<?php

use Illuminate\Foundation\Inspiring;
use Illuminate\Support\Facades\Artisan;

Artisan::command('inspire', function () {
    $this->comment(Inspiring::quote());
})->purpose('Display an inspiring quote');
</file>

<file path="app/Classes/PaymentGateways/PaymentGateway.php">
<?php

namespace App\Classes\PaymentGateways;

class PaymentGateway
{
}
</file>

<file path="app/Classes/PaymentGateways/PaymentGatewayFactory.php">
<?php

namespace App\Classes\PaymentGateways;

use App\Contracts\PaymentGatewayInterface;

class PaymentGatewayFactory
{
    public static function create(array $connection): PaymentGatewayInterface
    {
        return match ($connection['pg_class']) {
            'ICICI' => new ICICI($connection['attributes']),
            default => throw new \Exception('Invalid payment gateway type.'),
        };
    }
    public static function createEmpty($pg_class): PaymentGatewayInterface
    {
        return match ($pg_class) {
            'ICICI' => new ICICI([]),
            default => throw new \Exception('Invalid payment gateway type.'),
        };
    }
}
</file>

<file path="app/Concerns/PasswordValidationRules.php">
<?php

namespace App\Concerns;

use Illuminate\Contracts\Validation\Rule;
use Illuminate\Contracts\Validation\ValidationRule;
use Illuminate\Validation\Rules\Password;

trait PasswordValidationRules
{
    /**
     * Get the validation rules used to validate passwords.
     *
     * @return array<int, ValidationRule|Rule|Password|array<mixed>|string>
     */
    protected function passwordRules(): array
    {
        return ['required', 'string', Password::default(), 'confirmed'];
    }

    /**
     * Get the validation rules used to validate the current password.
     *
     * @return array<int, ValidationRule|Rule|Password|array<mixed>|string>
     */
    protected function currentPasswordRules(): array
    {
        return ['required', 'string', 'current_password'];
    }
}
</file>

<file path="app/DTO/PaymentRequestDTO.php">
<?php

namespace App\DTO;

use App\Enums\PaymentType;
use App\Enums\TransactionType;
use Devhammed\LaravelBrickMoney\Currency;
use Devhammed\LaravelBrickMoney\Money;
use Spatie\LaravelData\Data;

class PaymentRequestDTO extends Data
{
    public readonly Currency $currency;

    public readonly Money $amount;

    public readonly TransactionType $transactionType;

    public readonly array $customer;
    public readonly PaymentType $paymentType;

    protected ?int $pgConnectionId;
    public function __construct(
        public readonly string $clientDbId,
        public readonly string $clientId,
        string $currency,
        int|float|string $amount,
        public readonly string $site_reference_id,
        TransactionType|string $transactionType,
        array $customer,
        PaymentType|string $paymentType,
        public readonly array $requestData = [],
        int|null $pgConnectionId = null
    ) {
        $this->currency = Currency::of($currency);
        $this->amount = Money::of($amount, $this->currency);
        $this->transactionType = $transactionType instanceof TransactionType
            ? $transactionType
            : TransactionType::from($transactionType);

        $this->customer = $customer;

        $this->paymentType = $paymentType instanceof PaymentType
            ? $paymentType
            : PaymentType::from($paymentType);

        $this->pgConnectionId = $pgConnectionId;
    }

    public function setPgConnectionId(int $pgConnectionId):void
    {
        $this->pgConnectionId = $pgConnectionId;
    }
    public function getPgConnectionId():int|null
    {
        return $this->pgConnectionId;
    }
}
</file>

<file path="app/DTO/PaymentResponseDTO.php">
<?php

namespace App\DTO;

use App\Enums\PaymentMethod;
use App\Enums\TransactionStatus;
use Carbon\Carbon;
use Devhammed\LaravelBrickMoney\Currency;
use Devhammed\LaravelBrickMoney\Money;
use Spatie\LaravelData\Data;

class PaymentResponseDTO extends Data
{
    public function __construct(
        public readonly string $transactionDbId,
        public readonly string $siteReferenceId,
        public readonly TransactionStatus $status,
        public readonly string $transactionId,
        public readonly string $description,
        public readonly Money $amount,
        public readonly Money $pgFees,
        public readonly Money $totalAmount,
        public readonly Carbon $transactionDateTime,
        public readonly Currency $currency,
        public readonly PaymentMethod $paymentMethod,
        public readonly string $clientName,
        public readonly string $pgConnection,
        public readonly array $pgResponseRaw,
    ) {
    }
}
</file>

<file path="app/Enums/ConnectionType.php">
<?php

namespace App\Enums;

enum ConnectionType: string
{
    case TEST = "TEST";
    case PRODUCTION = "PRODUCTION";

    /**
     * @return array<string, string>
     */
    static public function labels(): array
    {
        return [
            "TEST" => self::TEST->value,
            "PRODUCTION" => self::PRODUCTION->value,
        ];
    }
}
</file>

<file path="app/Enums/PaymentMethod.php">
<?php

namespace App\Enums;

enum PaymentMethod: string
{
    case CARD = 'card';
    case UPI = 'upi';
    case NETBANKING = 'nettbanking';
    case CREDIT_CARD = 'creditcard';
    case DEBIT_CARD = 'debitcard';
    case WALLET = 'wallet';

    case UNKNOWN = 'unknown';
}
</file>

<file path="app/Filament/Resources/ClientConnections/Concerns/ValidatesClientConnection.php">
<?php

namespace App\Filament\Resources\ClientConnections\Concerns;

use App\Enums\TransactionType;
use App\Models\ClientConnection;
use App\Models\PGConnection;
use Filament\Notifications\Notification;
use Filament\Support\Exceptions\Halt;

trait ValidatesClientConnection
{
    /**
     * Guard the record from being persisted with an environment that
     * doesn't match its gateway connection, an invalid transaction type,
     * or with more active connections than the client is allowed to have.
     *
     * @param  array<string, mixed>  $data
     */
    protected function validateClientConnection(array $data, ?int $ignoreRecordId = null): void
    {
        $pgConnection = PGConnection::find($data['pg_connection_id'] ?? null);

        if ($pgConnection && $pgConnection->type->value !== $data['type']) {
            $this->failClientConnectionValidation(
                "The connection's environment ({$data['type']}) must match the payment gateway's environment ({$pgConnection->type->value})."
            );
        }

        if (TransactionType::tryFrom($data['transaction_type'] ?? '') === null) {
            $this->failClientConnectionValidation(
                'The selected transaction type is invalid.'
            );
        }

        if (! ($data['status'] ?? false)) {
            return;
        }

        $activeConnectionsCount = ClientConnection::query()
            ->where('client_id', $data['client_id'] ?? null)
            ->where('status', true)
            ->when($ignoreRecordId, fn ($query) => $query->whereKeyNot($ignoreRecordId))
            ->when($data['is_recurring'], fn ($query) => $query->where('is_recurring', $data['is_recurring']))
            ->count();

        $maxActiveConnections = 1;

        if (($activeConnectionsCount + 1) > $maxActiveConnections) {
            $this->failClientConnectionValidation(
                'This client already has an active connection. Only one active connection is allowed unless it is recurring.'
            );
        }
    }

    protected function failClientConnectionValidation(string $message): void
    {
        Notification::make()
            ->title('Invalid client connection')
            ->body($message)
            ->danger()
            ->send();

        throw new Halt;
    }
}
</file>

<file path="app/Filament/Resources/ClientConnections/Schemas/ClientConnectionsForm.php">
<?php

namespace App\Filament\Resources\ClientConnections\Schemas;

use App\Enums\ConnectionType;
use App\Enums\TransactionType;
use Filament\Forms\Components\Select;
use Filament\Forms\Components\Toggle;
use Filament\Schemas\Components\Section;
use Filament\Schemas\Schema;
use Filament\Support\Icons\Heroicon;

class ClientConnectionsForm
{
    public static function configure(Schema $schema): Schema
    {
        return $schema
            ->components([
                Section::make('Connection')
                    ->description('Link a client to a payment gateway connection.')
                    ->icon(Heroicon::OutlinedLink)
                    ->columns(2)
                    ->components([
                        Select::make('client_id')
                            ->label('Client')
                            ->relationship('client', 'name')
                            ->prefixIcon(Heroicon::OutlinedBuildingStorefront)
                            ->searchable()
                            ->preload()
                            ->required(),

                        Select::make('transaction_type')
                            ->label('Transaction Type')
                            ->options(TransactionType::labels())
                            ->default(TransactionType::SALE->value)
                            ->prefixIcon(Heroicon::OutlinedShare)
                            ->searchable()
                            ->preload()
                            ->required(),

                        Select::make('pg_connection_id')
                            ->label('Payment Gateway Connection')
                            ->relationship('pgConnection', 'name')
                            ->prefixIcon(Heroicon::OutlinedCreditCard)
                            ->searchable()
                            ->preload()
                            ->required(),
                    ]),

                Section::make('Settings')
                    ->description('How this connection behaves.')
                    ->icon(Heroicon::OutlinedAdjustmentsHorizontal)
                    ->columns(4)
                    ->components([
                        Select::make('type')
                            ->label('Environment')
                            ->prefixIcon(Heroicon::OutlinedAdjustmentsHorizontal)
                            ->options(ConnectionType::labels())
                            ->default(ConnectionType::TEST->value)
                            ->helperText('Must match the selected gateway connection\'s environment.')
                            ->columnSpan(4)
                            ->required(),

                        Toggle::make('status')
                            ->label('Active')
                            ->onIcon(Heroicon::OutlinedCheckCircle)
                            ->offIcon(Heroicon::OutlinedXCircle)
                            ->onColor('success')
                            ->offColor('danger')
                            ->helperText('Only one active connection allowed per client.')
                            ->columnSpan(1)
                            ->default(true),

                        Toggle::make('is_recurring')
                            ->label('Is Recurring')
                            //->helperText('Allows up to 2 active connections.')
                            ->columnSpan(1)
                            ->default(false),

                        Toggle::make('pg_fees_recovery')
                            ->label('Recover Gateway Fees from Client')
                            ->helperText('Passes gateway processing fees on to the client.')
                            ->columnSpan(2)
                            ->default(false),
                    ]),
            ]);
    }
}
</file>

<file path="app/Filament/Resources/ClientConnections/Tables/ClientConnectionsTable.php">
<?php

namespace App\Filament\Resources\ClientConnections\Tables;

use App\Enums\PaymentType;
use App\Enums\TransactionType;
use App\Models\ClientConnection;
use Filament\Actions\Action;
use Filament\Actions\BulkActionGroup;
use Filament\Actions\DeleteBulkAction;
use Filament\Actions\EditAction;
use Filament\Actions\ForceDeleteBulkAction;
use Filament\Actions\RestoreBulkAction;
use Filament\Actions\ViewAction;
use Filament\Support\Icons\Heroicon;
use Filament\Tables\Columns\IconColumn;
use Filament\Tables\Columns\TextColumn;
use Filament\Tables\Filters\TrashedFilter;
use Filament\Tables\Table;

class ClientConnectionsTable
{
    public static function configure(Table $table): Table
    {
        return $table
            ->columns([
                TextColumn::make('client.name')
                    ->label('Client')
                    ->searchable(),

                TextColumn::make('pgConnection.name')
                    ->label('Payment Gateway Connection')
                    ->searchable(),

                IconColumn::make('is_recurring')
                    ->label('Is Recurring')
                    ->boolean(),

                IconColumn::make('status')
                    ->label('Status')
                    ->boolean(),
            ])
            ->filters([
                TrashedFilter::make(),
            ])
            ->recordActions([
                ViewAction::make(),
                EditAction::make(),
                Action::make('testConnection')
                    ->label('Test Connection')
                    ->icon(Heroicon::OutlinedPlay)
                    ->color('success')
                    ->url(fn (ClientConnection $record): string => route('testPayment', [
                        'clientId' => $record->client->client_id,
                        'paymentType' => $record->is_recurring
                            ? PaymentType::SUBSCRIPTION->value
                            : PaymentType::ONE_TIME_PAYMENT->value,
                        'transactionType' => $record->transaction_type?->value ?? TransactionType::SALE->value,
                    ]))
                    ->openUrlInNewTab(),
            ])
            ->toolbarActions([
                BulkActionGroup::make([
                    DeleteBulkAction::make(),
                    ForceDeleteBulkAction::make(),
                    RestoreBulkAction::make(),
                ]),
            ]);
    }
}
</file>

<file path="app/Filament/Resources/Clients/Pages/CreateClients.php">
<?php

namespace App\Filament\Resources\Clients\Pages;

use App\Filament\Resources\Clients\ClientsResource;
use App\Models\Client;
use App\Models\User;
use Filament\Resources\Pages\CreateRecord;
use Illuminate\Support\Str;
use RuntimeException;

class CreateClients extends CreateRecord
{
    protected static string $resource = ClientsResource::class;

    /**
     * @param  array<string, mixed>  $data
     * @return array<string, mixed>
     */
    protected function mutateFormDataBeforeCreate(array $data): array
    {
        $data['user_id'] ??= auth()->id();
        $data['uuid'] ??= (string) Str::ulid();
        $data['client_id'] ??= Str::upper(Str::random(16));
        $data['client_secret'] ??= Str::random(40);

        return $data;
    }

    protected function afterCreate(): void
    {
        $this->getClientRecord()->users->each(
            fn (User $user) => $user->givePermissionTo('can_view_client'),
        );
    }

    protected function getClientRecord(): Client
    {
        if (! $this->record instanceof Client) {
            throw new RuntimeException('Expected record to be an instance of '.Client::class);
        }

        return $this->record;
    }
}
</file>

<file path="app/Filament/Resources/Clients/Pages/EditClients.php">
<?php

namespace App\Filament\Resources\Clients\Pages;

use App\Filament\Resources\Clients\ClientsResource;
use App\Models\Client;
use App\Models\User;
use Filament\Actions\DeleteAction;
use Filament\Actions\ForceDeleteAction;
use Filament\Actions\RestoreAction;
use Filament\Actions\ViewAction;
use Filament\Resources\Pages\EditRecord;
use RuntimeException;

class EditClients extends EditRecord
{
    protected static string $resource = ClientsResource::class;

    protected function getHeaderActions(): array
    {
        return [
            ViewAction::make(),
            DeleteAction::make(),
            ForceDeleteAction::make(),
            RestoreAction::make(),
        ];
    }

    protected function afterSave(): void
    {
        $this->getClientRecord()->users->each(
            fn (User $user) => $user->givePermissionTo('can_view_client'),
        );
    }

    protected function getClientRecord(): Client
    {
        if (! $this->record instanceof Client) {
            throw new RuntimeException('Expected record to be an instance of '.Client::class);
        }

        return $this->record;
    }
}
</file>

<file path="app/Filament/Resources/Clients/Schemas/ClientsForm.php">
<?php

namespace App\Filament\Resources\Clients\Schemas;

use App\Models\User;
use Filament\Forms\Components\KeyValue;
use Filament\Forms\Components\Select;
use Filament\Forms\Components\TextInput;
use Filament\Forms\Components\Toggle;
use Filament\Schemas\Components\Section;
use Filament\Schemas\Schema;
use Filament\Support\Icons\Heroicon;
use Illuminate\Database\Eloquent\Builder;

class ClientsForm
{
    public static function configure(Schema $schema): Schema
    {
        return $schema
            ->components([
                Section::make('Client Details')
                    ->description('The basics identifying this client.')
                    ->icon(Heroicon::OutlinedBuildingStorefront)
                    ->columns(4)
                    ->components([
                        TextInput::make('name')
                            ->prefixIcon(Heroicon::OutlinedTag)
                            ->columnSpan(3)
                            ->required()
                            ->maxLength(255),

                        Toggle::make('status')
                            ->label('Active')
                            ->onIcon(Heroicon::OutlinedCheckCircle)
                            ->offIcon(Heroicon::OutlinedXCircle)
                            ->onColor('success')
                            ->offColor('danger')
                            ->columnSpan(1)
                            ->default(true),
                    ]),

                Section::make('User Permissions')
                    ->description('Selected users are granted the "can_view_client" permission.')
                    ->icon(Heroicon::OutlinedShieldCheck)
                    ->components([
                        Select::make('users')
                            ->hiddenLabel()
                            ->relationship(
                                name: 'users',
                                titleAttribute: 'name',
                                modifyQueryUsing: static fn (Builder $query): Builder => static::usersWithUserRole($query),
                            )
                            ->multiple()
                            ->searchable()
                            ->preload(),
                    ]),

                Section::make('URLs')
                    ->description('Where clients are sent during and after authentication.')
                    ->icon(Heroicon::OutlinedGlobeAlt)
                    ->columns(4)
                    ->components([
                        TextInput::make('website')
                            ->prefixIcon(Heroicon::OutlinedGlobeAlt)
                            ->url()
                            ->columnSpan(4)
                            ->required()
                            ->maxLength(255),

                        TextInput::make('redirect_uri')
                            ->label('Redirect URI')
                            ->prefixIcon(Heroicon::OutlinedArrowUturnLeft)
                            ->url()
                            ->columnSpan(3)
                            ->required()
                            ->maxLength(255),

                        Select::make('redirect_uri_separator')
                            ->label('Query Separator')
                            ->options([
                                '?' => '?',
                                '&' => '&',
                            ])
                            ->default('?')
                            ->columnSpan(1)
                            ->required(),

                        TextInput::make('webhook_uri')
                            ->label('Webhook URI')
                            ->prefixIcon(Heroicon::OutlinedBolt)
                            ->url()
                            ->columnSpan(4)
                            ->maxLength(255),
                    ]),

                Section::make('Additional Data')
                    ->description('Optional, freeform metadata stored alongside this client.')
                    ->icon(Heroicon::OutlinedRectangleStack)
                    ->collapsible()
                    ->collapsed()
                    ->components([
                        KeyValue::make('data')
                            ->hiddenLabel()
                            ->keyLabel('Key')
                            ->valueLabel('Value')
                            ->addable()
                            ->deletable()
                            ->reorderable(),
                    ]),
            ]);
    }

    /**
     * @param  Builder<User>  $query
     * @return Builder<User>
     */
    protected static function usersWithUserRole(Builder $query): Builder
    {
        return $query->role('user');
    }
}
</file>

<file path="app/Filament/Resources/Clients/Tables/ClientsTable.php">
<?php

namespace App\Filament\Resources\Clients\Tables;

use Filament\Actions\Action;
use Filament\Actions\BulkActionGroup;
use Filament\Actions\DeleteBulkAction;
use Filament\Actions\EditAction;
use Filament\Actions\ForceDeleteBulkAction;
use Filament\Actions\RestoreBulkAction;
use Filament\Actions\ViewAction;
use Filament\Support\Icons\Heroicon;
use Filament\Tables\Columns\IconColumn;
use Filament\Tables\Columns\TextColumn;
use Filament\Tables\Filters\TrashedFilter;
use Filament\Tables\Table;

class ClientsTable
{
    public static function configure(Table $table): Table
    {
        return $table
            ->columns([
                TextColumn::make('name')
                    ->searchable(),

                TextColumn::make('client_id')
                    ->label('Client ID')
                    ->copyable()
                    ->searchable(),

                TextColumn::make('website')
                    ->url(fn (string $state): string => $state)
                    ->openUrlInNewTab(),

                IconColumn::make('status')
                    ->label('Status')
                    ->boolean(),
            ])
            ->filters([
                TrashedFilter::make(),
            ])
            ->recordActions([
                ViewAction::make(),
                EditAction::make(),
                Action::make('apiLogs')
                    ->label('API Logs')
                    ->icon(Heroicon::OutlinedDocumentText)
                    ->color('gray')
                    ->modalHeading(fn ($record): string => "API Logs - {$record->name}")
                    ->modalContent(fn ($record) => view('filament.modals.client-api-logs', ['client' => $record]))
                    ->modalSubmitAction(false)
                    ->modalCancelActionLabel('Close')
                    ->modalWidth('7xl'),
            ])
            ->toolbarActions([
                BulkActionGroup::make([
                    DeleteBulkAction::make(),
                    ForceDeleteBulkAction::make(),
                    RestoreBulkAction::make(),
                ]),
            ]);
    }
}
</file>

<file path="app/Filament/Resources/PGConnections/Schemas/PGConnectionAttributeSchema.php">
<?php

namespace App\Filament\Resources\PGConnections\Schemas;

use App\Models\SupportedPaymentGateway;
use Filament\Forms\Components\Placeholder;
use Filament\Forms\Components\Radio;
use Filament\Forms\Components\TextInput;
use Filament\Forms\Components\Toggle;
use Filament\Schemas\Components\Component;
use Illuminate\Support\Str;

class PGConnectionAttributeSchema
{
    /**
     * The "attributes" json column on supported_payment_gateways stores a
     * 'required' map of attribute-key => spec, where spec is "type" or
     * "type|extra" (extra being a default value for string/float, or a
     * comma-separated list of options for radio). e.g.:
     *   'fees_rate' => 'float'
     *   'paymentAction' => 'string|Sale'
     *   'mode' => 'radio|live,sandbox'
     *
     * @return array<string, array{type: string, extra: ?string}>
     */
    public static function requiredSpecFor(?string $pgClass): array
    {
        if (blank($pgClass)) {
            return [];
        }

        $gateway = SupportedPaymentGateway::query()
            ->where('pg_class', $pgClass)
            ->first();

        $required = $gateway?->attributes['required'] ?? [];

        $specs = [];

        foreach ($required as $key => $definition) {
            [$type, $extra] = array_pad(explode('|', (string) $definition, 2), 2, null);

            $specs[$key] = [
                'type' => $type,
                'extra' => $extra,
            ];
        }

        return $specs;
    }

    /**
     * Build the form fields for the given gateway's required attributes,
     * rendered using the field type appropriate for each attribute's datatype.
     *
     * @return array<Component>
     */
    public static function fields(?string $pgClass): array
    {
        $specs = static::requiredSpecFor($pgClass);

        if ($specs === []) {
            return [
                Placeholder::make('attributes_placeholder')
                    ->hiddenLabel()
                    ->columnSpanFull()
                    ->content('Select a payment gateway above to configure its attributes.'),
            ];
        }

        return collect($specs)
            ->map(fn (array $spec, string $key): Component => static::fieldFor($key, $spec))
            ->values()
            ->all();
    }

    /**
     * @param  array{type: string, extra: ?string}  $spec
     */
    protected static function fieldFor(string $key, array $spec): Component
    {
        $label = static::labelFor($key);
        $statePath = "attributes.{$key}";

        return match ($spec['type']) {
            'boolean' => Toggle::make($statePath)
                ->label($label)
                ->default(false),

            'url' => TextInput::make($statePath)
                ->label($label)
                ->url()
                ->required(),

            'float' => TextInput::make($statePath)
                ->label($label)
                ->numeric()
                ->default($spec['extra'])
                ->required(),

            'radio' => Radio::make($statePath)
                ->label($label)
                ->options(static::radioOptions($spec['extra']))
                ->default($spec['extra'] ? Str::of($spec['extra'])->before(',')->toString() : null)
                ->required(),

            default => TextInput::make($statePath)
                ->label($label)
                ->default($spec['extra'])
                ->required(),
        };
    }

    /**
     * Validate a submitted attributes array against the required schema for
     * the given gateway. Returns a map of "attributes.key" => error message
     * for every attribute that is missing or does not match its datatype.
     *
     * @param  array<string, mixed>  $attributes
     * @return array<string, string>
     */
    public static function validate(?string $pgClass, array $attributes): array
    {
        $errors = [];

        foreach (static::requiredSpecFor($pgClass) as $key => $spec) {
            if ($error = static::validateValue($key, $spec, $attributes[$key] ?? null)) {
                $errors["attributes.{$key}"] = $error;
            }
        }

        return $errors;
    }

    /**
     * @param  array{type: string, extra: ?string}  $spec
     */
    protected static function validateValue(string $key, array $spec, mixed $value): ?string
    {
        $label = static::labelFor($key);

        return match ($spec['type']) {
            'boolean' => is_bool($value) ? null : "{$label} must be true or false.",
            'url' => (is_string($value) && $value !== '' && Str::isUrl($value))
                ? null
                : "{$label} must be a valid URL.",
            'float' => is_numeric($value) ? null : "{$label} must be a number.",
            'radio' => (is_string($value) && array_key_exists($value, static::radioOptions($spec['extra'])))
                ? null
                : "{$label} must be one of: {$spec['extra']}.",
            default => (is_string($value) && $value !== '') ? null : "{$label} is required.",
        };
    }

    /**
     * @return array<string, string>
     */
    protected static function radioOptions(?string $extra): array
    {
        $options = collect(explode(',', (string) $extra))
            ->map(fn (string $option): string => trim($option))
            ->filter()
            ->all();

        return array_combine($options, $options);
    }

    protected static function labelFor(string $key): string
    {
        return (string) Str::of($key)->replace('_', ' ')->headline();
    }
}
</file>

<file path="app/Filament/Resources/Transactions/Pages/ViewTransactions.php">
<?php

namespace App\Filament\Resources\Transactions\Pages;

use App\Enums\TransactionStatus;
use App\Filament\Resources\Transactions\TransactionsResource;
use App\Services\TransactionService;
use Filament\Actions\Action;
use Filament\Actions\EditAction;
use Filament\Notifications\Notification;
use Filament\Resources\Pages\ViewRecord;
use Filament\Support\Icons\Heroicon;

class ViewTransactions extends ViewRecord
{
    protected static string $resource = TransactionsResource::class;

    protected function getHeaderActions(): array
    {
        return [
            Action::make('checkTransactionStatus')
                ->label('Check Transaction Status')
                ->icon(Heroicon::OutlinedArrowPath)
                ->color('warning')
                ->visible(fn (): bool => $this->record->status === TransactionStatus::PENDING)
                ->requiresConfirmation()
                ->action(function (TransactionService $transactionService): void {
                    try {
                        $transactionService->getTransactionStatus($this->record->id);

                        $this->record->refresh();

                        Notification::make()
                            ->title('Transaction status updated')
                            ->body("Current status: {$this->record->status->value}")
                            ->success()
                            ->send();
                    } catch (\Throwable $e) {
                        Notification::make()
                            ->title('Failed to check transaction status')
                            ->body($e->getMessage())
                            ->danger()
                            ->send();
                    }
                }),

            EditAction::make(),
        ];
    }
}
</file>

<file path="app/Filament/Resources/Transactions/Schemas/TransactionsInfolist.php">
<?php

namespace App\Filament\Resources\Transactions\Schemas;

use App\Enums\TransactionStatus;
use App\Models\Transaction;
use Filament\Infolists\Components\TextEntry;
use Filament\Schemas\Components\Section;
use Filament\Schemas\Schema;

class TransactionsInfolist
{
    public static function configure(Schema $schema): Schema
    {
        return $schema
            ->components([
                Section::make('Overview')
                    ->columns(3)
                    ->schema([
                        TextEntry::make('id'),
                        TextEntry::make('site_reference_id')
                            ->label('Site Reference ID')
                            ->copyable(),
                        TextEntry::make('transaction_id')
                            ->label('Transaction ID')
                            ->placeholder('-')
                            ->copyable(),
                        TextEntry::make('status')
                            ->badge()
                            ->color(fn (TransactionStatus $state): string => match ($state) {
                                TransactionStatus::SUCCESS => 'success',
                                TransactionStatus::FAILED => 'danger',
                                TransactionStatus::PENDING, TransactionStatus::PROCESSING => 'warning',
                                TransactionStatus::CANCELLED => 'gray',
                                TransactionStatus::REFUNDED => 'info',
                            }),
                        TextEntry::make('payment_method')
                            ->badge()
                            ->placeholder('-'),
                        TextEntry::make('transaction_date_time')
                            ->label('Transaction Date')
                            ->dateTime()
                            ->placeholder('-'),
                    ]),

                Section::make('Client & Customer')
                    ->columns(3)
                    ->schema([
                        TextEntry::make('client.name')
                            ->label('Client'),
                        TextEntry::make('pgConnection.name')
                            ->label('PG Connection')
                            ->placeholder('-'),
                        TextEntry::make('customer.name')
                            ->label('Customer')
                            ->placeholder('-'),
                        TextEntry::make('customer.email')
                            ->label('Customer Email')
                            ->placeholder('-'),
                        TextEntry::make('customer.mobile')
                            ->label('Customer Mobile')
                            ->placeholder('-'),
                    ]),

                Section::make('Amounts')
                    ->columns(4)
                    ->schema([
                        TextEntry::make('amount')
                            ->state(fn (Transaction $record): string => $record->amount['amount']->format(true)),
                        TextEntry::make('transaction_amount')
                            ->label('Transaction Amount')
                            ->state(fn (Transaction $record): string => $record->transaction_amount['transaction_amount']->format(true)),
                        TextEntry::make('pg_fees')
                            ->label('PG Fees')
                            ->state(fn (Transaction $record): string => $record->pg_fees['pg_fees']->format(true)),
                        TextEntry::make('total_amount')
                            ->label('Total Amount')
                            ->state(fn (Transaction $record): string => $record->total_amount['total_amount']->format(true)),
                    ]),

                Section::make('Raw Payloads')
                    ->columns(1)
                    ->collapsible()
                    ->schema([
                        TextEntry::make('request_data')
                            ->label('Request Data')
                            ->placeholder('-')
                            ->state(fn (Transaction $record): ?string => $record->request_data ? json_encode($record->request_data, JSON_PRETTY_PRINT) : null)
                            ->extraAttributes(['class' => 'font-mono text-xs whitespace-pre-wrap'])
                            ->columnSpanFull(),
                        TextEntry::make('response_data')
                            ->label('Response Data')
                            ->placeholder('-')
                            ->state(fn (Transaction $record): ?string => $record->response_data ? json_encode($record->response_data, JSON_PRETTY_PRINT) : null)
                            ->extraAttributes(['class' => 'font-mono text-xs whitespace-pre-wrap'])
                            ->columnSpanFull(),
                    ]),

                Section::make('Timestamps')
                    ->columns(2)
                    ->schema([
                        TextEntry::make('created_at')
                            ->dateTime(),
                        TextEntry::make('updated_at')
                            ->dateTime(),
                    ]),
            ]);
    }
}
</file>

<file path="app/Filament/Resources/Transactions/Tables/TransactionsTable.php">
<?php

namespace App\Filament\Resources\Transactions\Tables;

use App\Enums\TransactionStatus;
use App\Models\Transaction;
use Filament\Actions\BulkActionGroup;
use Filament\Actions\DeleteBulkAction;
use Filament\Actions\EditAction;
use Filament\Actions\ViewAction;
use Filament\Tables\Columns\TextColumn;
use Filament\Tables\Table;

class TransactionsTable
{
    public static function configure(Table $table): Table
    {
        return $table
            ->columns([
                TextColumn::make('id')
                    ->sortable(),

                TextColumn::make('client.name')
                    ->label('Client')
                    ->searchable()
                    ->sortable(),

                TextColumn::make('customer.name')
                    ->label('Customer')
                    ->searchable()
                    ->sortable(),

                TextColumn::make('site_reference_id')
                    ->label('Site Reference ID')
                    ->searchable()
                    ->sortable(),

                TextColumn::make('status')
                    ->badge()
                    ->color(fn (TransactionStatus $state): string => match ($state) {
                        TransactionStatus::SUCCESS => 'success',
                        TransactionStatus::FAILED => 'danger',
                        TransactionStatus::PENDING, TransactionStatus::PROCESSING => 'warning',
                        TransactionStatus::CANCELLED => 'gray',
                        TransactionStatus::REFUNDED => 'info',
                    })
                    ->searchable()
                    ->sortable(),

                TextColumn::make('amount')
                    ->state(fn (Transaction $record): string => $record->amount['amount']->format(true))
                    ->sortable(),

                TextColumn::make('total_amount')
                    ->label('Total Amount')
                    ->state(fn (Transaction $record): string => $record->total_amount['total_amount']->format(true))
                    ->sortable(),

                TextColumn::make('created_at')
                    ->label('Date Created')
                    ->dateTime()
                    ->sortable(),
            ])
            ->filters([
                //
            ])
            ->recordActions([
                ViewAction::make(),
                EditAction::make(),
            ])
            ->toolbarActions([
                BulkActionGroup::make([
                    DeleteBulkAction::make(),
                ]),
            ]);
    }
}
</file>

<file path="app/Filament/Resources/Users/Tables/UsersTable.php">
<?php

namespace App\Filament\Resources\Users\Tables;

use App\Models\User;
use Filament\Actions\Action;
use Filament\Actions\BulkActionGroup;
use Filament\Actions\DeleteBulkAction;
use Filament\Actions\EditAction;
use Filament\Actions\ForceDeleteBulkAction;
use Filament\Actions\RestoreBulkAction;
use Filament\Forms\Components\Select;
use Filament\Notifications\Notification;
use Filament\Tables\Columns\TextColumn;
use Filament\Tables\Filters\TrashedFilter;
use Filament\Tables\Table;

class UsersTable
{
    public static function configure(Table $table): Table
    {
        return $table
            ->columns([
                TextColumn::make('name'),
                TextColumn::make('roles.name')
                    ->label('Role'),
            ])
            ->filters([
                TrashedFilter::make(),
            ])
            ->recordActions([
                EditAction::make(),
                Action::make('changeRole')
                    ->label('Role')
                    ->link()
                    ->visible(fn (User $record): bool => $record->hasRole('superadmin'))
                    ->fillForm(fn (User $record): array => [
                        'role' => $record->roles()->value('name'),
                    ])
                    ->schema([
                        Select::make('role')
                            ->label('Role')
                            ->options([
                                'admin' => 'Admin',
                                'user' => 'User',
                            ])
                            ->required(),
                    ])
                    ->action(function (array $data, User $record): void {
                        if (! $record->fresh()->hasRole('superadmin')) {
                            Notification::make()
                                ->title('Only superadmin users can have their role changed here.')
                                ->danger()
                                ->send();

                            return;
                        }

                        $record->syncRoles([$data['role']]);

                        Notification::make()
                            ->title('Role updated.')
                            ->success()
                            ->send();
                    }),
            ])
            ->toolbarActions([
                BulkActionGroup::make([
                    DeleteBulkAction::make(),
                    ForceDeleteBulkAction::make(),
                    RestoreBulkAction::make(),
                ]),
            ]);
    }
}
</file>

<file path="app/Filament/Widgets/DashboardStatsWidget.php">
<?php

namespace App\Filament\Widgets;

use App\Enums\TransactionStatus;
use App\Models\Client;
use App\Models\Transaction;
use App\Models\User;
use Carbon\CarbonInterface;
use Devhammed\LaravelBrickMoney\Money;
use Filament\Support\Icons\Heroicon;
use Filament\Widgets\StatsOverviewWidget;
use Filament\Widgets\StatsOverviewWidget\Stat;
use Illuminate\Database\Eloquent\Builder;
use Illuminate\Database\Eloquent\Collection;

class DashboardStatsWidget extends StatsOverviewWidget
{
    protected function getStats(): array
    {
        $user = auth()->user();

        return [
            Stat::make('Clients', $this->clientsCount($user))
                ->icon(Heroicon::OutlinedBuildingStorefront)
                ->color('primary'),

            Stat::make('Total Collected Today', $this->collectedAmount($user, now()->startOfDay(), now()->endOfDay()))
                ->icon(Heroicon::OutlinedBanknotes)
                ->color('success'),

            Stat::make('Total Collected This Month', $this->collectedAmount($user, now()->startOfMonth(), now()->endOfMonth()))
                ->icon(Heroicon::OutlinedCalendarDays)
                ->color('success'),
        ];
    }

    protected function clientsCount(User $user): int
    {
        if ($user->can('can_view_all_clients')) {
            return Client::count();
        }

        if ($user->can('can_view_client')) {
            return Client::whereHas('users', fn (Builder $query) => $query->whereKey($user->id))->count();
        }

        return 0;
    }

    protected function collectedAmount(User $user, CarbonInterface $from, CarbonInterface $to): string
    {
        $query = Transaction::query()
            ->where('status', TransactionStatus::SUCCESS->value)
            ->whereBetween('transaction_date_time', [$from, $to]);

        $this->scopeToPermittedClients($query, $user);

        $totals = $query
            ->selectRaw('currency, sum(amount) as total')
            ->groupBy('currency')
            ->get();

        return $this->formatTotals($totals);
    }

    /**
     * @param  Builder<Transaction>  $query
     */
    protected function scopeToPermittedClients(Builder $query, User $user): void
    {
        if ($user->can('can_view_all_transactions')) {
            return;
        }

        if ($user->can('can_view_client')) {
            $query->whereHas('client.users', fn (Builder $query) => $query->whereKey($user->id));

            return;
        }

        $query->whereRaw('1 = 0');
    }

    /**
     * @param  Collection<int, Transaction>  $totals
     */
    protected function formatTotals(Collection $totals): string
    {
        if ($totals->isEmpty()) {
            return (string) Money::ofMinor(0, 'INR');
        }

        return $totals
            ->map(fn (Transaction $row): string => (string) Money::ofMinor(
                (int) $row->getAttribute('total'),
                $row->currency,
            ))
            ->implode(' + ');
    }
}
</file>

<file path="app/Filament/Widgets/RecentTransactionsWidget.php">
<?php

namespace App\Filament\Widgets;

use App\Enums\TransactionStatus;
use App\Models\Transaction;
use App\Models\User;
use Devhammed\LaravelBrickMoney\Money;
use Filament\Tables\Columns\TextColumn;
use Filament\Tables\Table;
use Filament\Widgets\TableWidget;
use Illuminate\Database\Eloquent\Builder;

class RecentTransactionsWidget extends TableWidget
{
    protected static ?string $heading = 'Recent Transactions';

    protected int|string|array $columnSpan = 'full';

    public function table(Table $table): Table
    {
        return $table
            ->query($this->getTransactionsQuery())
            ->paginated(false)
            ->columns([
                TextColumn::make('client.name')
                    ->label('Client'),

                TextColumn::make('transaction_id')
                    ->label('Transaction ID')
                    ->searchable(),

                TextColumn::make('amount')
                    ->label('Amount')
                    ->formatStateUsing(fn (Transaction $record): string => (string) Money::ofMinor(
                        $record->getRawOriginal('amount'),
                        $record->getRawOriginal('currency'),
                    )),

                TextColumn::make('status')
                    ->badge()
                    ->color(fn (TransactionStatus $state): string => match ($state) {
                        TransactionStatus::SUCCESS => 'success',
                        TransactionStatus::FAILED, TransactionStatus::CANCELLED => 'danger',
                        TransactionStatus::PENDING, TransactionStatus::PROCESSING => 'warning',
                        default => 'gray',
                    }),

                TextColumn::make('transaction_date_time')
                    ->label('Date')
                    ->dateTime(),
            ]);
    }

    /**
     * @return Builder<Transaction>
     */
    protected function getTransactionsQuery(): Builder
    {
        $user = auth()->user();

        $query = Transaction::query()
            ->with('client:id,name')
            ->latest('transaction_date_time')
            ->limit(10);

        $this->scopeToPermittedClients($query, $user);

        return $query;
    }

    /**
     * @param  Builder<Transaction>  $query
     */
    protected function scopeToPermittedClients(Builder $query, User $user): void
    {
        if ($user->can('can_view_all_transactions')) {
            return;
        }

        if ($user->can('can_view_client')) {
            $query->whereHas('client.users', fn (Builder $query) => $query->whereKey($user->id));

            return;
        }

        $query->whereRaw('1 = 0');
    }
}
</file>

<file path="app/Models/Client.php">
<?php

namespace App\Models;

use Illuminate\Database\Eloquent\Model;
use Illuminate\Database\Eloquent\Relations\BelongsTo;
use Illuminate\Database\Eloquent\Relations\BelongsToMany;
use Illuminate\Database\Eloquent\Relations\HasMany;
use Illuminate\Database\Eloquent\Relations\HasManyThrough;
use Illuminate\Database\Eloquent\Relations\HasOne;
use Illuminate\Database\Eloquent\SoftDeletes;

#Use
class Client extends Model
{
    use SoftDeletes;
    protected $casts = [
        'data' => 'array',
        'status' => 'boolean',
    ];

    /**
     * @return BelongsTo<User, $this>
     */
    public function user(): BelongsTo
    {
        return $this->belongsTo(User::class);
    }

    /**
     * @return BelongsToMany<User, $this>
     */
    public function users(): BelongsToMany
    {
        return $this->belongsToMany(User::class);
    }

    /**
     * @return HasMany<Transaction, $this>
     */
    public function transactions(): HasMany
    {
        return $this->hasMany(Transaction::class);
    }

    /**
     * @return HasMany<Subscription, $this>
     */
    public function subscriptions(): HasMany
    {
        return $this->hasMany(Subscription::class);
    }

    /**
     * @return HasManyThrough<SubscriptionTransaction, Subscription, $this>
     */
    public function subscriptionTransactions(): HasManyThrough
    {
        return $this->hasManyThrough(SubscriptionTransaction::class, Subscription::class);
    }

    /**
     * @return HasOne<ClientConnection, $this>
     */
    public function connections(): HasOne
    {
        return $this->hasOne(ClientConnection::class);
    }

    /**
     * @return HasOne<ClientConnection, $this>
     */
    public function oneTimePGConnection(): HasOne
    {
        return $this->hasOne(ClientConnection::class)
            ->where("is_recurring", "=",false);
    }

    /**
     * @return HasMany<ClientCustomer, $this>
     */
    public function customers(): HasMany
    {
        return $this->hasMany(ClientCustomer::class);
    }
}
</file>

<file path="app/Models/ClientCustomer.php">
<?php

namespace App\Models;

use Illuminate\Database\Eloquent\Model;
use Illuminate\Database\Eloquent\Relations\BelongsTo;
use Illuminate\Database\Eloquent\Relations\HasMany;

class ClientCustomer extends Model
{
    /**
     * @return BelongsTo<Client, $this>
     */
    public function client(): BelongsTo
    {
        return $this->belongsTo(Client::class);
    }

    /**
     * @return HasMany<Transaction, $this>
     */
    public function transactions(): HasMany
    {
        return $this->hasMany(Transaction::class);
    }
}
</file>

<file path="app/Models/PGConnection.php">
<?php

namespace App\Models;

use App\Enums\ConnectionType;
use Illuminate\Database\Eloquent\Model;
use Illuminate\Database\Eloquent\Relations\HasMany;

class PGConnection extends Model
{
    protected $table = 'pg_connections';
    protected $casts = [
        'attributes' => 'array',
        'status' => 'boolean',
        'type' => ConnectionType::class,
    ];

    /**
     * @return HasMany<Transaction, $this>
     */
    public function transactions(): HasMany
    {
        return $this->hasMany(Transaction::class);
    }

    /**
     * @return HasMany<ClientConnection, $this>
     */
    public function clientConnections(): HasMany
    {
        return $this->hasMany(ClientConnection::class, 'pg_connection_id');
    }

}
</file>

<file path="app/Models/Subscription.php">
<?php

namespace App\Models;

use App\Enums\PaymentMethod;
use App\Enums\SubscriptionPeriod;
use App\Enums\SubscriptionType;
use Devhammed\LaravelBrickMoney\Casts\AsCurrency;
use Devhammed\LaravelBrickMoney\Casts\AsIntegerMoney;
use Illuminate\Database\Eloquent\Model;
use Illuminate\Database\Eloquent\Relations\BelongsTo;
use Illuminate\Database\Eloquent\SoftDeletes;

class Subscription extends Model
{
    use softDeletes;
    protected function casts(): array
    {
        return [
            'currency' => AsCurrency::class,
            'payment_method' => PaymentMethod::class,
            'status' => 'boolean',
            'amount' => AsIntegerMoney::of('currency'),
            'interval' => 'integer',
            'period' => SubscriptionPeriod::class,
            'start_date_time' => 'datetime',
            'end_date_time' => 'datetime',
            'subscription_type' => SubscriptionType::class,

        ];
    }

    /**
     * @return BelongsTo<Client, $this>
     */
    public function client(): BelongsTo
    {
        return $this->belongsTo(Client::class);
    }
}
</file>

<file path="app/Models/SubscriptionTransaction.php">
<?php

namespace App\Models;

use Devhammed\LaravelBrickMoney\Casts\AsCurrency;
use Devhammed\LaravelBrickMoney\Casts\AsIntegerMoney;
use Illuminate\Database\Eloquent\Model;
use Illuminate\Database\Eloquent\Relations\BelongsTo;
use Illuminate\Database\Eloquent\Relations\HasOneThrough;

class SubscriptionTransaction extends Model
{
    protected $table = 'subscriptions_transactions';

    protected function casts(): array
    {
        return [
            'amount' => AsIntegerMoney::of('currency'),
            'currency' => AsCurrency::class,
            'pg_fees' => AsIntegerMoney::of('currency'),
            'pg_tax' => AsIntegerMoney::of('currency'),
            'transaction_date_time' => 'datetime',
            'data' => 'array',
        ];
    }

    /**
     * @return BelongsTo<Subscription, $this>
     */
    public function subscription(): BelongsTo
    {
        return $this->belongsTo(Subscription::class);
    }

    /**
     * @return HasOneThrough<Client, Subscription, $this>
     */
    public function client(): HasOneThrough
    {
        return $this->hasOneThrough(
            Client::class,
            Subscription::class,
            'id',
            'id',
            'subscription_id',
            'client_id',
        );
    }
}
</file>

<file path="app/Models/User.php">
<?php

namespace App\Models;

// use Illuminate\Contracts\Auth\MustVerifyEmail;
use Database\Factories\UserFactory;
use Illuminate\Database\Eloquent\Attributes\Fillable;
use Illuminate\Database\Eloquent\Attributes\Hidden;
use Illuminate\Database\Eloquent\Factories\HasFactory;
use Illuminate\Database\Eloquent\SoftDeletes;
use Illuminate\Foundation\Auth\User as Authenticatable;
use Illuminate\Notifications\Notifiable;
use Spatie\Permission\Traits\HasRoles;

#[Fillable(['name', 'email', 'password'])]
#[Hidden(['password', 'two_factor_secret', 'two_factor_recovery_codes', 'remember_token'])]
class User extends Authenticatable
{
    /** @use HasFactory<UserFactory> */
    use HasFactory, Notifiable, HasRoles,SoftDeletes;

    /**
     * Get the attributes that should be cast.
     *
     * @return array<string, string>
     */
    protected function casts(): array
    {
        return [
            'email_verified_at' => 'datetime',
            'password' => 'hashed',
        ];
    }
}
</file>

<file path="app/Policies/TransactionPolicy.php">
<?php

namespace App\Policies;

use App\Models\Transaction;
use App\Models\User;
use Illuminate\Auth\Access\Response;

class TransactionPolicy
{
    /**
     * Determine whether the user can view any models.
     */
    public function viewAny(User $user): bool
    {
        return $user->can('can_view_all_transactions') || $user->can('can_view_client');
    }

    /**
     * Determine whether the user can view the model.
     */
    public function view(User $user, Transaction $transaction): bool
    {
        if ($user->can('can_view_all_transactions')) {
            return true;
        }

        return $user->can('can_view_client')
            && $transaction->client->users()->whereKey($user->id)->exists();
    }

    /**
     * Determine whether the user can create models.
     */
    public function create(User $user): bool
    {
        return false;
    }

    /**
     * Determine whether the user can update the model.
     */
    public function update(User $user, Transaction $transaction): bool
    {
        return false;
    }

    /**
     * Determine whether the user can delete the model.
     */
    public function delete(User $user, Transaction $transaction): bool
    {
        return false;
    }

    /**
     * Determine whether the user can restore the model.
     */
    public function restore(User $user, Transaction $transaction): bool
    {
        return false;
    }

    /**
     * Determine whether the user can permanently delete the model.
     */
    public function forceDelete(User $user, Transaction $transaction): bool
    {
        return false;
    }
}
</file>

<file path="app/Policies/UserPolicy.php">
<?php

namespace App\Policies;

use App\Models\User;
use Illuminate\Auth\Access\Response;

class UserPolicy
{
    /**
     * Determine whether the user can view any models.
     */
    public function viewAny(User $user): bool
    {
        return ($user->can("can_view_all_user"));
    }

    /**
     * Determine whether the user can view the model.
     */
    public function view(User $user, User $users): bool
    {
        return ($user->can("can_view_user"));
    }

    /**
     * Determine whether the user can create models.
     */
    public function create(User $user): bool
    {
        return ($user->can("can_create_user"));
    }

    /**
     * Determine whether the user can update the model.
     */
    public function update(User $user, User $users): bool
    {
        return ($user->can("can_edit_user"));
    }

    /**
     * Determine whether the user can delete the model.
     */
    public function delete(User $user, User $users): bool
    {
        return ($user->can("can_delete_user"));
    }

    /**
     * Determine whether the user can restore the model.
     */
    public function restore(User $user, User $users): bool
    {
        return false;
    }

    /**
     * Determine whether the user can permanently delete the model.
     */
    public function forceDelete(User $user, User $users): bool
    {
        return false;
    }
}
</file>

<file path="app/Providers/AppServiceProvider.php">
<?php

namespace App\Providers;

use Carbon\CarbonImmutable;
use Illuminate\Database\Eloquent\Model;
use Illuminate\Support\Facades\Date;
use Illuminate\Support\Facades\DB;
use Illuminate\Support\ServiceProvider;
use Illuminate\Validation\Rules\Password;

class AppServiceProvider extends ServiceProvider
{
    /**
     * Register any application services.
     */
    public function register(): void
    {
        //
    }

    /**
     * Bootstrap any application services.
     */
    public function boot(): void
    {
        $this->configureDefaults();
    }

    /**
     * Configure default behaviors for production-ready applications.
     */
    protected function configureDefaults(): void
    {
        Model::unguard();
        Date::use(CarbonImmutable::class);

        DB::prohibitDestructiveCommands(
            app()->isProduction(),
        );

        Password::defaults(fn (): ?Password => app()->isProduction()
            ? Password::min(12)
                ->mixedCase()
                ->letters()
                ->numbers()
                ->symbols()
                ->uncompromised()
            : null,
        );
    }
}
</file>

<file path="app/Providers/Filament/DashboardPanelProvider.php">
<?php

namespace App\Providers\Filament;

use App\Filament\Widgets\DashboardStatsWidget;
use App\Filament\Widgets\RecentTransactionsWidget;
use Filament\Http\Middleware\Authenticate;
use Filament\Http\Middleware\AuthenticateSession;
use Filament\Http\Middleware\DisableBladeIconComponents;
use Filament\Http\Middleware\DispatchServingFilamentEvent;
use Filament\Pages\Dashboard;
use Filament\Panel;
use Filament\PanelProvider;
use Filament\Support\Colors\Color;
use Illuminate\Cookie\Middleware\AddQueuedCookiesToResponse;
use Illuminate\Cookie\Middleware\EncryptCookies;
use Illuminate\Foundation\Http\Middleware\PreventRequestForgery;
use Illuminate\Routing\Middleware\SubstituteBindings;
use Illuminate\Session\Middleware\StartSession;
use Illuminate\View\Middleware\ShareErrorsFromSession;

class DashboardPanelProvider extends PanelProvider
{
    public function panel(Panel $panel): Panel
    {
        return $panel
            ->default()
            ->id('dashboard')
            ->path('dashboard')
            ->login()
            ->colors([
                'primary' => Color::Green,
            ])
            ->navigationGroups([
                'Transactions',
                'Settings',
            ])
            ->discoverResources(in: app_path('Filament/Resources'), for: 'App\Filament\Resources')
            ->discoverPages(in: app_path('Filament/Pages'), for: 'App\Filament\Pages')
            ->pages([
                Dashboard::class,
            ])
            ->discoverWidgets(in: app_path('Filament/Widgets'), for: 'App\Filament\Widgets')
            ->widgets([
                DashboardStatsWidget::class,
                RecentTransactionsWidget::class,
            ])
            ->middleware([
                EncryptCookies::class,
                AddQueuedCookiesToResponse::class,
                StartSession::class,
                AuthenticateSession::class,
                ShareErrorsFromSession::class,
                PreventRequestForgery::class,
                SubstituteBindings::class,
                DisableBladeIconComponents::class,
                DispatchServingFilamentEvent::class,
            ])
            ->authMiddleware([
                Authenticate::class,
            ]);
    }
}
</file>

<file path="config/services.php">
<?php

return [

    /*
    |--------------------------------------------------------------------------
    | Third Party Services
    |--------------------------------------------------------------------------
    |
    | This file is for storing the credentials for third party services such
    | as Mailgun, Postmark, AWS and more. This file provides the de facto
    | location for this type of information, allowing packages to have
    | a conventional file to locate the various service credentials.
    |
    */

    'postmark' => [
        'key' => env('POSTMARK_API_KEY'),
    ],

    'resend' => [
        'key' => env('RESEND_API_KEY'),
    ],

    'ses' => [
        'key' => env('AWS_ACCESS_KEY_ID'),
        'secret' => env('AWS_SECRET_ACCESS_KEY'),
        'region' => env('AWS_DEFAULT_REGION', 'us-east-1'),
    ],

    'slack' => [
        'notifications' => [
            'bot_user_oauth_token' => env('SLACK_BOT_USER_OAUTH_TOKEN'),
            'channel' => env('SLACK_BOT_USER_DEFAULT_CHANNEL'),
        ],
    ],

    'pg_transaction_notifications' => [
        'email_enabled' => env('PG_TRANSACTION_EMAIL_ENABLED', true),
    ],

];
</file>

<file path="database/migrations/0001_01_01_000000_create_users_table.php">
<?php

use Illuminate\Database\Migrations\Migration;
use Illuminate\Database\Schema\Blueprint;
use Illuminate\Support\Facades\Schema;

return new class extends Migration
{
    /**
     * Run the migrations.
     */
    public function up(): void
    {
        Schema::create('users', function (Blueprint $table) {
            $table->id();
            $table->string('name');
            $table->string('email')->unique();
            $table->timestamp('email_verified_at')->nullable();
            $table->string('password');
            $table->rememberToken();
            $table->timestamp('deleted_at')->nullable();
            $table->timestamps();
        });

        Schema::create('password_reset_tokens', function (Blueprint $table) {
            $table->string('email')->primary();
            $table->string('token');
            $table->timestamp('created_at')->nullable();
        });

        Schema::create('sessions', function (Blueprint $table) {
            $table->string('id')->primary();
            $table->foreignId('user_id')->nullable()->index();
            $table->string('ip_address', 45)->nullable();
            $table->text('user_agent')->nullable();
            $table->longText('payload');
            $table->integer('last_activity')->index();
        });
    }

    /**
     * Reverse the migrations.
     */
    public function down(): void
    {
        Schema::dropIfExists('users');
        Schema::dropIfExists('password_reset_tokens');
        Schema::dropIfExists('sessions');
    }
};
</file>

<file path="database/migrations/2026_07_14_140115_create_transactions_table.php">
<?php

use Illuminate\Database\Migrations\Migration;
use Illuminate\Database\Schema\Blueprint;
use Illuminate\Support\Facades\Schema;

return new class extends Migration
{
    /**
     * Run the migrations.
     */
    public function up(): void
    {
        Schema::create('transactions', function (Blueprint $table) {
            $table->id();
            $table->foreignId('client_id')->constrained();
            $table->foreignId('client_customer_id')->constrained();
            $table->string('site_reference_id');
            $table->unique(['client_id', 'site_reference_id']);
            $table->foreignId('pg_connection_id')->constrained();
            $table->integer('amount');
            $table->string('currency')->default('INR');
            $table->integer('transaction_amount');
            $table->string('response_code')->comment('0: success, 1: failed');
            $table->string('status')->comment('PENDING, SUCCESS, FAILED');
            $table->string('payment_method');
            $table->string('transaction_purpose')->nullable();
            $table->string('transaction_id');
            $table->index(['transaction_id']);
            $table->integer('service_tax_amount')->default(0);
            $table->integer('processing_fee_amount')->default(0);
            $table->boolean('recover_pg_fees')->default(false)->comment('true: recover pg fees, false: do not recover');
            $table->integer('pg_fees')->default(0);
            $table->integer('total_amount')->default(0);
            $table->dateTime('transaction_date_time')->comment('transaction date time');
            $table->json('response_data')->nullable();
            $table->timestamps();
        });
    }

    /**
     * Reverse the migrations.
     */
    public function down(): void
    {
        Schema::dropIfExists('transactions');
    }
};
</file>

<file path="database/seeders/DatabaseSeeder.php">
<?php

namespace Database\Seeders;

use App\Models\User;
use Illuminate\Database\Console\Seeds\WithoutModelEvents;
use Illuminate\Database\Seeder;

class DatabaseSeeder extends Seeder
{
    use WithoutModelEvents;

    /**
     * Seed the application's database.
     */
    public function run(): void
    {
        $this->call([
            RolesAndPermissionsSeeder::class,
            SupportedPGSeeder::class,
        ]);

    }
}
</file>

<file path="routes/api-v1.php">
<?php

use App\Http\Controllers\API\V1\PaymentController;
use App\Http\Middleware\HandleApiClientEncryptedRequest;
use App\Http\Middleware\HandleApiRequest;
use Illuminate\Http\Request;
use Illuminate\Support\Facades\Route;

/*
Route::get('/user', function (Request $request) {
    return $request->user();
})->middleware('auth:sanctum');*/

Route::group(['middleware' => [HandleApiClientEncryptedRequest::class]], function () {
    Route::get('/initPayment', [PaymentController::class, 'initiatePayment'])
        ->name('initPayment');
});

Route::group(['middleware' => [HandleApiRequest::class]], function () {
    Route::get('/transaction/{reference_id}', [PaymentController::class, 'getTransactionDetails'])
        ->name('transaction.details');

    Route::get('/transactions', [PaymentController::class, 'getTransactions'])
        ->name('transactions.list');
});

Route::any('/handleResponse/{pgClass}', [PaymentController::class, 'handlePaymentResponse'])->name('handlePaymentResponse');
</file>

<file path="app/Models/ClientConnection.php">
<?php

namespace App\Models;

use App\Enums\ConnectionType;
use App\Enums\TransactionType;
use Illuminate\Database\Eloquent\Model;
use Illuminate\Database\Eloquent\Relations\BelongsTo;
use Illuminate\Database\Eloquent\Relations\HasOne;
use Illuminate\Database\Eloquent\SoftDeletes;

class ClientConnection extends Model
{
    use SoftDeletes;
    protected $table = 'clients_connections';

    protected $casts = [
        'pg_fees_recovery' => 'boolean',
        'is_recurring' => 'boolean',
        'status' => 'boolean',
        'type' => ConnectionType::class,
        'transaction_type' => TransactionType::class,
    ];

    /**
     * @return BelongsTo<Client, $this>
     */
    public function client(): BelongsTo
    {
        return $this->belongsTo(Client::class);
    }

    /**
     * @return BelongsTo<PGConnection, $this>
     */
    public function pgConnection(): BelongsTo
    {
        return $this->belongsTo(PGConnection::class);
    }
}
</file>

<file path="app/Models/Transaction.php">
<?php

namespace App\Models;

use App\Enums\PaymentMethod;
use App\Enums\TransactionStatus;
use Devhammed\LaravelBrickMoney\Casts\AsCurrency;
use Devhammed\LaravelBrickMoney\Casts\AsIntegerMoney;
use Illuminate\Database\Eloquent\Model;
use Illuminate\Database\Eloquent\Relations\BelongsTo;

class Transaction extends Model
{
    protected function casts(): array
    {
        return [
            'amount' => AsIntegerMoney::of('currency'),
            'transaction_amount' => AsIntegerMoney::of('currency'),
            'currency' => AsCurrency::class,
            'status' => TransactionStatus::class,
            'payment_method' => PaymentMethod::class,
            'service_tax_amount' => AsIntegerMoney::of('currency'),
            'processing_fee_amount' => AsIntegerMoney::of('currency'),
            'recover_pg_fees' => 'boolean',
            'pg_fees' => AsIntegerMoney::of('currency'),
            'total_amount' => AsIntegerMoney::of('currency'),
            'transaction_date_time' => "datetime",
            'request_data' => 'array',
            'response_data' => 'array',
        ];
    }

    /**
     * @return BelongsTo<ClientCustomer, $this>
     */
    public function customer(): BelongsTo
    {
        return $this->belongsTo(ClientCustomer::class);
    }

    /**
     * @return BelongsTo<Client, $this>
     */
    public function client(): BelongsTo
    {
        return $this->belongsTo(Client::class);
    }

    /**
     * @return BelongsTo<PGConnection, $this>
     */
    public function pgConnection(): BelongsTo
    {
        return $this->belongsTo(PGConnection::class, 'pg_connection_id');
    }
}
</file>

<file path="database/seeders/SupportedPGSeeder.php">
<?php

namespace Database\Seeders;

use App\Models\SupportedPaymentGateway;
use Illuminate\Database\Seeder;

class SupportedPGSeeder extends Seeder
{
    public function run(): void
    {
        echo "Running SupportedPGSeeder\n";
        $supportedPaymentGateways = [
            'ICICI' => ['pg_class'=> 'ICICI',
                'required' => [
                    'merchant_id' => 'string',
                    'aggregator_id' => 'string',
                    'supports_refunds' => 'boolean',
                    'encryption_key' => 'string',
                    'sub_merchant_id' => 'string',
                    'default_base_url' => 'url',
                    'paymode' => 'string',
                    'fees_included_in_amount' => 'boolean',
                    'fees_rate' => 'float',
            ]],
            'RAZORPAY' => [
                'pg_class'=> 'RAZORPAY',
                'required' => [
                    'supports_refunds' => 'boolean',
                    'key_id' => 'string',
                    'key_secret' => 'string',
                    'fees_included_in_amount' => 'boolean',
                    'fees_rate' => 'float',
            ]],
            'PAYPAL' => [
                'pg_class'=> 'PAYPAL',
                'required' => [
                    'mode' => 'radio|live,sandbox',
                    'paymentAction' => 'string|Sale',
                    'locale' => 'string|en_US',
                    'validate_ssl' => 'boolean',
                    'currency' => 'string|USD',
                    'client_id' => 'string',
                    'secret' => 'string',
                    'fees_included_in_amount' => 'boolean',
                    'return_url' => 'string',
            ]],
            /*'STRIPE' => ['required' => [
                'supports_refunds' => 'boolean',
                'key_id' => 'string',
                'key_secret' => 'string',
                'fees_included_in_amount' => 'boolean',
                'fees_rate' => 'float',
            ]],
            'CASHFREE' => ['required' => [
                'supports_refunds' => 'boolean',
                'key_id' => 'string',
                'key_secret' => 'string',
                'fees_included_in_amount' => 'boolean',
                'fees_rate' => 'float',
            ]],*/
        ];

        foreach ($supportedPaymentGateways as $name => $options) {
            $pg = SupportedPaymentGateway::updateOrCreate(
                ['name' => $name],
                ['name' => $name, 'pg_class' => $options['pg_class'], 'attributes' => $options]
            );
            echo $name . " inserted \n";
        }
        echo "Ran SupportedPGSeeder\n";
    }
}
</file>

<file path="routes/web.php">
<?php

use App\Http\Controllers\UtilsController;
use Illuminate\Support\Facades\Route;

Route::redirect('/', '/dashboard')->name('home');
Route::get("testPayment",[ UtilsController::class,'testPayment'])->name('testPayment');
</file>

</files>
